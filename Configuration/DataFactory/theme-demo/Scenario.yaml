# The records of the seed set "theme-demo", in the scenario format of
# typo3/testing-framework as sbuerk/data-factory reads it.
#
# UIDS
#
# Every record declares its uid, and the numbering is a rule rather than a
# sequence:
#
#   pages               1-10, the first tree, and 30-39, 50-56, 70-73,
#                       90-92, 110-112, 130, 150-151 and 170-177 - the
#                       pages added below its sections, forty-eight in all.
#                       The odd decades from 30 up are reserved; the ids
#                       actually used are fewer - see below for why the new
#                       ones skip decades
#   tt_content          the uid of its page times 100, plus its position on
#                       that page: the third element of page 6 is 603, the
#                       second of page 35 is 3502
#   tx_theme_list_item  1-18 on page 8, numbered in the order they are
#                       declared - the link list's four deliberately not in
#                       the order they render in, see there - then 100-129 on
#                       the pages of the icon content elements below it,
#                       150-177 on pages 90 to 92, 450-454 on page 90 with
#                       the card wall, 460-465 on page 150, 470-474 on page
#                       151, and 500-503 on page 170. Ninety-six records in
#                       all: the ranges are deliberately not contiguous, and
#                       the numbers between them are free
#
# The development instances mirror this tree below "/legacy/", generated by
# "Build/Scripts/generateLegacyScenario.php", which moves every uid by 1000
# in every table. For "tt_content" that is ten pages: the mirror of the
# content of page p takes the uids of the content of page p + 10. So a page
# with content never sits ten uids from another one:
#
#   11-19   taken by the mirror of the content of pages 1-9
#   20-29   the account pages of the instance set ("Accounts.yaml" of
#           "packages-dev/dev-site"), 2101 and 2201 their content
#   30-39   new pages, their mirrors' content in 40-49
#   50-59   new pages, their mirrors' content in 60-69
#
# and the next pages take 70-79, then 90-99. "GeneratedLegacyScenarioTest"
# fails on a uid the composed instance set declares twice.
#
# A declared "id" is a suggestion to DataHandler, honoured for an admin backend
# user only - which is why "data-factory:import" refuses to run as anybody
# else. A record without one would not get an auto increment uid either, but
# one from a counter starting at 10000, so nothing here relies on that.
#
# The uids are what the rest of the repository points at: the committed site
# configurations of the development instances name root page 1, the file
# references in "config.yml" name the records they hang on, the "shortcut"
# element names "tt_content_601", the link fields name "t3://page?uid=2", and
# the functional tests assert against pages 1 to 10.
#
# RELATIONS
#
# An inline relation needs no construct of its own. The parent writes the
# comma separated list of its children's declared ids into its relation field,
# the children are records of their own on the same page, and DataHandler
# resolves that list exactly like a backend form submit - writing "uid_foreign",
# "tablename" and "sorting_foreign" from the TCA of the parent field. Their
# order is the order of that list.
#
# "hidden: 0" sits on the wildcard entity and nowhere else. The "pages" TCA
# defaults the column to 1, so a page written without it exists and renders
# nothing. It must not be repeated on a declared entity: the wildcard is merged
# into each of them with array_merge_recursive(), so a key on both sides
# becomes the list [0, 0] and reaches the database as the string "Array".
entitySettings:
  '*':
    nodeColumnName: 'pid'
    columnNames: {id: 'uid'}
    defaultValues: {pid: 0, hidden: 0}
  page:
    isNode: true
    tableName: 'pages'
    parentColumnName: 'pid'
    defaultValues: {doktype: 1}
  content:
    tableName: 'tt_content'
  listItem:
    tableName: 'tx_theme_list_item'

entities:
  page:
    - self:
          id: 1
          title: 'Theme demo'
          slug: '/'
          is_siteroot: 1
          # "backend_layout" is an ordinary field of the record: everything a
          # scenario declares is written as it stands. The identifiers are the
          # ones registered in "Configuration/PageTsConfig/BackendLayouts".
          #
          # A site root is where the footer columns are conventionally edited,
          # which is what the "start" layout offers over "content".
          backend_layout: 'start'
      entities:
        content:
          - self:
                id: 101
                CType: 'header'
                header: 'A frontend to look at'
                header_layout: 1
          - self:
                id: 102
                CType: 'text'
                header: 'What this is'
                header_layout: 2
                bodytext: |
                  <p>This page tree is seeded from a definition in the repository, so a
                  development instance can be rebuilt from nothing without clicking a
                  page tree together by hand.</p>
                  <p>Three sections are in the navigation: the
                  <a href="t3://page?uid=5">content elements</a>, one page per
                  element with its variants; the
                  <a href="t3://page?uid=2">typography</a>, from rich text and
                  elements; and the <a href="t3://page?uid=9">styleguide</a>, which
                  renders every component of the library, with the
                  <a href="t3://page?uid=10">form showcase</a>, a request form put
                  together from the form components alone.</p>
      children:
        - self:
              id: 2
              title: 'Typography'
              slug: '/typography'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 201
                    CType: 'header'
                    header: 'Typography'
                    header_layout: 1
              - self:
                    id: 202
                    CType: 'text'
                    header: 'Running text'
                    header_layout: 2
                    bodytext: |
                      <p>A paragraph of running text, so the theme has something to set
                      in its body font. It carries <strong>bold</strong> and
                      <em>emphasised</em> words and a <a href="t3://page?uid=1">link</a>, which are
                      the three inline cases a stylesheet has to answer for.</p>

              # The appearance fields of the core "Appearance" tab and of the
              # header palette, one band each, so the four bands the theme
              # adds to "frame_class" can be looked at next to plain running
              # text. "frame_class", "space_*_class", "header_position" and
              # "tx_theme_header_style" become modifiers in
              # "Layouts/ContentElement.html" and
              # "Partials/ContentElement/Header.html"; the page TSconfig in
              # "Configuration/PageTsConfig/ContentElementAppearance.tsconfig"
              # offers exactly these values in the form.
              - self:
                    id: 203
                    CType: 'text'
                    header: 'A surface band, header centred'
                    header_layout: 2
                    header_position: 'center'
                    frame_class: 'surface'
                    space_before_class: 'large'
                    bodytext: |
                      <p>The frame "Surface band" fills the element with the surface
                      colour and draws the hairline around it. The header is centred,
                      and the space before the element is "large".</p>
              - self:
                    id: 204
                    CType: 'text'
                    header: 'A raised band, header at the end, looking like heading 4'
                    header_layout: 2
                    header_position: 'right'
                    tx_theme_header_style: 'h4'
                    frame_class: 'raised'
                    bodytext: |
                      <p>The raised surface is white in the light appearance, so the
                      hairline is what draws this band there. The header is an h2 set
                      in the look of heading 4.</p>
              - self:
                    id: 205
                    CType: 'text'
                    header: 'An accent band, header in the display size'
                    header_layout: 2
                    tx_theme_header_style: 'display'
                    frame_class: 'accent'
                    bodytext: |
                      <p>A tint of the primary accent of the active palette, with a
                      <a href="t3://page?uid=3">link to the media page</a> in it:
                      switch the palette, and the tint follows it.</p>
              - self:
                    id: 206
                    CType: 'text'
                    header: 'An inverse band, header at the start'
                    header_layout: 2
                    header_position: 'left'
                    frame_class: 'inverse'
                    space_after_class: 'extra-large'
                    bodytext: |
                      <p>The page in the other appearance: dark on a light page, light
                      on a dark one, with every colour of the theme turned with it -
                      the <a href="t3://page?uid=1">link to the start page</a> as well.
                      The space after the element is "extra large".</p>

          # The typography section: the rich text path, from records. The
          # styleguide (uid 9) shows the same components as literal markup;
          # these pages prove that a record reaches them. Seeded "bodytext" is
          # written through DataHandler like a backend save, so the RTE
          # processing of the installation applies to it - with the theme's
          # preset, everything below survives - but it never went through
          # CKEditor, which is why it holds more than the toolbar offers.
          # The first element of each page says so.
          children:
            - self:
                  id: 52
                  title: 'Text'
                  slug: '/typography/text'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5201
                        CType: 'header'
                        header: 'Text'
                        header_layout: 1
                  - self:
                        id: 5202
                        CType: 'text'
                        bodytext: |
                          <p class="theme-lead">Running text, headings inside it, the inline
                          semantics and the alignments of the rich text editor - all from
                          one text element each.</p>
                          <p>The rich text of these pages is written into the database the
                          way a backend save writes it, so the processing of the rich text
                          preset has run over it. It has not been typed into the editor:
                          several things here need its source view.</p>
                  - self:
                        id: 5203
                        CType: 'text'
                        header: 'Headings inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The header of the element is its heading 2. The rich text
                          continues the outline below it.</p>
                          <h3>A heading 3 inside the text</h3>
                          <p>The editor offers heading 2 and heading 3, so a text can carry
                          sections of its own without an element per section.</p>
                          <h3>A second heading 3, long enough to wrap onto a second line at
                          the width of the content column</h3>
                          <p>A long heading is balanced over its lines, so the last line is
                          not a single orphaned word.</p>
                  - self:
                        id: 5204
                        CType: 'text'
                        header: 'Inline semantics'
                        header_layout: 2
                        bodytext: |
                          <p><strong>Strong importance</strong> and <em>stress emphasis</em>,
                          <mark>a marked passage</mark>, <small>small print</small>,
                          <s>no longer accurate</s>, <del>deleted</del> and <ins>inserted</ins>
                          text, H<sub>2</sub>O and E = mc<sup>2</sup>.</p>
                          <p>An <abbr title="Rich Text Editor">RTE</abbr> writes
                          <code>&lt;p&gt;</code> around a paragraph; <kbd>Ctrl</kbd> +
                          <kbd>B</kbd> makes a word strong. A <dfn>content element</dfn> is
                          a record rendered into a column of a page. <q>A quotation inside
                          a sentence</q>, as <cite>the style guide</cite> puts it, gets its
                          quotation marks from the language.</p>
                  - self:
                        id: 5205
                        CType: 'text'
                        header: 'Alignment'
                        header_layout: 2
                        bodytext: |
                          <p class="theme-text--start">Aligned to the start of the line, which
                          is where every paragraph starts anyway.</p>
                          <p class="theme-text--center">Centred, for a short line that stands
                          on its own.</p>
                          <p class="theme-text--end">Aligned to the end of the line.</p>
                          <p class="theme-text--justify">Justified: both edges of the column
                          are straight, and the spaces between the words stretch to make them
                          so. Without hyphenation the gaps grow on a narrow column, which is
                          why the theme leaves justification to the paragraph that asks for
                          it.</p>
                  - self:
                        id: 5206
                        CType: 'text'
                        header: 'Language and direction'
                        header_layout: 2
                        bodytext: |
                          <p lang="de">Ein deutscher Absatz mit langen Zusammensetzungen wie
                          Inhaltselementdarstellung und Seitenbaumkonfigurationsdatei: mit der
                          Sprache am Absatz trennt der Browser sie am Zeilenende.</p>
                          <p dir="rtl" lang="ar">هذه فقرة قصيرة باللغة العربية، تبدأ من
                          اليمين وتنتهي في اليسار.</p>

            - self:
                  id: 53
                  title: 'Lists'
                  slug: '/typography/lists'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5301
                        CType: 'header'
                        header: 'Lists'
                        header_layout: 1
                  - self:
                        id: 5302
                        CType: 'text'
                        header: 'Lists inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The lists of the rich text keep the markers of the element
                          baseline, nested lists included:</p>
                          <ul>
                            <li>A first item</li>
                            <li>A second item, holding a list of its own
                              <ul>
                                <li>A nested item</li>
                                <li>A second nested item
                                  <ul><li>A third level</li></ul>
                                </li>
                              </ul>
                            </li>
                            <li>A last item</li>
                          </ul>
                          <ol>
                            <li>A first step</li>
                            <li>A second step, with sub steps
                              <ol><li>A sub step</li><li>Another sub step</li></ol>
                            </li>
                            <li>A last step</li>
                          </ol>
                  - self:
                        id: 5303
                        CType: 'text'
                        header: 'The list component inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The class of a list is kept by the rich text processing, so a
                          list in the source view can be one of the list component:</p>
                          <ul class="theme-list theme-list--check">
                            <li>Check marks, from the source view</li>
                            <li>The same component the bullet list element renders</li>
                          </ul>
                          <ul class="theme-list theme-list--columns-2">
                            <li>Header</li><li>Text</li><li>Text &amp; Images</li>
                            <li>Text &amp; Media</li><li>Images</li><li>Bullet list</li>
                            <li>Table</li><li>File links</li>
                          </ul>
                  # The element, rather than rich text: see "/elements/core/bullets"
                  # (uid 35) for every layout.
                  - self:
                        id: 5304
                        CType: 'bullets'
                        header: 'A definition list from the bullet list element'
                        header_layout: 2
                        bullets_type: 2
                        bodytext: |
                          Unstyled|No marker and no indent
                          Check|A check mark in front of every item
                          Icon|An icon of the set in front of every item
                          Columns|The items flow into two or three columns
                          Divided|A hairline between the rows
                  - self:
                        id: 5305
                        CType: 'text'
                        bodytext: '<p>Every layout of the bullet list element is on its <a href="t3://page?uid=35">own page</a>.</p>'

            - self:
                  id: 54
                  title: 'Tables'
                  slug: '/typography/tables'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5401
                        CType: 'header'
                        header: 'Tables'
                        header_layout: 1
                  # A table of the rich text keeps its classes through the
                  # parsing function of the frontend, but is not wrapped in the
                  # scroll region the table element renders - which is why the
                  # table element is the way to a wide table.
                  - self:
                        id: 5402
                        CType: 'text'
                        header: 'A table inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>A table of the rich text, with the classes of the table
                          component written in the source view. It has no scroll region of
                          its own, so it has to fit the column.</p>
                          <table class="theme-table theme-table--striped">
                            <caption>The two appearances</caption>
                            <thead><tr><th scope="col">Appearance</th><th scope="col">Background</th><th scope="col">Text</th></tr></thead>
                            <tbody>
                              <tr><th scope="row">Light</th><td>A near white</td><td>A near black</td></tr>
                              <tr><th scope="row">Dark</th><td>A near black</td><td>A near white</td></tr>
                            </tbody>
                          </table>
                  - self:
                        id: 5403
                        CType: 'table'
                        header: 'The table element, with a header column and a footer'
                        header_layout: 2
                        table_class: 'compact'
                        table_caption: 'Contrast of the text colours on the page background'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 2
                        table_tfoot: 1
                        bodytext: |
                          Primary text|The body copy|at least 7:1
                          Secondary text|Leads and captions|at least 4.5:1
                          Muted text|Dates and hints|at least 4.5:1
                          Every text colour|Holds AA on its surface|4.5:1
                  - self:
                        id: 5404
                        CType: 'text'
                        bodytext: '<p>Every class of the table element is on its <a href="t3://page?uid=36">own page</a>.</p>'

            - self:
                  id: 55
                  title: 'Quotes and code'
                  slug: '/typography/quotes-and-code'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5501
                        CType: 'header'
                        header: 'Quotes and code'
                        header_layout: 1
                  - self:
                        id: 5502
                        CType: 'text'
                        header: 'A quotation inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>A block quotation of the rich text, set apart from the running
                          text around it:</p>
                          <blockquote><p>Programs must be written for people to read, and only
                          incidentally for machines to execute.</p></blockquote>
                          <p>- <cite>Abelson and Sussman, Structure and Interpretation of
                          Computer Programs</cite></p>
                  # The testimonial element renders the quote component with its
                  # attribution; the name is "header", the role "subheader".
                  - self:
                        id: 5503
                        CType: 'theme_testimonial'
                        header: 'Harold Abelson and Gerald Jay Sussman'
                        subheader: 'Preface to the first edition, 1985'
                        bodytext: 'Programs must be written for people to read, and only incidentally for machines to execute.'
                  - self:
                        id: 5504
                        CType: 'text'
                        header: 'Preformatted text inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The editor offers "Pre-Formatted Text" as a heading option. It
                          keeps line breaks and spaces, and scrolls rather than wraps:</p>
                          <pre><code>tt_content.bullets =&lt; lib.contentElement
                          tt_content.bullets.templateName = ContentElements/Bullets</code></pre>
                  # The code block component needs a figure, a caption and a
                  # scroll region with a label - markup for the HTML element,
                  # which renders it untouched.
                  - self:
                        id: 5505
                        CType: 'html'
                        header: 'A code block with its file name (name only, not rendered)'
                        bodytext: |
                          <h2 class="theme-content-element__heading">A code block with its file name</h2>
                          <figure class="theme-code">
                              <figcaption class="theme-code__caption">
                                  <span class="theme-code__filename">config/sites/demo/config.yaml</span>
                                  <span class="theme-code__language">YAML</span>
                              </figcaption>
                              <pre class="theme-code__block" role="region" tabindex="0" aria-label="config/sites/demo/config.yaml"><code>base: 'https://theme.example.com/'
                          rootPageId: 1
                          dependencies:
                            - sbuerk/theme-extension-development</code></pre>
                          </figure>

            # A long form article: every component of the section in one
            # reading flow, the way a page of a site uses them.
            - self:
                  id: 56
                  title: 'Article'
                  slug: '/typography/article'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5601
                        CType: 'header'
                        header: 'How a content element reaches the page'
                        header_layout: 1
                  - self:
                        id: 5602
                        CType: 'text'
                        bodytext: |
                          <p class="theme-lead">An editor fills in a form and saves it. A
                          moment later a visitor reads a paragraph. Between the two lie a
                          database row, a few lines of configuration and a template - and
                          each of them decides something about what the visitor sees.</p>
                          <p>This article follows one element along that way. It is also a
                          specimen: a long text, set with the components a site package gets
                          from the theme, so the rhythm of headings, paragraphs, lists,
                          figures, tables and code can be read as a whole rather than one
                          specimen at a time.</p>
                  - self:
                        id: 5603
                        CType: 'text'
                        header: 'The record'
                        header_layout: 2
                        bodytext: |
                          <p>Everything starts as a row of the table <code>tt_content</code>.
                          Its type, the CType, decides which fields the form shows and which
                          rendering the frontend picks. The fields an editor fills in are
                          plain columns of that row: a header, a text, a layout, a frame.</p>
                          <h3>What the form does not show</h3>
                          <p>Some columns are there without a field in the form. The page the
                          element belongs to, its position in the column and its language are
                          written by the backend itself. They matter just as much: a content
                          element is rendered because a page asks for the content of one of
                          its columns, not because the element exists.</p>
                  - self:
                        id: 5604
                        CType: 'textpic'
                        header: 'The frame'
                        header_layout: 2
                        imageorient: 17
                        imagecols: 1
                        bodytext: |
                          <p>Every element is rendered through one frame, the layout
                          <code>ContentElement</code> of the theme. It writes the wrapper
                          with the anchor of the element, the CType as a modifier and as an
                          attribute, and the modifiers of the appearance fields: the band of
                          the frame, the space before and after.</p>
                          <p>The frame is also what the outlines of the development mode
                          draw. Switch them on in the display settings, and every element
                          of this article shows its boundary and its CType - the image
                          beside this text belongs to the same element as the text itself.</p>
                          <p>The image is placed in the text, at the end of the line. The
                          gallery processor of the core computes its width from the column
                          and the number of columns, and the theme floats it, so this text
                          flows around it and continues under it once it is past.</p>
                  - self:
                        id: 5605
                        CType: 'bullets'
                        header: 'What the frame reads'
                        header_layout: 2
                        bullets_type: 0
                        layout: 1
                        bodytext: |
                          The frame: default, no frame, or one of four bands
                          The space before and after the element
                          The alignment and the look of the header
                          The CType, for the modifier and the outline
                  - self:
                        id: 5606
                        CType: 'text'
                        header: 'The template'
                        header_layout: 2
                        bodytext: |
                          <p>Inside the frame, the template of the CType renders what is
                          particular to it. A text element renders its header and its rich
                          text; a table element renders a table from the lines of its text
                          field, split by a data processor before the template sees them.</p>
                          <blockquote><p>A template receives data it can render and never
                          computes what a processor could hand it.</p></blockquote>
                          <p>That division is the reason a table element and a table of the
                          rich text look the same: both end in the same component, one from
                          a processor, the other from the source view of the editor.</p>
                  - self:
                        id: 5607
                        CType: 'table'
                        header: 'The fields of a table element'
                        header_layout: 3
                        table_class: 'striped'
                        table_caption: 'What the table element reads, and what it does with it'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 1
                        bodytext: |
                          Field|Holds|Read by
                          bodytext|The rows, one per line|TableProcessor
                          table_delimiter|The character code of the column separator|TableProcessor
                          table_header_position|Header row, header column, or none|TableProcessor
                          table_class|The look of the table|Table.html
                  - self:
                        id: 5608
                        CType: 'html'
                        header: 'The TypoScript of the bullet list (name only, not rendered)'
                        bodytext: |
                          <figure class="theme-code">
                              <figcaption class="theme-code__caption">
                                  <span class="theme-code__filename">Configuration/TypoScript/ContentElements.typoscript</span>
                                  <span class="theme-code__language">TypoScript</span>
                              </figcaption>
                              <pre class="theme-code__block" role="region" tabindex="0" aria-label="Configuration/TypoScript/ContentElements.typoscript"><code>tt_content.bullets =&lt; lib.contentElement
                          tt_content.bullets {
                              templateName = ContentElements/Bullets
                          }</code></pre>
                          </figure>
                  - self:
                        id: 5609
                        CType: 'text'
                        header: 'What is left to the editor'
                        header_layout: 2
                        bodytext: |
                          <p>The editor chooses the type, fills in the fields and picks from
                          the values the form offers. The theme offers only values it
                          renders: a frame it does not draw is not in the list, and a field
                          it does not read is not in the form.</p>
                          <p>Everything else - the markup, the spacing, the colours in both
                          appearances - follows from the theme, which is what lets a page of
                          many elements read as one page.</p>

        - self:
              id: 3
              title: 'Media'
              slug: '/media'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 301
                    CType: 'header'
                    header: 'Media'
                    header_layout: 1
              # The image is attached in "config.yml", which names this uid.
              - self:
                    id: 302
                    CType: 'image'
                    header: 'A single image'
                    header_layout: 2
              # The gallery layout is driven by the TCA fields of the element,
              # not by the template: "imagecols" is what an editor changes in
              # the backend to get a second column.
              - self:
                    id: 303
                    CType: 'image'
                    header: 'Two images, two columns'
                    header_layout: 2
                    imagecols: 2

        # The one page that declares no "backend_layout" at all, and it stays
        # that way on purpose: it is the only page in the tree that reaches the
        # hard coded "default" fallback of "PageLayoutResolver", so that path
        # has no other coverage. Asserted by "ShowcaseTreeTest".
        - self:
              id: 4
              title: 'Empty page'
              slug: '/empty'
              # Out of the menus: it is a fixture of the layout fallback, not
              # a page a reviewer navigates to. The header would hold it - the
              # acceptance test of the header row holds seven top level
              # entries at 1280 pixels - but a reviewer has no reason to
              # open it from the menu. Reached by URL and by the links of the
              # theme elements on page 8.
              nav_hide: 1

        # The showcase branch. Its three children each demonstrate one family of
        # content elements, and between them they carry every CType this theme
        # renders - which "ShowcaseTreeTest" holds them to, by reading the
        # TypoScript rather than a list kept in the test.
        - self:
              id: 5
              title: 'Elements'
              slug: '/elements'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 501
                    CType: 'header'
                    header: 'Elements'
                    header_layout: 1
              - self:
                    id: 502
                    CType: 'text'
                    bodytext: |
                      <p>Three pages, one per family: the classic types
                      <em>EXT:frontend</em> registers without supplying a rendering,
                      the eleven menu types, and the types this extension adds itself.
                      Each of them appears on the page of its family, so a single branch
                      of the tree answers what the theme does with it.</p>
                      <p>The theme types with layouts of their own - the heroes, the call
                      to action and the testimonial - also have a page each below the
                      theme elements, showing every layout.</p>
                      <p>Below the classic types, each has a page of its own with its
                      variants, and the <a href="t3://page?uid=51">Frames</a> page shows
                      every value of the Appearance tab, one element each.</p>
          children:
            - self:
                  id: 6
                  title: 'Core elements'
                  slug: '/elements/core'
                  # Read by the "menu_abstract" and "menu_related_pages" elements
                  # on page 7, which list the children of page 5 - this page
                  # among them. Without an abstract the item renders as a bare
                  # link, and without a shared keyword the related-pages menu
                  # comes back empty; both look like the element is broken rather
                  # than like the target page is missing a field.
                  abstract: 'Every classic content type EXT:frontend registers without supplying a rendering for it.'
                  keywords: 'content elements, core'
                  # "content_sidebar" rather than "content", and its sibling
                  # page 8 deliberately does not use it: two pages under one
                  # parent rendering with and without the sub navigation is what
                  # proves the backend layout is resolved per page instead of
                  # inherited down the branch.
                  #
                  # Nothing is seeded into the sidebar column (colPos 1) and
                  # nothing needs to be: "Partials/Page/Sidebar.html" renders the
                  # sub navigation above the "sidebar" slot unconditionally, so
                  # the aside is populated on a page that puts all its content in
                  # "main".
                  backend_layout: 'content_sidebar'
              entities:
                content:
                  # "header" - the whole element is a heading, nothing else.
                  #
                  # The "shortcut" element at the bottom of this page names this
                  # one by its uid: "records" is a plain string column holding
                  # "tt_content_<uid>", so the target's uid has to be known while
                  # the scenario is written.
                  #
                  # "header_layout" is the heading level: 1-5 are <h1>-<h5>, 0
                  # falls back to <h2>, and 100 means "do not display" - see
                  # "Partials/ContentElement/Header.html".
                  - self:
                        id: 601
                        CType: 'header'
                        header: 'Core elements'
                        header_layout: 1

                  # "text" - "bodytext" through "f:format.html", which means
                  # "lib.parseFunc_RTE": links are rewritten by typolink and
                  # lines that are not already wrapped are wrapped in <p>.
                  #
                  # The markup available here is not free-form. This CType's
                  # "bodytext" is declared "enableRichtext", so DataHandler runs
                  # the RTE "db" transformation over it ON SAVE, and which tags
                  # survive depends on the installation. With rte_ckeditor it is
                  # the processing of the rich text preset - the theme's own
                  # imports the core "Processing.yaml", which keeps "code" and
                  # "kbd". Without it, it is "RteHtmlParser::$defaultAllowedTagsList"
                  # (typo3/cms-core, Classes/Html/RteHtmlParser.php), which has
                  # neither. Dropping a tag is not an error and reports nothing;
                  # it is simply not in the row afterwards. So the copy of this
                  # element uses neither, and the "html" element further down is
                  # the one place on this page where they survive in every
                  # installation - its copy says so.
                  - self:
                        id: 602
                        CType: 'text'
                        header: 'What this page is'
                        header_layout: 2
                        bodytext: |
                          <p>Every classic content type this theme renders is on this
                          page exactly once, so a single URL answers what each one
                          looks like and which of its fields the theme actually reads.
                          The copy of each element describes that element.</p>
                          <p>The rendering is defined in
                          <em>Configuration/TypoScript/ContentElements.typoscript</em>.
                          This theme does not depend on <em>fluid_styled_content</em>,
                          so a content type without a branch in that file would render
                          the core's <strong>"no rendering definition"</strong> notice
                          instead of anything at all.</p>
                          <p>Each of them also has a page of its own below this one, in the
                          sub navigation, showing its variants: every orientation of
                          <a href="t3://page?uid=32">Text &amp; Images</a>, every layout of
                          the <a href="t3://page?uid=35">bullet list</a>, every class of the
                          <a href="t3://page?uid=36">table</a> and so on.</p>

                  # "bullets" - "bodytext" is one item per line, split by the
                  # core's own "SplitProcessor" (delimiter LF,
                  # "removeEmptyEntries = 1", so the trailing newline of this
                  # block scalar does not become an empty item).
                  #
                  # "bullets_type" selects the list element: 0 = <ul>, 1 = <ol>,
                  # 2 = <dl>. 0 is also the TCA default; it is written out anyway
                  # so the value the copy below talks about is visible next to it.
                  - self:
                        id: 603
                        CType: 'bullets'
                        header: 'Bullet list'
                        header_layout: 2
                        bullets_type: 0
                        bodytext: |
                          One item per line, with no markup and no delimiter
                          Each line is escaped on output, so a typed tag stays text
                          bullets_type 1 renders the same lines as an ordered list
                          bullets_type 2 renders them as a definition list of terms

                  # "table" - the element whose fields are least like their names.
                  #
                  # "bodytext" is delimiter separated text, one row per line, and
                  # "TableProcessor" (Classes/DataProcessing/TableProcessor.php)
                  # decodes it into header/body/footer rows before the template
                  # sees it. The fields that control the decode store CHARACTER
                  # CODES, not characters:
                  #
                  #   table_delimiter  the column separator, TCA default 124 = "|"
                  #                    (the other offered codes are 59, 44, 58, 9)
                  #   table_enclosure  the quoting character, 0 = none
                  #                    (the other offered codes are 39 and 34)
                  #
                  # 0 for "no enclosure" never reaches PHP as an empty string:
                  # "fgetcsv()" rejects that with a ValueError, so the processor
                  # passes chr(0) instead. That matters for the content here -
                  # with no real enclosure character, no cell below may contain
                  # the delimiter, which is why the first body row spells "124"
                  # out in words instead of showing the character.
                  #
                  # The remaining two are ordinary:
                  #
                  #   table_header_position  0 = none, 1 = first row is <thead>,
                  #                          2 = first cell of every row is a
                  #                              <th scope="row">
                  #   table_tfoot            1 = the last remaining row (after the
                  #                              header row was taken off) becomes
                  #                              <tfoot>
                  #
                  # "cols" and "table_class" exist in the core TCA of this element
                  # and are deliberately not set: the processor pads every row to
                  # the widest one by itself, and the template styles the table
                  # through ".theme-table" rather than a class from the record.
                  - self:
                        id: 604
                        CType: 'table'
                        header: 'Table'
                        header_layout: 2
                        table_caption: 'The five table_* fields, and what this element sets them to'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 1
                        table_tfoot: 1
                        bodytext: |
                          Field|What it stores|Value on this element
                          table_delimiter|The character CODE of the column separator|124, a vertical bar
                          table_enclosure|The character CODE of the quoting character, 0 for none|0
                          table_header_position|0 none, 1 first row, 2 first column|1
                          table_tfoot|Whether the last row becomes a table footer|1
                          Decoded by|Classes/DataProcessing/TableProcessor.php|not by Table.html

                  # "textpic" - text plus the core "image" field, laid out by
                  # "GalleryProcessor" from "imageorient" and "imagecols".
                  #
                  # "imageorient" 17 is "in text, right"; the values are 0/1/2
                  # above, 8/9/10 below and 17/18 in text - the processor turns
                  # them into the vertical/horizontal position the gallery partial
                  # renders as modifier classes. Together with "imagecols: 1" this
                  # is deliberately the opposite of the "textmedia" element below,
                  # so the two are distinguishable on the page rather than looking
                  # like one element seeded twice.
                  - self:
                        id: 605
                        CType: 'textpic'
                        header: 'Text & Images'
                        header_layout: 2
                        imageorient: 17
                        imagecols: 1
                        bodytext: |
                          <p>Reads the <em>image</em> field, which the core TCA
                          restricts to images. The gallery is placed by
                          <em>imageorient</em> - 17 here, "in text, right" - and split
                          into <em>imagecols</em> columns, both of them computed by
                          the core's <strong>GalleryProcessor</strong> rather than by
                          the template.</p>

                  # "textmedia" - identical to "textpic" except for the source
                  # field: "assets" allows every "common-media-type", not only
                  # images. Same two processors, and "GalleryProcessor" does not
                  # care which field the files came from.
                  #
                  # "imageorient" 8 is "below, centered", so this is also the one
                  # element on the page where the template puts the body text
                  # before the gallery in the DOM.
                  - self:
                        id: 606
                        CType: 'textmedia'
                        header: 'Text & Media'
                        header_layout: 2
                        imageorient: 8
                        imagecols: 2
                        bodytext: |
                          <p>Reads the <em>assets</em> field instead of
                          <em>image</em>, so a video or an audio file is allowed here
                          where "Text &amp; Images" above would refuse it. With
                          <em>imageorient</em> 8 the media sits below the text and
                          centered, and <em>imagecols</em> 2 puts the two files side
                          by side.</p>

                  # "image" - the same two processors as "textpic" without the
                  # body text. Kept minimal on purpose: the Media page (uid 3)
                  # already demonstrates the gallery settings, and this page only
                  # has to be complete, not to repeat them.
                  - self:
                        id: 607
                        CType: 'image'
                        header: 'Images'
                        header_layout: 2

                  # "uploads" - a file list, not a gallery.
                  #
                  # The fields "Uploads.html" actually reads are all set here:
                  #
                  #   uploads_type          0 = name only, 1 = name and file type
                  #                         icon, 2 = name and thumbnail, the
                  #                         modifiers of ".theme-file-list". 2
                  #                         renders a thumbnail only for files
                  #                         FAL classifies as an image, which an
                  #                         SVG is (image/* maps to
                  #                         FileType::IMAGE), and the icon of the
                  #                         file type for any other.
                  #   filelink_size         shows the byte count
                  #   uploads_description   shows the description of the file
                  #                         REFERENCE - which is why both
                  #                         references in "config.yml" carry one,
                  #                         and why they differ from the ones the
                  #                         same files carry elsewhere on this
                  #                         page.
                  #   filelink_sorting      "FilesProcessor" sorts natively, so
                  #                         this overrides the order the
                  #                         references are declared in - by name
                  #                         ascending, the portrait file first.
                  #                         That is the point of setting it.
                  #
                  # "target" is left at its default (empty): the template passes
                  # it straight into the link, and a demo page has no business
                  # forcing new windows.
                  #
                  # "file_collections" is the second source "FilesProcessor" is
                  # wired to and stays empty: a demonstration of one field does
                  # not justify seeding a "sys_file_collection".
                  - self:
                        id: 608
                        CType: 'uploads'
                        header: 'File links'
                        header_layout: 2
                        uploads_type: 2
                        filelink_size: 1
                        uploads_description: 1
                        filelink_sorting: 'name'
                        filelink_sorting_direction: 'asc'

                  # "html" - "bodytext" straight out, through "f:format.raw": no
                  # escaping, no parseFunc, no RTE. That is the entire element.
                  #
                  # Its "header" is not rendered - EXT:frontend relabels the
                  # field to "Name (not visible in frontend)" for this CType, the
                  # same way it does for "div" and "shortcut" - so the copy below
                  # has to carry its own heading, and it also introduces those
                  # two elements, which follow it and have no visible header
                  # either.
                  - self:
                        id: 609
                        CType: 'html'
                        header: 'Plain HTML (name only, not rendered)'
                        bodytext: |
                          <h2 class="theme-content-element__heading">Plain HTML</h2>
                          <p>This paragraph, its heading and the class on that heading
                          were written as raw markup in <code>bodytext</code> and
                          reached the page untouched. Nothing was escaped and no rich
                          text parsing ran, which is what separates this element from
                          "Text" further up - the <code>code</code> tags in this
                          sentence survive here in every installation. That
                          element's field is declared <em>enableRichtext</em>, so a
                          save keeps only what the processing of the rich text preset
                          allows: <code>code</code> with the theme's preset, nothing
                          of the kind without the rich text editor installed. Access to raw HTML is not restricted by
                          anything in the frontend: the <code>CType</code> field
                          carries <code>authMode = explicitAllow</code>, so a
                          non-admin editor can only pick it if a backend group grants
                          it explicitly.</p>
                          <p>Two elements follow that have no visible header either.
                          The horizontal rule below is the <strong>Divider</strong>
                          element, which renders nothing but that rule. After it,
                          <strong>Insert Records</strong> renders the page's own
                          "Core elements" heading a second time - it is a reference to
                          the first element on this page, not a copy of it.</p>

                  # "div" - a horizontal rule and nothing else. The core TCA gives
                  # this CType exactly one field besides "CType", and relabels it
                  # to "Name (not visible in frontend)", so the header below is
                  # there for the backend and the page module only.
                  - self:
                        id: 610
                        CType: 'div'
                        header: 'Divider before the record reference'

                  # "shortcut" - renders other content elements in place.
                  #
                  # "records" is a "type: group" field limited to "tt_content"
                  # (EXT:frontend/Configuration/TCA/tt_content.php). Its value is
                  # a comma separated list, each entry in the form
                  # "<tablename>_<uid>": "RecordsContentObject" hands it to
                  # "RelationHandler", which reverses the string and splits it on
                  # the first "_" from the right, so the id is the part after the
                  # last underscore and everything before it is the table name. A
                  # bare uid works too - a single allowed table makes it fall
                  # back to that table - but the qualified form is what the
                  # backend writes and what makes the scenario readable.
                  #
                  # This is also the element that demonstrates the cycle break of
                  # PR #19: inside a shortcut, "tt_content.shortcut" is redefined
                  # to render nothing, so a shortcut reached through a shortcut
                  # produces no output. TYPO3 v13.4 has a recursion guard of its
                  # own ("recordRegister"), v14 has none, and this element renders
                  # identically on both because the theme does not rely on it.
                  - self:
                        id: 611
                        CType: 'shortcut'
                        header: 'Insert records (name only, not rendered)'
                        records: 'tt_content_601'

              # One page per classic CType, below the page that shows each of
              # them once. The page is named after the CType and shows its
              # variants: every value of the field that changes how the
              # element looks, one element each. "ShowcaseTreeTest" derives
              # the CTypes from the TypoScript and the values from the TCA and
              # the page TSconfig, and holds these pages to both.
              #
              # "content_sidebar" like their parent, so the sub navigation
              # lists the sibling pages next to every one of them.
              children:
                # "header" - "header_layout" is the level, "header_position"
                # the alignment. The look independent of the level,
                # "tx_theme_header_style", is on the Frames page (uid 51).
                - self:
                      id: 30
                      title: 'Header'
                      slug: '/elements/core/header'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3001
                            CType: 'header'
                            header: 'Header'
                            header_layout: 1
                      - self:
                            id: 3002
                            CType: 'text'
                            header: 'Levels and positions'
                            header_layout: 2
                            bodytext: |
                              <p>The element is a heading and nothing else. Its level is the
                              value of "Layout" in the header palette, heading 1 to heading 5;
                              "Default" is a heading 2, and "Hidden" keeps the record and
                              renders nothing. The alignment moves the heading to the start,
                              the centre or the end of the line - the logical edges, so it
                              follows a right-to-left language.</p>
                      - self:
                            id: 3003
                            CType: 'header'
                            header: 'Heading level 2'
                            header_layout: 2
                      - self:
                            id: 3004
                            CType: 'header'
                            header: 'Heading level 3'
                            header_layout: 3
                      - self:
                            id: 3005
                            CType: 'header'
                            header: 'Heading level 4'
                            header_layout: 4
                      - self:
                            id: 3006
                            CType: 'header'
                            header: 'Heading level 5'
                            header_layout: 5
                      - self:
                            id: 3007
                            CType: 'header'
                            header: 'The default level, rendered as heading 2'
                            header_layout: 0
                      # Rendered as the wrapper alone: the record exists, the
                      # heading does not. Visible with the outlines switched on.
                      - self:
                            id: 3008
                            CType: 'header'
                            header: 'Hidden, and not displayed'
                            header_layout: 100
                      - self:
                            id: 3009
                            CType: 'header'
                            header: 'Centred'
                            header_layout: 2
                            header_position: 'center'
                      - self:
                            id: 3010
                            CType: 'header'
                            header: 'At the end of the line'
                            header_layout: 2
                            header_position: 'right'
                      - self:
                            id: 3011
                            CType: 'header'
                            header: 'At the start of the line'
                            header_layout: 2
                            header_position: 'left'
                      - self:
                            id: 3012
                            CType: 'header'
                            header: 'No alignment chosen'
                            header_layout: 2
                            header_position: ''

                # "text" - the rich text field and nothing else. The
                # typography section (below page 2) shows what rich text can
                # hold; this page shows the element.
                - self:
                      id: 31
                      title: 'Text'
                      slug: '/elements/core/text'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3101
                            CType: 'header'
                            header: 'Text'
                            header_layout: 1
                      - self:
                            id: 3102
                            CType: 'text'
                            header: 'Running text'
                            header_layout: 2
                            bodytext: |
                              <p>The text element renders its header and its rich text, and
                              the rich text goes through the parsing function of the frontend,
                              so a <a href="t3://page?uid=35">link to another page</a> is
                              resolved and <strong>strong</strong> and <em>emphasised</em>
                              words keep their meaning.</p>
                              <p>A second paragraph, so the space between two of them is on
                              the page as well.</p>
                      - self:
                            id: 3103
                            CType: 'text'
                            header: 'A list and a quotation inside the text'
                            header_layout: 2
                            bodytext: |
                              <p>Rich text holds lists and quotations of its own:</p>
                              <ul><li>an unordered list,</li><li>with a second item,</li></ul>
                              <ol><li>an ordered one,</li><li>numbered by the browser.</li></ol>
                              <blockquote><p>A quotation set apart from the text around it.</p></blockquote>
                      - self:
                            id: 3104
                            CType: 'text'
                            bodytext: |
                              <p>This element has no header, so it starts with its text.</p>
                      # "layout" 1 sets the text in columns - two where each
                      # gets 30 characters, one on a phone. Long enough to
                      # break, with a heading, a list and a quotation that
                      # move to the next column whole rather than split.
                      - self:
                            id: 3105
                            CType: 'text'
                            header: 'Text in columns'
                            header_layout: 2
                            layout: 1
                            bodytext: |
                              <p>The layout Columns sets the rich text of the element in
                              two columns wherever each of them can be at least thirty
                              characters wide, and in one column where it cannot - on a
                              phone, or in a narrow sidebar. The text itself is the same
                              markup as in running text; only the block around it
                              changes.</p>
                              <p>A hairline separates the columns. A paragraph keeps at
                              least two of its lines on either side of a break, so a
                              column never starts or ends with a single stranded
                              line.</p>
                              <h3>What stays in one piece</h3>
                              <p>A heading moves with the paragraph it opens, and the
                              blocks below are never cut in half by a column
                              break:</p>
                              <ul><li>a quotation,</li><li>a table or a code block,</li><li>every item of a list.</li></ul>
                              <blockquote><p>Columns suit short, self-contained text: a
                              notice, a set of facts, an imprint. A long article reads
                              better in one column at the measure.</p></blockquote>
                              <p>The first element of the text starts at the top of the
                              first column, level with the second.</p>

                # "textpic" - one element per value of "imageorient", in the
                # order the select offers them. The in-text values carry a
                # portrait image and the others a landscape one, and all share
                # one paragraph long enough to show where the text runs past
                # the image.
                - self:
                      id: 32
                      title: 'Text & Images'
                      slug: '/elements/core/textpic'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3201
                            CType: 'header'
                            header: 'Text & Images'
                            header_layout: 1
                      - self:
                            id: 3202
                            CType: 'textpic'
                            header: 'Above, centred (0)'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 1
                            bodytext: &textpicBody |
                              <p>The position of the image is the value of
                              <em>imageorient</em> in the heading. The core's
                              <strong>GalleryProcessor</strong> turns it into a vertical and a
                              horizontal position, and the gallery partial turns those into
                              modifier classes. This paragraph is long enough to run past an
                              image placed in the text, which is what tells the variants that
                              let the text flow back under the image from the ones that keep it
                              beside the image.</p>
                      - self:
                            id: 3203
                            CType: 'textpic'
                            header: 'Above, right (1)'
                            header_layout: 2
                            imageorient: 1
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3204
                            CType: 'textpic'
                            header: 'Above, left (2)'
                            header_layout: 2
                            imageorient: 2
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3205
                            CType: 'textpic'
                            header: 'Below, centred (8)'
                            header_layout: 2
                            imageorient: 8
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3206
                            CType: 'textpic'
                            header: 'Below, right (9)'
                            header_layout: 2
                            imageorient: 9
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3207
                            CType: 'textpic'
                            header: 'Below, left (10)'
                            header_layout: 2
                            imageorient: 10
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3208
                            CType: 'textpic'
                            header: 'In text, right (17)'
                            header_layout: 2
                            imageorient: 17
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3209
                            CType: 'textpic'
                            header: 'In text, left (18)'
                            header_layout: 2
                            imageorient: 18
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3210
                            CType: 'textpic'
                            header: 'In text, right, no wrap (25)'
                            header_layout: 2
                            imageorient: 25
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3211
                            CType: 'textpic'
                            header: 'In text, left, no wrap (26)'
                            header_layout: 2
                            imageorient: 26
                            imagecols: 1
                            bodytext: *textpicBody

                # "textmedia" - the same processors as "textpic" on the
                # "assets" field, so four orientations and three column counts
                # are enough to show it is the same layout.
                - self:
                      id: 33
                      title: 'Text & Media'
                      slug: '/elements/core/textmedia'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3301
                            CType: 'header'
                            header: 'Text & Media'
                            header_layout: 1
                      - self:
                            id: 3302
                            CType: 'textmedia'
                            header: 'Two files above the text, two columns'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 2
                            bodytext: &textmediaBody |
                              <p>The same gallery as "Text &amp; Images", read from the
                              <em>assets</em> field, which also takes a video or an audio
                              file. Orientation and columns are the values in the heading.</p>
                      - self:
                            id: 3303
                            CType: 'textmedia'
                            header: 'One file in the text, right'
                            header_layout: 2
                            imageorient: 17
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3304
                            CType: 'textmedia'
                            header: 'One file in the text, left, no wrap'
                            header_layout: 2
                            imageorient: 26
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3305
                            CType: 'textmedia'
                            header: 'Three files below the text, three columns'
                            header_layout: 2
                            imageorient: 8
                            imagecols: 3
                            bodytext: *textmediaBody
                      # The two media types "assets" takes that "image" does
                      # not. Both render as ".theme-media" rather than through
                      # "f:image" - see "Partials/ContentElement/Gallery.html".
                      # The film carries a caption track, in
                      # "tx_theme_captions": a field of the theme, because a
                      # file reference has nowhere to keep one. It is paired
                      # with the film by its file name.
                      #
                      # THE FILM DOES NOT PLAY, AND IS NOT MEANT TO. It is an
                      # "ftyp" and a "free" box - the header that makes the
                      # file "video/mp4" and nothing else - so a browser draws
                      # the player and its controls and then fails to decode
                      # it. A playable film needs an encoder, and committing
                      # one would put a megabyte of video in an extension
                      # whose showcase is about markup. The audio file beside
                      # it IS playable: a second of a 440 Hz tone. See
                      # "config.yml" and "docs/development/seeding.md".
                      - self:
                            id: 3306
                            CType: 'textmedia'
                            header: 'A video, with a caption track'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 1
                            bodytext: >-
                              <p>A video file in the <em>assets</em> field renders as a
                              native player with its controls, and the WebVTT file of
                              the same name becomes its caption track. The file itself
                              is a placeholder that carries no picture, so the player
                              shows its controls and nothing else.</p>
                      - self:
                            id: 3307
                            CType: 'textmedia'
                            header: 'An audio file'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 1
                            bodytext: >-
                              <p>An audio file renders as a native player as well. It
                              draws no picture, so its height is the browser's and only
                              its width is the theme's. This one is a second of a
                              440&nbsp;Hz tone.</p>

                # "image" - one element per column count up to four, each with
                # as many files as columns. The select offers up to eight; at
                # the gallery width of the theme a fifth column is a row of
                # thumbnails, which the page has no reason to show.
                - self:
                      id: 34
                      title: 'Images'
                      slug: '/elements/core/image'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3401
                            CType: 'header'
                            header: 'Images'
                            header_layout: 1
                      - self:
                            id: 3402
                            CType: 'image'
                            header: 'One column'
                            header_layout: 2
                            imagecols: 1
                      - self:
                            id: 3403
                            CType: 'image'
                            header: 'Two columns'
                            header_layout: 2
                            imagecols: 2
                      - self:
                            id: 3404
                            CType: 'image'
                            header: 'Three columns'
                            header_layout: 2
                            imagecols: 3
                      - self:
                            id: 3405
                            CType: 'image'
                            header: 'Four columns'
                            header_layout: 2
                            imagecols: 4
                      # "image_zoom" - the editor's "Enlarge on click". Every
                      # image of this element carries the badge and the link to
                      # the file, and with the theme's script the link opens the
                      # lightbox instead, which moves between the three.
                      - self:
                            id: 3406
                            CType: 'image'
                            header: 'Three columns, enlargeable'
                            header_layout: 2
                            imagecols: 3
                            image_zoom: 1

                # "bullets" - every "layout" on an unordered list, two on an
                # ordered one, and the definition list, which reads
                # "term|description" per line and ignores the layout. See
                # "Templates/ContentElements/Bullets.html".
                - self:
                      id: 35
                      title: 'Bullet list'
                      slug: '/elements/core/bullets'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3501
                            CType: 'header'
                            header: 'Bullet list'
                            header_layout: 1
                      - self:
                            id: 3502
                            CType: 'bullets'
                            header: 'Bullets or numbers'
                            header_layout: 2
                            bullets_type: 0
                            layout: 0
                            bodytext: &bulletsBody |
                              One item per line, typed into a plain text field
                              Each line is escaped, so a typed tag stays text
                              The layout picks the marker of the whole list
                      - self:
                            id: 3503
                            CType: 'bullets'
                            header: 'Check marks'
                            header_layout: 2
                            bullets_type: 0
                            layout: 1
                            bodytext: *bulletsBody
                      # The icon of the list is picked in "tx_theme_icon", the
                      # icon picker of the theme, from the curated list of
                      # "IconPicker.tsconfig". Without one the layout renders
                      # the markers of layout 0 - see "Bullets.html".
                      - self:
                            id: 3504
                            CType: 'bullets'
                            header: 'Icons'
                            header_layout: 2
                            bullets_type: 0
                            layout: 2
                            tx_theme_icon: 'star'
                            bodytext: *bulletsBody
                      - self:
                            id: 3505
                            CType: 'bullets'
                            header: 'Inline'
                            header_layout: 2
                            bullets_type: 0
                            layout: 3
                            bodytext: |
                              Header
                              Text
                              Bullet list
                              Table
                              File links
                      - self:
                            id: 3506
                            CType: 'bullets'
                            header: 'Numbered'
                            header_layout: 2
                            bullets_type: 1
                            layout: 0
                            bodytext: |
                              Pick the list type "Numbered list"
                              Type one step per line
                              Save, and the browser numbers the steps
                      - self:
                            id: 3507
                            CType: 'bullets'
                            header: 'Numbered, with check marks'
                            header_layout: 2
                            bullets_type: 1
                            layout: 1
                            bodytext: |
                              The list stays ordered for a screen reader
                              The check mark replaces the number on screen
                      - self:
                            id: 3508
                            CType: 'bullets'
                            header: 'Definition list'
                            header_layout: 2
                            bullets_type: 2
                            bodytext: |
                              Bullets or numbers|The markers of the element baseline
                              Check marks|A check mark in front of every item
                              Icons|One icon of the set in front of every item
                              Inline|The items on one line, a hairline between them
                              A term without a description

                # "table" - one element per "table_class": the three of the
                # core TCA and the five "TCEFORM/TableClass.tsconfig" adds, all
                # on the same rows so only the class differs.
                - self:
                      id: 36
                      title: 'Table'
                      slug: '/elements/core/table'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3601
                            CType: 'header'
                            header: 'Table'
                            header_layout: 1
                      - self:
                            id: 3602
                            CType: 'table'
                            header: 'Default, with a header row and a footer'
                            header_layout: 2
                            table_class: ''
                            table_caption: 'The spacing scale'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            table_tfoot: 1
                            bodytext: &tableBody |
                              Value|Token|Used for
                              extra-small|--theme-space-2|Tight groups
                              small|--theme-space-4|Related elements
                              medium|--theme-space-6|The default gap
                              large|--theme-space-8|A new topic
                              extra-large|--theme-space-10|A new section
                      - self:
                            id: 3603
                            CType: 'table'
                            header: 'Striped'
                            header_layout: 2
                            table_class: 'striped'
                            table_caption: 'The spacing scale, striped'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3604
                            CType: 'table'
                            header: 'Bordered'
                            header_layout: 2
                            table_class: 'bordered'
                            table_caption: 'The spacing scale, bordered'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3605
                            CType: 'table'
                            header: 'Striped columns'
                            header_layout: 2
                            table_class: 'striped-columns'
                            table_caption: 'The spacing scale, striped columns'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3606
                            CType: 'table'
                            header: 'Hover'
                            header_layout: 2
                            table_class: 'hover'
                            table_caption: 'The spacing scale, a tint on the row under the pointer'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3607
                            CType: 'table'
                            header: 'Borderless'
                            header_layout: 2
                            table_class: 'borderless'
                            table_caption: 'The spacing scale, borderless'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3608
                            CType: 'table'
                            header: 'Compact, with a header column'
                            header_layout: 2
                            table_class: 'compact'
                            table_caption: 'The spacing scale, compact'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 2
                            bodytext: *tableBody
                      - self:
                            id: 3609
                            CType: 'table'
                            header: 'Sticky header'
                            header_layout: 2
                            table_class: 'sticky-header'
                            table_caption: 'The spacing scale, the header row stays in view'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody

                # "uploads" - one element per "uploads_type". The two files
                # and their references are declared in "config.yml".
                - self:
                      id: 37
                      title: 'File links'
                      slug: '/elements/core/uploads'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3701
                            CType: 'header'
                            header: 'File links'
                            header_layout: 1
                      - self:
                            id: 3702
                            CType: 'uploads'
                            header: 'Only the file name (0)'
                            header_layout: 2
                            uploads_type: 0
                      # The icon of a file type in front of every file - both
                      # files are SVG, so both get the image icon. Element
                      # 3705 below shows the icons of other types.
                      - self:
                            id: 3703
                            CType: 'uploads'
                            header: 'File name and file type icon (1)'
                            header_layout: 2
                            uploads_type: 1
                      - self:
                            id: 3704
                            CType: 'uploads'
                            header: 'File name and thumbnail, with size and description (2)'
                            header_layout: 2
                            uploads_type: 2
                            filelink_size: 1
                            uploads_description: 1
                      # The icons of four file types - a PDF, a text file, a
                      # zip archive and an image - with the size of each. The
                      # icon goes by the extension, see
                      # "Partials/ContentElement/FileIcon.html"; the files
                      # are declared in "config.yml". Sorted by name, so the
                      # order does not depend on the order of the references.
                      - self:
                            id: 3705
                            CType: 'uploads'
                            header: 'The icon of each file type, with the size (1)'
                            header_layout: 2
                            uploads_type: 1
                            filelink_size: 1
                            filelink_sorting: 'name'
                            filelink_sorting_direction: 'asc'
                      # A thumbnail where FAL can make one, and the icon of the
                      # file type in its square where it cannot: the image
                      # first, then the PDF and the zip archive.
                      - self:
                            id: 3706
                            CType: 'uploads'
                            header: 'A thumbnail for the image, the icon for the rest (2)'
                            header_layout: 2
                            uploads_type: 2
                            uploads_description: 1

                # "div" - a rule between two elements, and nothing else.
                - self:
                      id: 38
                      title: 'Divider'
                      slug: '/elements/core/div'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3801
                            CType: 'header'
                            header: 'Divider'
                            header_layout: 1
                      - self:
                            id: 3802
                            CType: 'text'
                            header: 'Before the divider'
                            header_layout: 2
                            bodytext: '<p>The divider below is an element of its own. Its only field is a name for the backend.</p>'
                      - self:
                            id: 3803
                            CType: 'div'
                            header: 'The divider between the two texts'
                      - self:
                            id: 3804
                            CType: 'text'
                            header: 'After the divider'
                            header_layout: 2
                            bodytext: '<p>A text element again, below the rule.</p>'

                # "html" - raw markup, unescaped and unparsed. Its header is
                # a name for the backend, so the markup brings its own
                # heading.
                - self:
                      id: 39
                      title: 'Plain HTML'
                      slug: '/elements/core/html'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3901
                            CType: 'header'
                            header: 'Plain HTML'
                            header_layout: 1
                      - self:
                            id: 3902
                            CType: 'html'
                            header: 'Keyboard input and a disclosure (name only, not rendered)'
                            bodytext: |
                              <h2 class="theme-content-element__heading">Keyboard input and a disclosure</h2>
                              <p>Written as markup and rendered as written: press
                              <kbd><kbd>Ctrl</kbd> + <kbd>K</kbd></kbd> to insert a link in the
                              rich text editor. Nothing here was escaped, and no rich text
                              processing ran over it.</p>
                              <details>
                                <summary>Who may use this element?</summary>
                                <p>An administrator, and an editor whose backend group allows the
                                CType explicitly: the field carries "authMode = explicitAllow".</p>
                              </details>

                # "shortcut" - other elements rendered in place: one, and two
                # in the order the field lists them.
                - self:
                      id: 50
                      title: 'Insert records'
                      slug: '/elements/core/shortcut'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 5001
                            CType: 'header'
                            header: 'Insert records'
                            header_layout: 1
                      - self:
                            id: 5002
                            CType: 'text'
                            header: 'What follows is not on this page'
                            header_layout: 2
                            bodytext: |
                              <p>The two elements below this text reference elements of the
                              pages "Bullet list" and "Table". They are rendered here from the
                              records on those pages, not copied: change one there, and it
                              changes here.</p>
                      - self:
                            id: 5003
                            CType: 'shortcut'
                            header: 'The check list of the bullet list page (name only, not rendered)'
                            records: 'tt_content_3503'
                      - self:
                            id: 5004
                            CType: 'shortcut'
                            header: 'The inline list, then the striped table (name only, not rendered)'
                            records: 'tt_content_3505,tt_content_3603'

            # Every "menu_*" content element the theme renders, each pointed at a
            # different part of the seeded tree so the eleven are told apart by
            # what they list rather than by their header alone.
            #
            # "backend_layout: content_sidebar" is deliberate and is asserted:
            # page 8 is a sibling under the same parent and does *not* use it, so
            # the pair prove the layout is resolved per page rather than
            # inherited down the branch.
            - self:
                  id: 7
                  title: 'Menu elements'
                  slug: '/elements/menu'
                  backend_layout: 'content_sidebar'
                  # Read by the "menu_abstract" element below, which lists the
                  # children of page 5 - this page among them. Without an
                  # abstract the item still renders, just as a bare link
                  # ("showAbstract" is guarded by "{item.data.abstract}" in
                  # "Partials/ContentElement/Menu.html"), and nothing says why.
                  abstract: 'The eleven menu content elements, each pointed at a different part of this page tree.'
                  # The keyword source of the "menu_related_pages" element below.
                  # "special = keywords" reads the keywords of the page selected
                  # in "pages" and matches every *other* page under the site root
                  # that shares one, so this page is the source and is itself
                  # excluded from the result.
                  keywords: 'content elements'
              entities:
                content:
                  - self:
                        id: 701
                        CType: 'header'
                        header: 'Menu elements'
                        header_layout: 1

                  # "special = list" - "pages" names the pages to render, in the
                  # order they are listed; nothing is looked up by rootline
                  # position. Three top level pages, so the list is visibly a
                  # hand-picked selection rather than a branch of the tree.
                  # Not page 4: it is "nav_hide", and every menu leaves such a
                  # page out unless "includeNotInMenu" is set, a "special =
                  # list" included.
                  - self:
                        id: 702
                        CType: 'menu_pages'
                        header: 'menu_pages: a hand-picked list of pages'
                        header_layout: 2
                        pages: '2,3,9'

                  # "special = directory" - the *children* of the selected pages,
                  # not the pages themselves. Page 5 is the parent of this page,
                  # so this renders its three siblings.
                  - self:
                        id: 703
                        CType: 'menu_subpages'
                        header: 'menu_subpages: the children of one page'
                        header_layout: 2
                        pages: '5'

                  # "special = list" plus "levels = 2" - the selected page itself
                  # *and* one level of its own children. Same starting point as
                  # "menu_subpages" above, so the two side by side show what the
                  # second level adds.
                  - self:
                        id: 704
                        CType: 'menu_section'
                        header: 'menu_section: one page and its children'
                        header_layout: 2
                        pages: '5'

                  # "special = directory" plus "levels = 2" - the children of the
                  # selected page, each with their own children beneath them.
                  # Rooted at the home page, so the "Elements" branch appears
                  # with its three sub pages nested under it.
                  - self:
                        id: 705
                        CType: 'menu_section_pages'
                        header: 'menu_section_pages: two levels below one page'
                        header_layout: 2
                        pages: '1'

                  # No source field at all: the TCA of this type has no "pages"
                  # (EXT:frontend,
                  # "Overrides/280-tt_content-content_type-menu_sitemap.php"),
                  # and the TypoScript empties "special" - it always
                  # starts at the site root and walks seven levels down.
                  - self:
                        id: 706
                        CType: 'menu_sitemap'
                        header: 'menu_sitemap: the whole site from its root'
                        header_layout: 2

                  # The same seven-level walk as "menu_sitemap", rooted at the
                  # selected page instead of the site root. This tree is only
                  # three levels deep, so what it renders here is the same single
                  # level "menu_subpages" above renders - the depth has nothing
                  # further to reach, and that is a property of the demo tree,
                  # not of the element.
                  - self:
                        id: 707
                        CType: 'menu_sitemap_pages'
                        header: 'menu_sitemap_pages: a whole branch from one page'
                        header_layout: 2
                        pages: '5'

                  # "special = directory" again, with each item's own "abstract"
                  # shown beneath its link. Rooted at page 5, so the items are
                  # pages 6, 7 and 8 - each of which needs an "abstract" of its
                  # own for this element to show anything but bare links.
                  - self:
                        id: 708
                        CType: 'menu_abstract'
                        header: 'menu_abstract: children with their abstracts'
                        header_layout: 2
                        pages: '5'

                  # "special = updated" - the selected page and everything
                  # beneath it, sorted by "SYS_LASTCHANGED" descending. That
                  # column is 0 on a freshly seeded tree and is only written when
                  # a page is first rendered in the frontend, so the links are
                  # all there from the start and the dates appear page by page as
                  # the tree is browsed. The theme sets no "maxAge", so nothing
                  # is filtered out by age.
                  - self:
                        id: 709
                        CType: 'menu_recently_updated'
                        header: 'menu_recently_updated: the tree by last change'
                        header_layout: 2
                        pages: '1'

                  # "special = keywords" - "pages" names the page whose keywords
                  # are the search term, not the pages to list. This page is that
                  # source (see "keywords" on the page above); the result is
                  # every other page under the site root sharing one of them.
                  - self:
                        id: 710
                        CType: 'menu_related_pages'
                        header: 'menu_related_pages: pages sharing a keyword'
                        header_layout: 2
                        pages: '7'

                  # No category is selected, deliberately: this tree seeds no
                  # "sys_category" records, so there is nothing to select. An
                  # empty selection renders an empty menu, which is the correct
                  # rendering of "nothing selected".
                  #
                  # "selected_categories: 0" rather than leaving the field out:
                  # it is the value DataHandler itself normalises an
                  # unresolvable selection to, so it is what the column holds
                  # once anything real has touched it, and it keeps this element
                  # identical to its sibling below so neither reads as the
                  # special case.
                  - self:
                        id: 711
                        CType: 'menu_categorized_pages'
                        header: 'menu_categorized_pages: pages by category'
                        header_layout: 2
                        selected_categories: 0
                        category_field: 'categories'

                  # The same deliberate gap as above.
                  #
                  # The explicit 0 used to be load-bearing: the TypoScript
                  # interpolated the field straight into "uid_local IN (...)",
                  # and an empty value made that "IN ()" - accepted by SQLite,
                  # rejected as a syntax error by MariaDB, MySQL and PostgreSQL,
                  # taking the whole page down rather than just the element. That
                  # is now guarded where it belongs, in
                  # "ContentElements.typoscript", because an editor who saves the
                  # element before picking a category hits the same path and no
                  # seed value can help them.
                  - self:
                        id: 712
                        CType: 'menu_categorized_content'
                        header: 'menu_categorized_content: content by category'
                        header_layout: 2
                        selected_categories: 0
                        category_field: 'categories'

                  # The card and thumbnail layouts of the two page menus -
                  # "layout" 1 and 2, enabled for these two CTypes only. The
                  # three pages below "Theme elements" carry page media and an
                  # abstract, which is what a card shows; "menu_subpages" of
                  # page 8 lists every page below it, with media or without.
                  - self:
                        id: 713
                        CType: 'menu_pages'
                        header: 'menu_pages as cards: page media, title and abstract'
                        header_layout: 2
                        layout: 1
                        pages: '90,91,92'
                  - self:
                        id: 714
                        CType: 'menu_pages'
                        header: 'menu_pages as thumbnails: page media and title'
                        header_layout: 2
                        layout: 2
                        pages: '90,91,92'
                  - self:
                        id: 715
                        CType: 'menu_subpages'
                        header: 'menu_subpages as cards: the pages below one page'
                        header_layout: 2
                        layout: 1
                        pages: '8'
                  - self:
                        id: 716
                        CType: 'menu_subpages'
                        header: 'menu_subpages as thumbnails'
                        header_layout: 2
                        layout: 2
                        pages: '8'

            # The content elements this extension registers itself, one each,
            # so the "theme_*" CTypes can be looked at side by side rather than
            # one fixture at a time. The layouts of the heroes, the call to
            # action and the testimonial are on the pages below this one.
            #
            # No "content_sidebar" here, unlike its two siblings 6 and 7: the
            # layout is resolved per page, not inherited down the branch, and
            # this page is what proves it.
            - self:
                  id: 8
                  title: 'Theme elements'
                  slug: '/elements/theme'
                  backend_layout: 'content'
                  # As on page 6: the abstract and the keyword are read by the
                  # two menu elements on page 7, not by anything on this page.
                  abstract: 'The content elements this extension registers itself, one each; the pages below show the layouts of the heroes, the call to action and the quotation.'
                  keywords: 'content elements, theme'
              entities:
                content:
                  # theme_hero - the full hero: heading, lead, media and one call
                  # to action. Its own template defaults an unset
                  # "header_layout" to h1 (Partials/ContentElement/Hero.html),
                  # which is right for a page whose hero is its top heading and
                  # wrong for a page carrying three of them, so the level is set
                  # explicitly here.
                  - self:
                        id: 801
                        CType: 'theme_hero'
                        header: 'The full hero'
                        header_layout: 2
                        bodytext: >-
                          The hero carries a heading, a lead paragraph, one media
                          slot and a single call to action. It is the widest element
                          the theme ships and is meant to open a page.
                        tx_theme_link: 't3://page?uid=2'
                        tx_theme_link_label: 'Read the typography page'
                        # The empty string is a real item of the select, not a
                        # missing value: it is the default variant and renders the
                        # plain ".theme-button" with no modifier.
                        tx_theme_link_variant: ''
                        # An icon of the shipped set, before the label. The four
                        # link styles are spread over 801 to 805: primary here,
                        # secondary 802, ghost 803 and 805, link 804.
                        tx_theme_link_icon: 'arrow-right'
                  # theme_hero_small - the same field set as theme_hero, rendered
                  # through the same partial with "compact: 1", so the difference
                  # between the two is only visible with both on one page.
                  - self:
                        id: 802
                        CType: 'theme_hero_small'
                        header: 'The reduced hero'
                        header_layout: 2
                        bodytext: >-
                          The same fields as the full hero, rendered compactly. The
                          element above and this one differ in nothing an editor
                          fills in - only in the variant that was chosen.
                        tx_theme_link: 't3://page?uid=3'
                        tx_theme_link_label: 'Look at the media page'
                        tx_theme_link_variant: 'secondary'
                  # theme_hero_text_only - no "image" field on the CType at all,
                  # so no file reference either. The hero renders without its
                  # "theme-hero--media" modifier.
                  - self:
                        id: 803
                        CType: 'theme_hero_text_only'
                        header: 'The hero without media'
                        header_layout: 2
                        bodytext: >-
                          This variant has no image field in the backend form, so
                          there is nothing to leave empty. What it demonstrates is the
                          hero body on its own, full width.
                        tx_theme_link: 't3://page?uid=1'
                        tx_theme_link_label: 'Back to the start page'
                        tx_theme_link_variant: 'ghost'
                  # theme_teaser - a short block of text with a call to action
                  # and no media. Like theme_hero_text_only it has no "image"
                  # field, which is why the teaser partial renders without its
                  # "theme-teaser__media" part.
                  - self:
                        id: 804
                        CType: 'theme_teaser'
                        header: 'A teaser without media'
                        bodytext: >-
                          A teaser is a secondary block: a heading, a couple of
                          sentences and one link. Its heading defaults to h2 rather
                          than the hero's h1.
                        tx_theme_link: 't3://page?uid=4'
                        tx_theme_link_label: 'Open the empty page'
                        tx_theme_link_variant: 'link'
                  # theme_media_teaser - the same teaser with an image beside the
                  # text. The link is an external URL rather than a page, because
                  # "tx_theme_link" is a TCA type=link field and holds whatever
                  # the link wizard encoded - a page, a file, a mail address or,
                  # as here, a plain URL.
                  - self:
                        id: 805
                        CType: 'theme_media_teaser'
                        header: 'A teaser beside an image'
                        bodytext: >-
                          Text and a single image side by side. The image is one
                          file reference; the theme deliberately wires no gallery
                          processing here, because there is no grid to arrange.
                        tx_theme_link: 'https://docs.typo3.org/'
                        tx_theme_link_label: 'Read the TYPO3 documentation'
                        tx_theme_link_variant: 'ghost'
                  # theme_testimonial - a quotation with its attribution.
                  # "header" and "subheader" are relabelled on this CType to hold
                  # the attributed person's name and role, not a content heading.
                  #
                  # No media, and no file reference: the element renders through
                  # ".theme-quote", which has no media slot, and the image field
                  # was removed from the CType rather than left inert. Do not add
                  # one back without giving the component a slot first.
                  - self:
                        id: 806
                        CType: 'theme_testimonial'
                        header: 'Ada Lovelace'
                        subheader: 'Mathematician, 1843'
                        bodytext: >-
                          The Analytical Engine weaves algebraic patterns just as
                          the Jacquard loom weaves flowers and leaves.
                  # theme_author - a person: portrait, name, role, a short bio and
                  # the person's own links. The name is the content element's
                  # heading and goes through the shared header partial; the role
                  # is "subheader" and sits inside the component.
                  #
                  # "tx_theme_list_items" is the inline relation: the declared
                  # ids of its children, in the order they are rendered.
                  - self:
                        id: 807
                        CType: 'theme_author'
                        header: 'Grace Hopper'
                        subheader: 'Compiler pioneer'
                        bodytext: >-
                          Wrote the first compiler and spent the rest of her career
                          arguing that a programming language should read like
                          English.
                        tx_theme_list_items: '1,2'
                  # theme_linklist - a flat list of links. "header" is the list
                  # title here, not a content heading, which is why the CType
                  # offers the bare field rather than the headers palette.
                  #
                  # The list names its children out of uid order on purpose:
                  # 5, 3, 6, 4 renders "Typography", "Media", "Empty page" and
                  # the unlabelled link, while the uid order - which is also
                  # the order they are created in - would put "Media" first.
                  # Only the relation can produce the rendered order, which is
                  # what lets "ShowcaseTreeTest" tell the relation from the uid
                  # or the insertion order.
                  - self:
                        id: 808
                        CType: 'theme_linklist'
                        header: 'Where to read more'
                        tx_theme_list_items: '5,3,6,4'
                  # theme_sociallinks - the same list, rendered as text labels:
                  # the logos of the platforms are Font Awesome's brands set,
                  # which the theme does not ship, so "link_label" carries the
                  # platform name. The child "link" is
                  # restricted to url, email and telephone on this relation, so
                  # each of the three appears once.
                  - self:
                        id: 809
                        CType: 'theme_sociallinks'
                        header: 'Elsewhere'
                        tx_theme_list_items: '7,8,9'
                  # theme_media_teaser_grid - several cards in a grid. "header"
                  # and "bodytext" are the lead-in above the grid; the cards
                  # themselves are the same child table, here with the full
                  # header/text/image/link set the other three relations narrow
                  # away.
                  - self:
                        id: 810
                        CType: 'theme_media_teaser_grid'
                        header: 'Three cards in a grid'
                        bodytext: >-
                          Every card is one row of the shared child table. The
                          lead-in above is the element's own heading and body.
                        tx_theme_list_items: '10,11,12'
                  # theme_notice - the six kinds of ".theme-alert", one element
                  # each. The kind decides the modifier and the "role" together
                  # ("Templates/ContentElements/ThemeNotice.html"): "status" for
                  # info and success, "alert" for warning and danger, "note" for
                  # note and tip. "header" is the title inside the component,
                  # not a content heading, and "bodytext" is rich text on this
                  # CType, so it is written as a paragraph.
                  - self:
                        id: 811
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'note'
                        header: 'A note'
                        bodytext: '<p>An aside to the text around it. It takes the note role and is never a live region, because nothing about it ever changes. It is also the kind a notice gets when nobody chose one.</p>'
                  - self:
                        id: 812
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'info'
                        header: 'Information'
                        bodytext: '<p>The neutral kind, and the look of the bare class. It takes the status role, a polite live region that waits for a pause before it is read.</p>'
                  - self:
                        id: 813
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'tip'
                        header: 'A tip'
                        bodytext: '<p>A recommendation, and the one kind that follows the palette: switch the palette in the display settings, and this is the only notice that changes colour.</p>'
                  - self:
                        id: 814
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'success'
                        header: 'Something finished'
                        bodytext: '<p>Confirms that something went through. It takes the status role, like the information.</p>'
                  - self:
                        id: 815
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'warning'
                        header: 'A warning'
                        bodytext: '<p>Something needs attention before it becomes a problem. It takes the alert role, the assertive one.</p>'
                  - self:
                        id: 816
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'danger'
                        header: 'Something failed'
                        bodytext: '<p>Assertive like the warning, with a glyph of its own: once colours are forced, the glyph is what tells the six kinds apart.</p>'
                  # theme_tabs - one tab per child. The ids of the tabs and
                  # panels are derived from the anchor of the element, "c817",
                  # so a second tab group on the page cannot collide with this
                  # one. The children's "text" is rich text on this relation
                  # only; the second one links to the start page to show it.
                  - self:
                        id: 817
                        CType: 'theme_tabs'
                        header: 'Three tabs'
                        header_layout: 2
                        tx_theme_list_items: '13,14,15'
                  # theme_accordion - native "details" sharing one "name",
                  # "c818-accordion", so opening one item closes the others.
                  - self:
                        id: 818
                        CType: 'theme_accordion'
                        header: 'An accordion'
                        header_layout: 2
                        tx_theme_list_items: '16,17,18'

                # The children of the four relations above. A relation is not a
                # containment: a child is an ordinary record on this page, and
                # only the relation columns DataHandler writes tie it to its
                # parent element.
                listItem:
                  # theme_author, uid 807. This relation is narrowed to the link
                  # palette on that CType, so a child carries "link" and
                  # "link_label" and nothing else - "header", "text" and "image"
                  # are not part of the form there and would be dead data.
                  - self:
                        id: 1
                        link: 't3://page?uid=1'
                        link_label: 'Profile'
                  - self:
                        id: 2
                        # TYPO3 link syntax, not an "href": an email address is
                        # stored with its "mailto:" scheme.
                        link: 'mailto:grace@example.org'
                        link_label: 'Write an email'

                  # theme_linklist, uid 808, which lists these as 5, 3, 6, 4.
                  - self:
                        id: 3
                        link: 't3://page?uid=3'
                        link_label: 'Media'
                  # No "link_label" on purpose: the label is optional, and a row
                  # without one falls back to the resolved URL rather than
                  # rendering an empty link. This entry is what shows that
                  # fallback in the seeded tree.
                  - self:
                        id: 4
                        link: 'https://typo3.org/'
                  - self:
                        id: 5
                        link: 't3://page?uid=2'
                        link_label: 'Typography'
                        # "link_icon" is part of the link palette of every
                        # relation; one row of the list shows it.
                        link_icon: 'book-open'
                  - self:
                        id: 6
                        link: 't3://page?uid=4'
                        link_label: 'Empty page'

                  # theme_sociallinks, uid 809.
                  - self:
                        id: 7
                        link: 'https://typo3.org/'
                        link_label: 'TYPO3 on the web'
                  - self:
                        id: 8
                        link: 'mailto:hello@example.org'
                        link_label: 'Send an email'
                  - self:
                        id: 9
                        # "tel:" plus digits, "+", "," and ";" - anything else is
                        # stripped when the link wizard encodes a number.
                        link: 'tel:+4930123456'
                        link_label: 'Call the office'

                  # theme_media_teaser_grid, uid 810. The images of the three
                  # cards are attached in "config.yml".
                  #
                  # The child's "text" is a plain text column, never RTE-enabled,
                  # and is rendered through nl2br - so it holds sentences and
                  # line breaks, never markup.
                  - self:
                        id: 10
                        header: 'Typography'
                        text: 'Headings, running text and the inline cases a stylesheet has to answer for.'
                        link: 't3://page?uid=2'
                        link_label: 'Open the typography page'
                        link_icon: 'arrow-right'
                  - self:
                        id: 11
                        header: 'Media'
                        text: 'A single image and a two column gallery, both driven by the fields of the element.'
                        link: 't3://page?uid=3'
                        link_label: 'Open the media page'
                  # No "link" on this one: unlike the other three relations, the
                  # grid does not require it, and a card without a link renders
                  # its title and text with no ".theme-card__link". That shape is
                  # part of what this page demonstrates.
                  - self:
                        id: 12
                        header: 'A card without a link'
                        text: |-
                          The link is optional on this relation only.
                          A card without one is a plain block of text.

                  # theme_tabs, uid 817. Title and rich text, nothing else: the
                  # relation is narrowed to those two on that CType. The text is
                  # stored as written - "overrideChildTca" enables the editor in
                  # the form only, and DataHandler runs no rich text
                  # transformation for it (see "tt_content_theme_tabs.php").
                  - self:
                        id: 13
                        header: 'Site set'
                        text: '<p>A site depends on the site set of the theme, and constants, page TSconfig and setup arrive together. This is the first tab, the one the markup selects.</p>'
                  - self:
                        id: 14
                        header: 'Static include'
                        text: '<p>An installation without site sets includes the static TypoScript in a template record instead: the same files, read the same way. The <a href="t3://page?uid=1">start page</a> of the second tree of a development instance is delivered like that.</p>'
                  - self:
                        id: 15
                        header: 'Without the script'
                        text: '<p>Switch JavaScript off and reload: the tab list is gone, and every panel is shown in a frame of its own, under a heading repeating its tab.</p>'

                  # theme_accordion, uid 818.
                  - self:
                        id: 16
                        header: 'What makes the items exclusive?'
                        text: '<p>One <em>name</em> shared by every item of this element. Open another item and this one closes, with no script involved.</p>'
                  - self:
                        id: 17
                        header: 'Why is the name derived from the element?'
                        text: '<p>The browser groups items by name across the whole page, so two accordions sharing one would close each other. The name is the anchor of the element with a suffix.</p>'
                  - self:
                        id: 18
                        header: 'Is anything open at first?'
                        text: '<p>No. Which item a reader meets first is what the order of the items already says.</p>'

              # One page per theme element with variants, named after it:
              # every value of the fields that change how it looks, one element
              # each. "ShowcaseTreeTest" reads the values from the TCA and the
              # page TSconfig and holds these pages to them. The elements on
              # page 8 above keep their default look.
              #
              # "content_sidebar" where the element fits beside the sub
              # navigation, so the sibling pages are listed next to them.
              children:
                # "theme_text_icon" - every position, shape and size of the
                # icon, and one element without an icon.
                - self:
                      id: 70
                      title: 'Text and icon'
                      slug: '/elements/theme/text-icon'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 7001
                            CType: 'header'
                            header: 'Text and icon'
                            header_layout: 1
                      - self:
                            id: 7002
                            CType: 'text'
                            header: 'Where the icon goes'
                            header_layout: 2
                            bodytext: |
                              <p>The heading, the text and the link of the element sit beside one
                              icon of the set. The icon goes at the start of the line, at its end or
                              above the text; it is drawn plain in the accent, or on a square or a
                              round tile of the accent; and it comes in three sizes. The elements
                              below show every one of those values.</p>
                      - self:
                            id: 7003
                            CType: 'theme_text_icon'
                            header: 'Configure once, in the site'
                            header_layout: 2
                            bodytext: |
                              <p>A site depends on the site set of the theme, and constants, page
                              TSconfig and setup arrive together. Nothing is copied into the site
                              package.</p>
                            tx_theme_icon: 'gear'
                            tx_theme_icon_position: 'start'
                            tx_theme_icon_shape: 'square'
                            tx_theme_icon_size: 'md'
                            tx_theme_link: 't3://page?uid=9'
                            tx_theme_link_label: 'Open the styleguide'
                            tx_theme_link_variant: ''
                      - self:
                            id: 7004
                            CType: 'theme_text_icon'
                            header: 'Templates that stay readable'
                            header_layout: 2
                            bodytext: |
                              <p>Every element is one Fluid template on the contract of a component.
                              A site package overrides the one file it wants to change and keeps the
                              rest.</p>
                            tx_theme_icon: 'code'
                            tx_theme_icon_position: 'end'
                            tx_theme_icon_shape: 'circle'
                            tx_theme_icon_size: 'lg'
                            tx_theme_link: 't3://page?uid=6'
                            tx_theme_link_label: 'See the core elements'
                            tx_theme_link_variant: 'secondary'
                            tx_theme_link_icon: 'arrow-right'
                      - self:
                            id: 7005
                            CType: 'theme_text_icon'
                            header: 'Readable for everybody'
                            header_layout: 2
                            bodytext: |
                              <p>Every colour of the theme is checked against the surfaces it sits
                              on, in both appearances and all five palettes. The icon beside this
                              text is decoration: the heading says the same.</p>
                            tx_theme_icon: 'universal-access'
                            tx_theme_icon_position: 'top'
                            tx_theme_icon_shape: 'plain'
                            tx_theme_icon_size: 'xl'
                      - self:
                            id: 7006
                            CType: 'theme_text_icon'
                            header: 'From a checkout to a running site'
                            header_layout: 2
                            bodytext: |
                              <p>Both development instances start with one command and seed this very
                              page tree, so a change is looked at in the backend and the frontend of
                              TYPO3 v13 and v14 alike.</p>
                            tx_theme_icon: 'rocket'
                            tx_theme_icon_position: 'top'
                            tx_theme_icon_shape: 'square'
                            tx_theme_icon_size: 'lg'
                            tx_theme_link: 't3://page?uid=2'
                            tx_theme_link_label: 'Read the typography pages'
                            tx_theme_link_variant: 'ghost'
                      - self:
                            id: 7007
                            CType: 'theme_text_icon'
                            header: 'An icon at the end, plain'
                            header_layout: 2
                            bodytext: |
                              <p>At the end of the line the icon moves to the other side of the text,
                              in a right-to-left page as well. It stays first in the markup.</p>
                            tx_theme_icon: 'shield-halved'
                            tx_theme_icon_position: 'end'
                            tx_theme_icon_shape: 'plain'
                            tx_theme_icon_size: 'md'
                      - self:
                            id: 7008
                            CType: 'theme_text_icon'
                            header: 'Without an icon'
                            header_layout: 2
                            bodytext: |
                              <p>No icon was picked for this element, so it renders its text alone,
                              without an empty tile where the icon would be.</p>
                            tx_theme_icon: ''
                            tx_theme_icon_position: 'start'
                            tx_theme_icon_shape: 'circle'
                            tx_theme_icon_size: 'md'

                # "theme_features" - every layout and every column count. The
                # children are list items 100-114, on this page like every
                # child of a relation.
                #
                # "content", not "content_sidebar" like its siblings: four
                # columns of the grid need 53.625rem inside the element, and
                # the main column beside the sub navigation has less, so the
                # four-column element would show three. "ShowcaseTreeTest"
                # holds every four-column features element to a page without
                # a sidebar.
                - self:
                      id: 71
                      title: 'Features'
                      slug: '/elements/theme/features'
                      backend_layout: 'content'
                  entities:
                    content:
                      - self:
                            id: 7101
                            CType: 'header'
                            header: 'Features'
                            header_layout: 1
                      - self:
                            id: 7102
                            CType: 'text'
                            header: 'Four layouts, up to four columns'
                            header_layout: 2
                            bodytext: |
                              <p>A group of features: an icon, a title, a sentence or two and a
                              link each. The layout sets the icon above the title, beside it on a
                              framed tile, or frames the whole feature; the fourth sets the heading
                              and the text of the element beside the grid. The grid takes two,
                              three or four columns at most, and fewer where a column would be too
                              narrow - one on a phone.</p>
                      - self:
                            id: 7103
                            CType: 'theme_features'
                            header: 'Why build against this theme'
                            header_layout: 2
                            bodytext: '<p>Three reasons, in columns of three, the icon on a tile above each title.</p>'
                            layout: 0
                            tx_theme_columns: 3
                            tx_theme_list_items: '100,101,102'
                      - self:
                            id: 7104
                            CType: 'theme_features'
                            header: 'What every element brings'
                            header_layout: 2
                            layout: 1
                            tx_theme_columns: 2
                            tx_theme_list_items: '103,104,105,106'
                      - self:
                            id: 7105
                            CType: 'theme_features'
                            header: 'The showcase at a glance'
                            header_layout: 2
                            layout: 2
                            tx_theme_columns: 4
                            tx_theme_list_items: '107,108,109,110'
                      - self:
                            id: 7106
                            CType: 'theme_features'
                            header: 'Two ways to deliver the theme'
                            header_layout: 2
                            bodytext: |
                              <p>A site takes the theme through its site set, an installation without
                              site sets through the static include. Both render the same pages, and
                              the tests hold them to it.</p>
                            layout: 3
                            tx_theme_columns: 2
                            tx_theme_list_items: '111,112,113,114'
                    listItem:
                      # theme_features 7103, in columns.
                      - self:
                            id: 100
                            header: 'Neutral by design'
                            text: 'An extension built against this theme shows its own structure, not a brand.'
                            icon: 'lightbulb'
                            link: 't3://page?uid=9'
                            link_label: 'Open the styleguide'
                            link_icon: 'arrow-right'
                      - self:
                            id: 101
                            header: 'Tested on both cores'
                            text: 'Every gate runs for TYPO3 v13 and for v14, each on its own dependency set.'
                            icon: 'list-check'
                            link: 't3://page?uid=6'
                            link_label: 'See the core elements'
                      # No link: a feature does not need one.
                      - self:
                            id: 102
                            header: 'Readable in every palette'
                            text: 'Every colour is checked in both appearances and all five palettes.'
                            icon: 'universal-access'
                      # theme_features 7104, hanging icons.
                      - self:
                            id: 103
                            header: 'A form of its own'
                            text: 'The backend form offers the fields the template renders, and no others.'
                            icon: 'pen-to-square'
                      - self:
                            id: 104
                            header: 'A template on a contract'
                            text: 'One Fluid file per element, written against the markup of a component.'
                            icon: 'code'
                      - self:
                            id: 105
                            header: 'Rich text where it is prose'
                            text: 'Headings and lists come from the rich text editor, with the classes of the theme.'
                            icon: 'book-open'
                      - self:
                            id: 106
                            header: 'Icons from one set'
                            text: 'Every icon is one of Font Awesome Free, picked by name in the form.'
                            icon: 'star'
                      # theme_features 7105, tiles.
                      - self:
                            id: 107
                            header: 'Typography'
                            text: 'Running text, lists, tables, quotes and code.'
                            icon: 'book'
                            link: 't3://page?uid=2'
                            link_label: 'Read'
                      - self:
                            id: 108
                            header: 'Media'
                            text: 'Images and galleries.'
                            icon: 'image'
                            link: 't3://page?uid=3'
                            link_label: 'Look'
                      - self:
                            id: 109
                            header: 'Core elements'
                            text: 'Every classic element, in its variants.'
                            icon: 'list'
                            link: 't3://page?uid=6'
                            link_label: 'Browse'
                      - self:
                            id: 110
                            header: 'Styleguide'
                            text: 'Every component, from its own markup.'
                            icon: 'gear'
                            link: 't3://page?uid=9'
                            link_label: 'Open'
                      # theme_features 7106, beside the introduction.
                      - self:
                            id: 111
                            header: 'Site set'
                            text: 'A dependency of the site; constants, page TSconfig and setup arrive together.'
                            icon: 'server'
                      - self:
                            id: 112
                            header: 'Static include'
                            text: 'The same TypoScript, included through a template record.'
                            icon: 'database'
                      - self:
                            id: 113
                            header: 'Never both'
                            text: 'A condition keeps the static include out of a site that names the set.'
                            icon: 'shield-halved'
                      - self:
                            id: 114
                            header: 'Compared page by page'
                            text: 'The second tree of an instance is rendered through the other path.'
                            icon: 'circle-check'

                # "theme_stats" - figures with and without icons, with and
                # without a sentence. List items 115-121.
                - self:
                      id: 72
                      title: 'Stats'
                      slug: '/elements/theme/stats'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 7201
                            CType: 'header'
                            header: 'Stats'
                            header_layout: 1
                      - self:
                            id: 7202
                            CType: 'text'
                            header: 'A figure and what it counts'
                            header_layout: 2
                            bodytext: |
                              <p>Each figure is a pair of a description list: what it counts is the
                              term, the figure its description, shown above it and larger, in digits
                              of one width. An icon before the figure and a sentence below it are
                              optional.</p>
                      - self:
                            id: 7203
                            CType: 'theme_stats'
                            header: 'The theme in figures'
                            header_layout: 2
                            tx_theme_list_items: '115,116,117,118'
                      - self:
                            id: 7204
                            CType: 'theme_stats'
                            header: 'Without icons'
                            header_layout: 2
                            tx_theme_list_items: '119,120,121'
                    listItem:
                      # theme_stats 7203. "header" is the figure, "subheader"
                      # what it counts.
                      - self:
                            id: 115
                            header: '2001'
                            subheader: 'Icons shipped'
                            text: 'The whole solid style of Font Awesome Free, inline.'
                            icon: 'image'
                      - self:
                            id: 116
                            header: '5'
                            subheader: 'Palettes'
                            text: 'Each varies the accents only.'
                            icon: 'star'
                      - self:
                            id: 117
                            header: '2'
                            subheader: 'Core versions'
                            text: 'TYPO3 v13.4 and v14.3, from one branch.'
                            icon: 'server'
                      - self:
                            id: 118
                            header: '1'
                            subheader: 'Breakpoint'
                            icon: 'desktop'
                      # theme_stats 7204.
                      - self:
                            id: 119
                            header: '44px'
                            subheader: 'Smallest tap target'
                            text: 'The floor for anything clickable.'
                      - self:
                            id: 120
                            header: '4.5:1'
                            subheader: 'Contrast of body text, at least'
                      - self:
                            id: 121
                            header: '5px'
                            subheader: 'Border radius'
                            text: 'The single radius of the Frame.'

                # "theme_steps" - numbered steps, and steps with icons. List
                # items 122-129.
                - self:
                      id: 73
                      title: 'Steps'
                      slug: '/elements/theme/steps'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 7301
                            CType: 'header'
                            header: 'Steps'
                            header_layout: 1
                      - self:
                            id: 7302
                            CType: 'text'
                            header: 'A process, in order'
                            header_layout: 2
                            bodytext: |
                              <p>The steps are an ordered list, numbered by the stylesheet, down the
                              page on a phone and across it from the medium breakpoint. A step may
                              show an icon in its marker instead of its number.</p>
                      - self:
                            id: 7303
                            CType: 'theme_steps'
                            header: 'Set up a development instance'
                            header_layout: 2
                            tx_theme_list_items: '122,123,124,125'
                      - self:
                            id: 7304
                            CType: 'theme_steps'
                            header: 'Release a version'
                            header_layout: 2
                            tx_theme_list_items: '126,127,128,129'
                    listItem:
                      # theme_steps 7303, numbered.
                      - self:
                            id: 122
                            header: 'Clone the repository'
                            text: 'One checkout holds both development instances.'
                      - self:
                            id: 123
                            header: 'Start the instance'
                            text: 'ddev start sets it up and seeds this page tree.'
                      - self:
                            id: 124
                            header: 'Log in'
                            text: 'The editor account and its password are documented.'
                      - self:
                            id: 125
                            header: 'Open the showcase'
                            text: 'Every element has a page below Elements.'
                      # theme_steps 7304, the icon in the marker; the last step
                      # has none and keeps its number.
                      - self:
                            id: 126
                            header: 'Run the gates'
                            text: 'Both core versions, one after the other.'
                            icon: 'list-check'
                      - self:
                            id: 127
                            header: 'Set the version'
                            text: 'In every file that carries one.'
                            icon: 'pen'
                      - self:
                            id: 128
                            header: 'Tag the release'
                            text: 'The tag starts the publishing.'
                            icon: 'tag'
                      - self:
                            id: 129
                            header: 'Tell people'
                            text: 'A step without an icon keeps its number.'
              # One page per collection element below "Theme elements", every
              # value of the fields that change how it looks - pages 90 to 92,
              # list items 150 to 177. "content_sidebar", like the pages of the
              # core elements: the sub navigation of the section lists them,
              # and "Theme elements" itself has no sidebar. Each page carries
              # page media and an abstract, which the card and thumbnail
              # layouts of the two page menus on page 7 show.
                - self:
                      id: 90
                      title: 'Card group'
                      slug: '/elements/theme/card-group'
                      backend_layout: 'content_sidebar'
                      abstract: 'Cards in a grid of two, three or four columns, and in one row that scrolls sideways.'
                  entities:
                    content:
                      - self:
                            id: 9001
                            CType: 'header'
                            header: 'Card group'
                            header_layout: 1
                      # "layout" 0 is the grid, "tx_theme_columns" the most cards
                      # side by side. The link of a card is a button of the style
                      # the card picks: the default, secondary, ghost and link
                      # are spread over the cards of this page.
                      - self:
                            id: 9002
                            CType: 'theme_card_group'
                            header: 'Three columns'
                            header_layout: 2
                            layout: 0
                            tx_theme_columns: 3
                            tx_theme_list_items: '150,151,152'
                      - self:
                            id: 9003
                            CType: 'theme_card_group'
                            header: 'Two columns'
                            header_layout: 2
                            layout: 0
                            tx_theme_columns: 2
                            tx_theme_list_items: '153,154'
                      - self:
                            id: 9004
                            CType: 'theme_card_group'
                            header: 'Four columns'
                            header_layout: 2
                            layout: 0
                            tx_theme_columns: 4
                            tx_theme_list_items: '155,156,157,158'
                      # "layout" 1 is the scroller: one row, three cards in view
                      # and the next one showing at the end. Five cards, so the
                      # strip always has something to scroll to.
                      - self:
                            id: 9005
                            CType: 'theme_card_group'
                            header: 'Around the showcase'
                            header_layout: 2
                            layout: 1
                            tx_theme_columns: 3
                            tx_theme_list_items: '159,160,161,162,163'
                            # The one seeded element with a link back to the
                            # top of the page: it is the last element of a
                            # "content_sidebar" page, which is where such a
                            # link is worth anything. Every other element keeps
                            # the TCA default of 0.
                            linkToTop: 1
                      # "layout" 2 is the wall: the cards keep their own
                      # heights and stack into columns, filled top to bottom.
                      # The texts are deliberately of very different lengths,
                      # because a wall of cards that are all the same height
                      # looks exactly like the grid.
                      - self:
                            id: 9006
                            CType: 'theme_card_group'
                            header: 'What people ask'
                            header_layout: 2
                            layout: 2
                            tx_theme_columns: 2
                            tx_theme_list_items: '450,451,452,453,454'
                    listItem:
                      # The images of the cards are attached in "config.yml".
                      - self:
                            id: 150
                            header: 'Workshops'
                            subheader: 'Every second Thursday'
                            text: 'Two hours on one topic of the theme, with the recording shared afterwards.'
                            link: 't3://page?uid=52'
                            link_label: 'Book a seat'
                            link_variant: ''
                            link_icon: 'arrow-right'
                      - self:
                            id: 151
                            header: 'Office hours'
                            subheader: 'Mondays, 14:00 to 16:00'
                            text: 'Bring a question about a site package, and leave with an answer or an issue that describes it.'
                            link: 't3://page?uid=10'
                            link_label: 'Send a question'
                            link_variant: 'secondary'
                      # No image: a card without media is the same card.
                      - self:
                            id: 152
                            header: 'Mailing list'
                            text: 'One message a month about what changed in the theme, and what is planned next.'
                            link: 'mailto:news@example.org'
                            link_label: 'Subscribe'
                            link_variant: 'link'
                      - self:
                            id: 153
                            header: 'Site set'
                            subheader: 'The recommended way'
                            text: 'A site depends on the set of the theme, and constants, page TSconfig and setup arrive together.'
                            link: 't3://page?uid=9'
                            link_label: 'Open the styleguide'
                            link_variant: 'ghost'
                      # No link: the card ends with its text.
                      - self:
                            id: 154
                            header: 'Static include'
                            subheader: 'For a site without sets'
                            text: 'The same files, included by a template record. The second tree of a development instance is delivered that way.'
                      - self:
                            id: 155
                            header: 'Plan'
                            text: 'Agree on what the page has to achieve, and for whom.'
                      - self:
                            id: 156
                            header: 'Build'
                            text: 'Put the page together from the elements the theme ships.'
                      - self:
                            id: 157
                            header: 'Review'
                            text: 'Read it on a phone, in the dark appearance and without a mouse.'
                      - self:
                            id: 158
                            header: 'Release'
                            text: 'Publish it, and write down what changed.'
                            link: 't3://page?uid=91'
                            link_label: 'See the timeline'
                      - self:
                            id: 159
                            header: 'Typography'
                            text: 'Running text, lists, tables, quotes and code, and a long article.'
                            link: 't3://page?uid=2'
                            link_label: 'Read the text'
                            link_variant: 'link'
                      - self:
                            id: 160
                            header: 'Media'
                            text: 'A single image and a two column gallery.'
                            link: 't3://page?uid=3'
                            link_label: 'Look at the images'
                            link_variant: 'link'
                      - self:
                            id: 161
                            header: 'Core elements'
                            text: 'One page per classic content element, with every variant.'
                            link: 't3://page?uid=6'
                            link_label: 'Open the core elements'
                            link_variant: 'link'
                      - self:
                            id: 162
                            header: 'Menu elements'
                            text: 'The eleven menu elements, and the card layouts of two of them.'
                            link: 't3://page?uid=7'
                            link_label: 'Open the menus'
                            link_variant: 'link'
                      - self:
                            id: 163
                            header: 'Frames'
                            text: 'Every band, every spacing and every header alignment.'
                            link: 't3://page?uid=51'
                            link_label: 'Open the frames'
                            link_variant: 'link'
                      # The cards of the wall, 450 to 454. Uneven on purpose.
                      - self:
                            id: 450
                            header: 'Does the theme need JavaScript?'
                            text: >-
                              No. Every component renders and reads without a script,
                              and the four that offer more with one say what they do
                              without it.
                            link: 't3://page?uid=9'
                            link_label: 'Open the styleguide'
                            link_variant: 'link'
                      - self:
                            id: 451
                            header: 'Can I use it with a site package?'
                            text: >-
                              Yes.
                      - self:
                            id: 452
                            header: 'Which TYPO3 versions are supported?'
                            text: >-
                              The branch you are looking at supports v13.4 and v14.3 on
                              PHP 8.2 and up. Each version has its own directory for the
                              code that differs, and the shared code never asks which
                              one it is running on. A card of a wall is never split
                              across a column break, however long its text gets.
                      - self:
                            id: 453
                            header: 'Where do the icons come from?'
                            text: >-
                              The solid set of Font Awesome Free, vendored into the
                              extension and committed.
                      - self:
                            id: 454
                            header: 'How do I report a problem?'
                            text: >-
                              Open an issue with the page it happens on and the core
                              version you are running.
                            link: 'mailto:issues@example.org'
                            link_label: 'Write an issue'
                            link_variant: 'secondary'

                - self:
                      id: 91
                      title: 'Timeline'
                      slug: '/elements/theme/timeline'
                      backend_layout: 'content_sidebar'
                      abstract: 'Dated entries on a line, oldest first and newest first.'
                  entities:
                    content:
                      - self:
                            id: 9101
                            CType: 'header'
                            header: 'Timeline'
                            header_layout: 1
                      # The relations list the entries out of date order on
                      # purpose, so the page shows that the element sorts them
                      # by their date.
                      - self:
                            id: 9102
                            CType: 'theme_timeline'
                            header: 'A relaunch, oldest first'
                            header_layout: 2
                            tx_theme_sort_direction: 'asc'
                            tx_theme_list_items: '166,164,168,165,167'
                      - self:
                            id: 9103
                            CType: 'theme_timeline'
                            header: 'Release notes of a site package, newest first'
                            header_layout: 2
                            tx_theme_sort_direction: 'desc'
                            tx_theme_list_items: '171,169,172,170'
                    listItem:
                      # "date" is a calendar date, written as the form sends
                      # it: an ISO date without a time.
                      - self:
                            id: 164
                            date: '2026-01-12'
                            header: 'Kick-off'
                            text: 'The editors, the developers and the owner of the site agree on what the relaunch has to achieve.'
                      - self:
                            id: 165
                            date: '2026-02-02'
                            header: 'Content audit'
                            text: 'Every page is listed with its owner, and a third of them is marked to be merged or retired.'
                      # The image is attached in "config.yml".
                      - self:
                            id: 166
                            date: '2026-02-23'
                            header: 'Prototype'
                            # An icon in place of the ring - the item's icon
                            # the features, the figures and the steps show.
                            icon: 'lightbulb'
                            text: 'Three templates and the elements they need, built from the theme and shown to the editors.'
                      - self:
                            id: 167
                            date: '2026-03-16'
                            header: 'Content migration'
                            text: 'The editors move the pages they kept, and rewrite the ones that needed it.'
                      - self:
                            id: 168
                            date: '2026-04-06'
                            header: 'Launch'
                            icon: 'rocket'
                            text: 'The new site goes live, and the old one redirects to it.'
                      - self:
                            id: 169
                            date: '2025-11-03'
                            header: 'Version 1.2'
                            text: 'Adds the dark appearance and a second palette.'
                      - self:
                            id: 170
                            date: '2025-06-16'
                            header: 'Version 1.1'
                            text: 'Adds the sub navigation and the page with a sidebar.'
                      - self:
                            id: 171
                            date: '2025-02-10'
                            header: 'Version 1.0'
                            text: 'The first release: a header, a footer and six content elements.'
                      - self:
                            id: 172
                            date: '2025-11-24'
                            header: 'Version 1.2.1'
                            text: 'Fixes the contrast of muted text on raised surfaces.'

                - self:
                      id: 92
                      title: 'Teaser list'
                      slug: '/elements/theme/teaser-list'
                      backend_layout: 'content_sidebar'
                      abstract: 'Rows with an image, a date and meta data, each row one link.'
                  entities:
                    content:
                      - self:
                            id: 9201
                            CType: 'header'
                            header: 'Teaser list'
                            header_layout: 1
                      - self:
                            id: 9202
                            CType: 'theme_teaser_list'
                            header: 'Recent articles'
                            header_layout: 2
                            tx_theme_list_items: '173,174,175,176,177'
                    listItem:
                      # The images of the first three rows are attached in
                      # "config.yml". The fourth has none, the fifth no link:
                      # it is an announcement rather than a teaser, and no
                      # target.
                      - self:
                            id: 173
                            header: 'Moving a site to site sets'
                            text: 'What changes for the TypoScript, the constants and the page TSconfig.'
                            date: '2026-03-02'
                            meta: '8 min read'
                            link: 't3://page?uid=52'
                      - self:
                            id: 174
                            header: 'Icons from a set, not from a font'
                            text: 'Why every icon of the theme is inline SVG, and what that costs.'
                            date: '2026-02-16'
                            meta: '5 min read'
                            link: 't3://page?uid=9'
                      - self:
                            id: 175
                            header: 'Light and dark from one declaration'
                            text: 'How one custom property carries both appearances.'
                            date: '2026-01-26'
                            meta: '6 min read'
                            link: 't3://page?uid=9'
                      - self:
                            id: 176
                            header: 'Tables that stay readable on a phone'
                            text: 'A scrolling region with a tab stop, rather than a table squeezed into the width.'
                            date: '2025-12-08'
                            link: 't3://page?uid=54'
                      - self:
                            id: 177
                            header: 'Announced: the next workshop'
                            text: 'Theme development on TYPO3 v14, for a day, with lunch.'
                            date: '2026-04-14'
                            meta: 'Berlin'

                # The carousel and the split tiles - pages 150 and 151, list
                # items 450 to 499. A decade of their own each, for the reason
                # the header of this file gives: the mirror of the content of
                # page p takes the uids of the content of page p + 10.
                - self:
                      id: 150
                      title: 'Carousel'
                      slug: '/elements/theme/carousel'
                      backend_layout: 'content_sidebar'
                      abstract: 'Slides in one track that scrolls sideways, with the caption below, above or over the picture.'
                  entities:
                    content:
                      - self:
                            id: 15001
                            CType: 'header'
                            header: 'Carousel'
                            header_layout: 1
                      # Every caption position, so the page shows all three.
                      # Nothing about a carousel moves on its own, so there is
                      # no field for an interval and none to seed.
                      - self:
                            id: 15002
                            CType: 'theme_carousel'
                            header: 'Every caption position'
                            header_layout: 2
                            tx_theme_list_items: '460,461,462,463'
                      # A carousel without a heading: the region takes the
                      # generic name rather than staying nameless.
                      - self:
                            id: 15003
                            CType: 'theme_carousel'
                            tx_theme_list_items: '464,465'
                    listItem:
                      # The pictures are attached in "config.yml"; 465 has
                      # none, so a slide without one is on the page too.
                      - self:
                            id: 460
                            header: 'The caption below'
                            text: >-
                              The default: the words sit under the picture.
                            link: 't3://page?uid=9'
                            link_label: 'Open the styleguide'
                      - self:
                            id: 461
                            header: 'The caption above'
                            text: >-
                              Painted before the picture, and still after it in the
                              markup.
                            caption_position: 'above'
                      - self:
                            id: 462
                            header: 'The caption over the picture'
                            text: >-
                              Over the foot of the picture on a wide screen, below it on
                              a narrow one.
                            caption_position: 'overlay'
                            link: 't3://page?uid=3'
                            link_label: 'Look at the images'
                            link_variant: 'secondary'
                      - self:
                            id: 463
                            header: 'The fourth slide'
                            text: >-
                              Out of view until the track is scrolled, which is what
                              makes it obvious that it scrolls.
                      - self:
                            id: 464
                            header: 'A carousel with no heading'
                            text: >-
                              The region is named after the element, and takes a generic
                              name where the element has none.
                      - self:
                            id: 465
                            header: 'A slide without a picture'
                            text: >-
                              The caption is the whole slide.
                - self:
                      id: 151
                      title: 'Split tiles'
                      slug: '/elements/theme/split-tiles'
                      backend_layout: 'content_sidebar'
                      abstract: 'Featurettes in a column, the picture changing sides from tile to tile, each tile with its own tone.'
                  entities:
                    content:
                      - self:
                            id: 15101
                            CType: 'header'
                            header: 'Split tiles'
                            header_layout: 1
                      # "layout" 0 starts the alternation with the picture at
                      # the start of the line, 1 at the end. Every tone is on
                      # the page, spread over the two.
                      - self:
                            id: 15102
                            CType: 'theme_split_tiles'
                            header: 'The first picture at the start'
                            header_layout: 2
                            layout: 0
                            tx_theme_list_items: '470,471,472'
                      - self:
                            id: 15103
                            CType: 'theme_split_tiles'
                            header: 'The first picture at the end'
                            header_layout: 2
                            layout: 1
                            tx_theme_list_items: '473,474'
                    listItem:
                      - self:
                            id: 470
                            header: 'Write the content first'
                            text: >-
                              A tile without a tone takes the surface fill, and its
                              picture sits at the start of the line.
                            link: 't3://page?uid=2'
                            link_label: 'Read the text'
                      - self:
                            id: 471
                            header: 'Pick the elements'
                            text: >-
                              The second tile is the accent tint, and its picture has
                              moved to the end of the line with no class of its own.
                            tone: 'accent'
                            link: 't3://page?uid=8'
                            link_label: 'Open the elements'
                            link_variant: 'secondary'
                      - self:
                            id: 472
                            header: 'Check it in the dark'
                            text: >-
                              The inverse tile turns the colour scheme of everything
                              inside it, the button included.
                            tone: 'inverse'
                            link: 't3://page?uid=9'
                            link_label: 'Open the styleguide'
                      - self:
                            id: 473
                            header: 'Nothing here yet'
                            text: >-
                              The placeholder tone: no fill and a dashed frame, for a
                              tile standing in for content that does not exist. This one
                              has no picture either.
                            tone: 'placeholder'
                      - self:
                            id: 474
                            header: 'The rhythm carries on'
                            text: >-
                              This element starts on the other foot, so the two groups
                              below one another read as one column of tiles rather than
                              repeating the same arrangement.
                            tone: 'accent'
                # "theme_external_media" - a video of another site, loaded only
                # when the reader presses play. Both ratios the form offers are
                # shown, and both ends of what the processor recognises: a
                # YouTube watch URL and a Vimeo one are rewritten to the host
                # that sets no cookie, and a host nobody vetted is not embedded
                # at all - that element renders the poster and the link, which
                # is also what every element here renders without JavaScript.
                #
                # The addresses are real ones. Nothing requests them: the page
                # carries no iframe until somebody presses the button.
                - self:
                      id: 130
                      title: 'External media'
                      slug: '/elements/theme/external-media'
                      backend_layout: 'content_sidebar'
                      abstract: 'A video of another site, with nothing loaded from it until the reader presses play.'
                  entities:
                    content:
                      - self:
                            id: 13001
                            CType: 'header'
                            header: 'External media'
                            header_layout: 1
                      - self:
                            id: 13002
                            CType: 'theme_external_media'
                            header: 'A YouTube video, widescreen'
                            header_layout: 2
                            tx_theme_embed_url: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ'
                            tx_theme_embed_title: 'The TYPO3 project in three minutes'
                            tx_theme_embed_ratio: '16-9'
                      - self:
                            id: 13003
                            CType: 'theme_external_media'
                            header: 'A Vimeo video, in the classic shape'
                            header_layout: 2
                            tx_theme_embed_url: 'https://vimeo.com/76979871'
                            tx_theme_embed_title: 'An interview about theme development'
                            tx_theme_embed_ratio: '4-3'
                      # No poster: the frame keeps its shape and stays empty
                      # until the video is played.
                      - self:
                            id: 13004
                            CType: 'theme_external_media'
                            header: 'Without a poster image'
                            header_layout: 2
                            tx_theme_embed_url: 'https://youtu.be/dQw4w9WgXcQ'
                            tx_theme_embed_title: 'The same video, without a poster'
                            tx_theme_embed_ratio: '16-9'
                      # A host the processor does not recognise. No iframe is
                      # built for it at all - see the class for why an
                      # arbitrary URL is not embedded - so this element is the
                      # poster and the link, and no button.
                      - self:
                            id: 13005
                            CType: 'theme_external_media'
                            header: 'A host that is not embedded'
                            header_layout: 2
                            tx_theme_embed_url: 'https://media.example.org/videos/the-launch'
                            tx_theme_embed_title: 'A recording of the launch'
                            tx_theme_embed_ratio: '16-9'
                # The pricing plans. "content" rather than "content_sidebar",
                # unlike most of its siblings: three plans share one row above
                # 48rem, and beside the sub navigation the column is too narrow
                # for three columns of prices to be compared at a glance.
                #
                # The features are one per line - a literal block, not a folded
                # one, because a line break is the structure of that column
                # here rather than a wrap of the source. The relation says so
                # with "wrap = off", the way the core marks the "bodytext" of
                # the bullet list.
                - self:
                      id: 170
                      title: 'Pricing'
                      slug: '/elements/theme/pricing'
                      backend_layout: 'content'
                      abstract: 'Plans side by side, each with a price, what it is per, its features and a button.'
                  entities:
                    content:
                      - self:
                            id: 17001
                            CType: 'header'
                            header: 'Pricing'
                            header_layout: 1
                      # Three plans, the middle one highlighted: the modifier is
                      # shown beside the plans it is meant to be compared
                      # against. The prices are of different widths on purpose,
                      # which is what the tabular figures line up.
                      - self:
                            id: 17002
                            CType: 'theme_pricing'
                            header: 'Three plans'
                            header_layout: 2
                            tx_theme_list_items: '500,501,502'
                      # One plan takes the whole column: the element lays out
                      # the plans it is given and has no column count to pick.
                      - self:
                            id: 17003
                            CType: 'theme_pricing'
                            header: 'A single plan'
                            header_layout: 2
                            tx_theme_list_items: '503'
                    listItem:
                      - self:
                            id: 500
                            header: 'Starter'
                            subheader: '0 €'
                            meta: 'per month'
                            text: |-
                              One site
                              Community support
                            link: 't3://page?uid=10'
                            link_label: 'Start for nothing'
                            link_variant: 'secondary'
                      # The recommended plan, and the only one of the three with
                      # "highlighted" set.
                      - self:
                            id: 501
                            header: 'Team'
                            subheader: '19 €'
                            meta: 'per seat and month'
                            highlighted: 1
                            text: |-
                              Ten sites
                              Support within one working day
                              Staging and deployment
                              Audit log
                            link: 't3://page?uid=10'
                            link_label: 'Choose Team'
                      # No link: a plan an editor left without one ends with its
                      # features.
                      - self:
                            id: 502
                            header: 'Agency'
                            subheader: '49 €'
                            meta: 'per seat and month'
                            text: |-
                              Sites without a limit
                              Support within four hours
                              A named contact
                      - self:
                            id: 503
                            header: 'Everything, one price'
                            subheader: '99 €'
                            meta: 'per month'
                            text: |-
                              Every feature of every plan above
                              Support within four hours
                            link: 't3://page?uid=10'
                            link_label: 'Get in touch'

                # The layouts and the eyebrow of the three heroes. The images
                # are attached in "config.yml". "header_layout" 2 throughout:
                # the page heading is the h1.
                - self:
                      id: 110
                      title: 'Hero layouts'
                      slug: '/elements/theme/hero'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 11001
                            CType: 'header'
                            header: 'Hero layouts'
                            header_layout: 1
                      - self:
                            id: 11002
                            CType: 'theme_hero'
                            tx_theme_hero_layout: ''
                            tx_theme_eyebrow: 'The default layout'
                            header: 'The image beside the text, at the start'
                            header_layout: 2
                            bodytext: >-
                              The hero as it always rendered: the image takes 45 per
                              cent of the row on a wide screen and sits above the text on
                              a narrow one. The short line above the title is the
                              eyebrow, a field of its own.
                            tx_theme_link: 't3://page?uid=8'
                            tx_theme_link_label: 'All theme elements'
                            tx_theme_link_icon: 'arrow-left'
                      - self:
                            id: 11003
                            CType: 'theme_hero'
                            tx_theme_hero_layout: 'image-end'
                            tx_theme_eyebrow: 'Image at the end'
                            header: 'The image beside the text, at the end'
                            header_layout: 2
                            bodytext: >-
                              The image moves to the end of the row. In the markup it
                              still comes first, so a screen reader meets the same order
                              as in the default layout.
                            tx_theme_link: 't3://page?uid=31'
                            tx_theme_link_label: 'Read about text in columns'
                            tx_theme_link_variant: 'secondary'
                      - self:
                            id: 11004
                            CType: 'theme_hero'
                            tx_theme_hero_layout: 'centred'
                            tx_theme_eyebrow: 'Centred'
                            header: 'Everything on one axis'
                            header_layout: 2
                            bodytext: >-
                              Title, lead, action and image centred, at every width.
                              The image is no wider than the reading measure, so it never
                              outgrows the text.
                            tx_theme_link: 't3://page?uid=2'
                            tx_theme_link_label: 'Open the typography'
                      - self:
                            id: 11005
                            CType: 'theme_hero'
                            tx_theme_hero_layout: 'screenshot'
                            tx_theme_eyebrow: 'Screenshot'
                            header: 'An application that continues below'
                            header_layout: 2
                            bodytext: >-
                              Centred text and the image below it, resting on the
                              bottom edge of the panel, which cuts it off. This is the one
                              layout that puts the image after the text in the markup as
                              well.
                            tx_theme_link: 't3://page?uid=9'
                            tx_theme_link_label: 'Open the styleguide'
                      - self:
                            id: 11006
                            CType: 'theme_hero'
                            tx_theme_hero_layout: 'bordered'
                            tx_theme_eyebrow: 'Cropped'
                            header: 'The image runs out of the panel'
                            header_layout: 2
                            bodytext: >-
                              The image sits at the end of the row and is cut off by the
                              end and the bottom edges of the panel. A hairline frames the
                              two edges that stay inside it.
                            tx_theme_link: 't3://page?uid=3'
                            tx_theme_link_label: 'Look at the media page'
                            tx_theme_link_variant: 'ghost'
                      - self:
                            id: 11007
                            CType: 'theme_hero_small'
                            tx_theme_hero_layout: 'image-end'
                            tx_theme_eyebrow: 'Reduced, image at the end'
                            header: 'The reduced hero offers three layouts'
                            header_layout: 2
                            bodytext: >-
                              The default, the image at the end, and centred. It has
                              too little height for an image cut off at an edge.
                      - self:
                            id: 11008
                            CType: 'theme_hero_small'
                            tx_theme_hero_layout: 'centred'
                            tx_theme_eyebrow: 'Reduced, centred'
                            header: 'A sub page header, centred'
                            header_layout: 2
                            bodytext: >-
                              The same panel with less air above and below, and its
                              image above the text.
                      - self:
                            id: 11009
                            CType: 'theme_hero_text_only'
                            tx_theme_hero_layout: 'centred'
                            tx_theme_eyebrow: 'Without media, centred'
                            header: 'No image, and two layouts'
                            header_layout: 2
                            bodytext: >-
                              The hero without media can be centred or not. There is no
                              image to place, so the other layouts are not offered.
                            tx_theme_link: 't3://page?uid=5'
                            tx_theme_link_label: 'Back to the elements'
                            tx_theme_link_variant: 'link'
                      # The default layout of the two smaller heroes, so each
                      # hero shows every layout it offers on this page.
                      - self:
                            id: 11010
                            CType: 'theme_hero_small'
                            tx_theme_hero_layout: ''
                            tx_theme_eyebrow: 'Reduced, the default'
                            header: 'The reduced hero in its default layout'
                            header_layout: 2
                            bodytext: >-
                              The image at the start of the row, as on the page of the
                              theme elements, now with an eyebrow above the title.
                      - self:
                            id: 11011
                            CType: 'theme_hero_text_only'
                            tx_theme_hero_layout: ''
                            tx_theme_eyebrow: 'Without media, the default'
                            header: 'Text at the start of the line'
                            header_layout: 2
                            bodytext: >-
                              The hero without media in its default layout: the text
                              starts at the start of the line and runs to the reading
                              measure.

                # The call to action in every tone and width: a box on the
                # surface with an icon and both links, a band on the accent
                # tint, a box in the other appearance, and a placeholder band
                # for content that does not exist yet.
                - self:
                      id: 111
                      title: 'Calls to action'
                      slug: '/elements/theme/cta'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 11101
                            CType: 'header'
                            header: 'Calls to action'
                            header_layout: 1
                      - self:
                            id: 11102
                            CType: 'theme_cta'
                            tx_theme_cta_tone: ''
                            tx_theme_cta_width: 'boxed'
                            tx_theme_icon: 'rocket'
                            header: 'Build the next extension against this theme'
                            header_layout: 2
                            bodytext: |
                              <p>Every content element, every component and both appearances
                              are on these pages. Start with the elements, or read the
                              component library first.</p>
                            tx_theme_link: 't3://page?uid=5'
                            tx_theme_link_label: 'See every element'
                            tx_theme_link_icon: 'arrow-right'
                            tx_theme_secondary_link: 't3://page?uid=9'
                            tx_theme_secondary_link_label: 'Open the styleguide'
                            tx_theme_secondary_link_variant: 'secondary'
                      - self:
                            id: 11103
                            CType: 'theme_cta'
                            tx_theme_cta_tone: 'accent'
                            tx_theme_cta_width: 'band'
                            tx_theme_icon: 'book-open'
                            header: 'The TYPO3 documentation'
                            header_layout: 2
                            bodytext: |
                              <p>A band across the column on the tint of the accent, the same
                              tint as the accent band of the Appearance tab. It follows the
                              palette chosen in the display settings.</p>
                            tx_theme_link: 'https://docs.typo3.org/'
                            tx_theme_link_label: 'Read the documentation'
                            tx_theme_secondary_link: 't3://page?uid=51'
                            tx_theme_secondary_link_label: 'Compare with the bands'
                            tx_theme_secondary_link_variant: 'link'
                      - self:
                            id: 11104
                            CType: 'theme_cta'
                            tx_theme_cta_tone: 'inverse'
                            tx_theme_cta_width: 'boxed'
                            tx_theme_icon: 'moon'
                            header: 'The other appearance, in a box'
                            header_layout: 2
                            bodytext: |
                              <p>The inverse tone turns the colour scheme of everything inside
                              it: dark on a light page, light on a dark one. The buttons turn
                              with it.</p>
                            tx_theme_link: 't3://page?uid=2'
                            tx_theme_link_label: 'Read the typography'
                            tx_theme_secondary_link: 't3://page?uid=10'
                            tx_theme_secondary_link_label: 'Try the forms'
                            tx_theme_secondary_link_variant: 'ghost'
                      - self:
                            id: 11105
                            CType: 'theme_cta'
                            tx_theme_cta_tone: 'placeholder'
                            tx_theme_cta_width: 'band'
                            tx_theme_icon: 'newspaper'
                            header: 'No articles yet'
                            header_layout: 2
                            bodytext: |
                              <p>The placeholder tone has no fill and a dashed frame. It
                              stands in for content that is not there yet, and says what to
                              do about it.</p>
                            tx_theme_link: 't3://page?uid=56'
                            tx_theme_link_label: 'Read the sample article'
                            tx_theme_link_icon: 'pen'

                # The three styles of the testimonial. The attributions are
                # real people and what they are known for; the quotations are
                # written for this page and attributed to nobody as a quote of
                # theirs - "header" and "subheader" say who is speaking in the
                # example, the copy says what the style does.
                - self:
                      id: 112
                      title: 'Quotation styles'
                      slug: '/elements/theme/quote'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 11201
                            CType: 'header'
                            header: 'Quotation styles'
                            header_layout: 1
                      - self:
                            id: 11202
                            CType: 'text'
                            header: 'Three ways to set a quotation'
                            header_layout: 2
                            bodytext: |
                              <p>The testimonial sets its quotation with a rule at the
                              start by default. A pull quote lifts a sentence out of the
                              text around it; a centred quotation stands on its own. Both
                              open with a quotation mark from the icon set.</p>
                      - self:
                            id: 11203
                            CType: 'theme_testimonial'
                            tx_theme_quote_style: ''
                            header: 'The default style'
                            subheader: 'A rule at the start'
                            bodytext: >-
                              The quotation as every testimonial rendered it so far: set
                              apart by a rule at the start of the line and by its weight,
                              without a quotation mark.
                      - self:
                            id: 11204
                            CType: 'theme_testimonial'
                            tx_theme_quote_style: 'pull'
                            header: 'The pull quote'
                            subheader: 'Larger, between two rules'
                            bodytext: >-
                              One sentence worth reading twice, lifted out of the text
                              and set large enough to be found while scrolling past.
                      - self:
                            id: 11205
                            CType: 'theme_testimonial'
                            tx_theme_quote_style: 'centred'
                            header: 'The centred quotation'
                            subheader: 'On one axis, held to the measure'
                            bodytext: >-
                              A quotation that stands on its own between two sections of
                              a page, centred below its mark and never wider than the
                              reading measure.
                      # A portrait of the attributed person, next to the name.
                      # The image is attached in "config.yml"; the template
                      # renders it as a decorative avatar, because the name
                      # beside it already says who is quoted.
                      - self:
                            id: 11206
                            CType: 'theme_testimonial'
                            tx_theme_quote_style: ''
                            header: 'With a portrait'
                            subheader: 'An avatar before the name'
                            bodytext: >-
                              A picture of the person quoted, as a round avatar at the
                              start of the attribution. It stands beside the name, so it
                              carries no text of its own.

            # The appearance fields of a content element, every value once:
            # the frames, the space before and after, the alignment and the
            # look of the header. Text elements throughout, so nothing but
            # the field under demonstration differs between two of them.
            # "ShowcaseTreeTest" reads the values the form offers - TCA and
            # page TSconfig - and holds this page to every one of them.
            #
            # The spacing elements sit on the surface band, so the edge the
            # space is measured from is on the page.
            - self:
                  id: 51
                  title: 'Frames'
                  slug: '/elements/frames'
                  backend_layout: 'content'
                  abstract: 'Every frame, every spacing and every header alignment and look of the Appearance tab, one element each.'
                  keywords: 'content elements, appearance'
              entities:
                content:
                  - self:
                        id: 5101
                        CType: 'header'
                        header: 'Frames'
                        header_layout: 1
                  - self:
                        id: 5102
                        CType: 'text'
                        header: 'The Appearance tab'
                        header_layout: 2
                        bodytext: |
                          <p>Every content element carries the same appearance fields,
                          whatever it renders. The frame fills the element with a band, the
                          spacing moves it away from its neighbours, and the header palette
                          aligns the heading and sets its look apart from its level. The
                          elements below are text elements, one per value.</p>
                  - self:
                        id: 5103
                        CType: 'text'
                        header: 'Frame: Default'
                        header_layout: 2
                        frame_class: 'default'
                        bodytext: &frameBody |
                          <p>Running text in the frame of the heading, with
                          <a href="t3://page?uid=51">a link</a> and <strong>strong</strong>
                          words, so the colours of the band are tested on more than one
                          role.</p>
                  - self:
                        id: 5104
                        CType: 'text'
                        header: 'Frame: No frame'
                        header_layout: 2
                        frame_class: 'none'
                        bodytext: *frameBody
                  - self:
                        id: 5105
                        CType: 'text'
                        header: 'Frame: Surface band'
                        header_layout: 2
                        frame_class: 'surface'
                        bodytext: *frameBody
                  - self:
                        id: 5106
                        CType: 'text'
                        header: 'Frame: Raised band'
                        header_layout: 2
                        frame_class: 'raised'
                        bodytext: *frameBody
                  - self:
                        id: 5107
                        CType: 'text'
                        header: 'Frame: Accent band'
                        header_layout: 2
                        frame_class: 'accent'
                        bodytext: *frameBody
                  - self:
                        id: 5108
                        CType: 'text'
                        header: 'Frame: Inverse band'
                        header_layout: 2
                        frame_class: 'inverse'
                        bodytext: *frameBody
                  - self:
                        id: 5109
                        CType: 'text'
                        header: 'Space before and after: extra small'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'extra-small'
                        space_after_class: 'extra-small'
                        bodytext: &spaceBody '<p>The space before and after this element is the value in its heading.</p>'
                  - self:
                        id: 5110
                        CType: 'text'
                        header: 'Space before and after: small'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'small'
                        space_after_class: 'small'
                        bodytext: *spaceBody
                  - self:
                        id: 5111
                        CType: 'text'
                        header: 'Space before and after: medium'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'medium'
                        space_after_class: 'medium'
                        bodytext: *spaceBody
                  - self:
                        id: 5112
                        CType: 'text'
                        header: 'Space before and after: large'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'large'
                        space_after_class: 'large'
                        bodytext: *spaceBody
                  - self:
                        id: 5113
                        CType: 'text'
                        header: 'Space before and after: extra large'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'extra-large'
                        space_after_class: 'extra-large'
                        bodytext: *spaceBody
                  - self:
                        id: 5114
                        CType: 'text'
                        header: 'Header centred'
                        header_layout: 2
                        header_position: 'center'
                        bodytext: &headerBody '<p>The heading of this element is set as its heading says.</p>'
                  - self:
                        id: 5115
                        CType: 'text'
                        header: 'Header at the end of the line'
                        header_layout: 2
                        header_position: 'right'
                        bodytext: *headerBody
                  - self:
                        id: 5116
                        CType: 'text'
                        header: 'Header at the start of the line'
                        header_layout: 2
                        header_position: 'left'
                        bodytext: *headerBody
                  # The looks, on a heading 3 each: the level stays the
                  # outline, the look is how loud the heading is.
                  - self:
                        id: 5117
                        CType: 'text'
                        header: 'A heading 3 in the display size'
                        header_layout: 3
                        tx_theme_header_style: 'display'
                        bodytext: *headerBody
                  - self:
                        id: 5118
                        CType: 'text'
                        header: 'A heading 3 like heading 1'
                        header_layout: 3
                        tx_theme_header_style: 'h1'
                        bodytext: *headerBody
                  - self:
                        id: 5119
                        CType: 'text'
                        header: 'A heading 3 like heading 2'
                        header_layout: 3
                        tx_theme_header_style: 'h2'
                        bodytext: *headerBody
                  - self:
                        id: 5120
                        CType: 'text'
                        header: 'A heading 3 like heading 3'
                        header_layout: 3
                        tx_theme_header_style: 'h3'
                        bodytext: *headerBody
                  - self:
                        id: 5121
                        CType: 'text'
                        header: 'A heading 3 like heading 4'
                        header_layout: 3
                        tx_theme_header_style: 'h4'
                        bodytext: *headerBody
                  - self:
                        id: 5122
                        CType: 'text'
                        header: 'A heading 3 like heading 5'
                        header_layout: 3
                        tx_theme_header_style: 'h5'
                        bodytext: *headerBody

        # The page layouts. One page per backend layout the theme registers
        # beyond the four a content page uses, each of them a wireframe: one
        # content element per column, labelled with the name of the slot and
        # the colPos it sits in, so the page itself answers which column is
        # which without opening the page module.
        #
        # The boxes are not drawn by anything new. Every content element
        # already carries the dashed outline and the CType chip of the Frame
        # variant, and "frame_class" tints it - so a wireframe box is an
        # ordinary "text" element with an appearance an editor can set.
        #
        # "ShowcaseTreeTest::everyRegisteredBackendLayoutIsUsedByASeededPage"
        # is what keeps this section complete: a layout registered in
        # "Configuration/PageTsConfig/BackendLayouts" with no seeded page
        # using it fails that test.
        - self:
              id: 171
              title: 'Layouts'
              slug: '/layouts'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 17101
                    CType: 'header'
                    header: 'Page layouts'
                    header_layout: 1
              - self:
                    id: 17102
                    CType: 'text'
                    header: 'What a backend layout decides'
                    header_layout: 2
                    bodytext: |
                      <p>A page's backend layout decides two separate things: which
                      columns the page module offers an editor, and which template the
                      frontend renders. The first is page TSconfig, the second is the
                      file named after the layout in
                      <code>Resources/Private/Templates/Page/</code>, and the colPos
                      numbers are the only thing that connects them.</p>
                      <p>Each page below shows one layout, with a labelled box in every
                      column it declares. The boxes are ordinary content elements: the
                      dashed outline and the type chip around them are the theme's own
                      development affordance, which the display settings in the header
                      switch off.</p>
              - self:
                    id: 17103
                    CType: 'menu_subpages'
                    header: 'One page per layout'
                    header_layout: 2
                    pages: '171'
          children:
            # "two_columns": stage above, then two equal columns. The second
            # element in the main column is there to show that a column
            # stacks its elements rather than turning each into a grid item -
            # the failure mode the column wrapper in
            # "Templates/Page/TwoColumns.html" exists to prevent.
            - self:
                  id: 172
                  title: 'Two columns'
                  slug: '/layouts/two-columns'
                  backend_layout: 'two_columns'
                  abstract: 'Two content columns of equal width, below a full width stage.'
              entities:
                content:
                  - self:
                        id: 17201
                        CType: 'text'
                        colPos: 2
                        frame_class: 'accent'
                        header: 'Stage - colPos 2'
                        header_layout: 1
                        bodytext: |
                          <p>The full width row above the columns. It is the same slot
                          the content page and the start page use, so a hero placed
                          here renders the same way on all three.</p>
                  - self:
                        id: 17202
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'Main content - colPos 0'
                        header_layout: 2
                        bodytext: |
                          <p>The first column, and the column every other layout has as
                          well. Content moved here from a content page keeps its colPos
                          and stays where it was.</p>
                  - self:
                        id: 17203
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'A second element in the same column'
                        header_layout: 3
                        bodytext: |
                          <p>Elements of one column stack; they do not become columns of
                          their own. The column is a wrapper in the markup for exactly
                          this reason.</p>
                  - self:
                        id: 17204
                        CType: 'text'
                        colPos: 3
                        frame_class: 'raised'
                        header: 'Second column - colPos 3'
                        header_layout: 2
                        bodytext: |
                          <p>The second column of this layout and of the two below it.
                          Both columns are half the content width from 768 pixels up and
                          stacked below that.</p>

            # "two_columns_wide": the same two slots, 2fr to 1fr. Same colPos
            # numbers as the page above, which is the point of the numbering.
            - self:
                  id: 173
                  title: 'Two columns, wide first'
                  slug: '/layouts/two-columns-wide'
                  backend_layout: 'two_columns_wide'
                  abstract: 'The same two columns, the first one twice as wide as the second.'
              entities:
                content:
                  - self:
                        id: 17301
                        CType: 'text'
                        colPos: 2
                        frame_class: 'accent'
                        header: 'Stage - colPos 2'
                        header_layout: 1
                        bodytext: |
                          <p>The same stage slot as on every other layout that has
                          one.</p>
                  - self:
                        id: 17302
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'Main content - colPos 0'
                        header_layout: 2
                        bodytext: |
                          <p>Two thirds of the content width. The ratio is stated twice -
                          as a three column page module grid this column spans two of,
                          and as a grid fraction in the template - because the page
                          module has whole columns to work with and the frontend has
                          fractions.</p>
                  - self:
                        id: 17303
                        CType: 'text'
                        colPos: 3
                        frame_class: 'raised'
                        header: 'Second column - colPos 3'
                        header_layout: 2
                        bodytext: |
                          <p>One third. Switching this page to the equal column layout
                          moves nothing: both layouts read colPos 0 and colPos 3.</p>

            # "three_columns": colPos 4 joins the two above it.
            - self:
                  id: 174
                  title: 'Three columns'
                  slug: '/layouts/three-columns'
                  backend_layout: 'three_columns'
                  abstract: 'Three content columns of equal width, below a full width stage.'
              entities:
                content:
                  - self:
                        id: 17401
                        CType: 'text'
                        colPos: 2
                        frame_class: 'accent'
                        header: 'Stage - colPos 2'
                        header_layout: 1
                        bodytext: |
                          <p>The stage spans all three columns.</p>
                  - self:
                        id: 17402
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'Main content - colPos 0'
                        header_layout: 2
                        bodytext: |
                          <p>The first of three equal columns.</p>
                  - self:
                        id: 17403
                        CType: 'text'
                        colPos: 3
                        frame_class: 'raised'
                        header: 'Second column - colPos 3'
                        header_layout: 2
                        bodytext: |
                          <p>The second. A page switched from here to one of the two
                          column layouts keeps this column where it is and leaves the
                          third unrendered, rather than reshuffling the first two.</p>
                  - self:
                        id: 17404
                        CType: 'text'
                        colPos: 4
                        frame_class: 'default'
                        header: 'Third column - colPos 4'
                        header_layout: 2
                        bodytext: |
                          <p>The third, and the only column of the theme that no other
                          layout declares. Left with the default appearance, so the
                          dashed outline is all that draws this box.</p>

            # "article": the running text held to the reading measure, with
            # the aside beside it. The aside is colPos 1, the same slot the
            # sidebar layout uses - see "Article.tsconfig" for why the number
            # is shared and the rendering is not.
            - self:
                  id: 175
                  title: 'Article'
                  slug: '/layouts/article'
                  backend_layout: 'article'
                  abstract: 'A long read at the reading measure, with an aside of notes beside it.'
              entities:
                content:
                  - self:
                        id: 17501
                        CType: 'text'
                        colPos: 2
                        frame_class: 'accent'
                        header: 'Stage - colPos 2'
                        header_layout: 1
                        bodytext: |
                          <p>The stage spans the article and its aside.</p>
                  - self:
                        id: 17502
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'The article - colPos 0'
                        header_layout: 2
                        bodytext: |
                          <p>Held to the reading measure, the same 68 characters the
                          lead of a hero and the running text of a quotation are held
                          to. The column may be wider than that; the text is not.</p>
                          <p>The measure is a property of the text rather than of the
                          layout, which is why it is a cap on this column and not a
                          grid track: a column holding something that is not prose - a
                          table, a gallery - still has the whole track to use.</p>
                  - self:
                        id: 17503
                        CType: 'text'
                        colPos: 1
                        frame_class: 'raised'
                        header: 'The aside - colPos 1'
                        header_layout: 2
                        bodytext: |
                          <p>The same column number as the sidebar of the sidebar
                          layout, so material moved between the two layouts stays where
                          it was. What differs is the rendering: there the slot goes
                          into the navigation column of the page shell, which also
                          carries the sub navigation and the table of contents; here it
                          is a column of the article grid that follows the article in
                          reading order and carries nothing but what an editor puts in
                          it.</p>

            # "cover": one column, centred in what the shell leaves between the
            # header and the footer. No stage and no breadcrumb.
            - self:
                  id: 176
                  title: 'Cover'
                  slug: '/layouts/cover'
                  backend_layout: 'cover'
                  abstract: 'A landing page of one column, centred in the viewport.'
              entities:
                content:
                  - self:
                        id: 17601
                        CType: 'text'
                        colPos: 0
                        frame_class: 'accent'
                        header: 'Cover - colPos 0'
                        header_layout: 1
                        bodytext: |
                          <p>The only column of this layout, centred between the header
                          and the footer. The page is as tall as the viewport whatever
                          is placed here, so a short cover sits in the middle of the
                          screen rather than under the header.</p>
                  - self:
                        id: 17602
                        CType: 'text'
                        colPos: 0
                        header: 'No stage, and no breadcrumb'
                        header_layout: 2
                        bodytext: |
                          <p>A cover page is the top of whatever it announces: a trail
                          above it would lead nowhere worth going, and a stage would be
                          a second place to put the banner that this page already is.</p>

            # "bands": the three columns of "three_columns", stacked and taken
            # out of the content container. The third band is "inverse", which
            # is the band that makes the full width obvious at a glance.
            - self:
                  id: 177
                  title: 'Full-width bands'
                  slug: '/layouts/bands'
                  backend_layout: 'bands'
                  abstract: 'Three bands, stacked, each spanning the full width of the viewport.'
              entities:
                content:
                  - self:
                        id: 17701
                        CType: 'text'
                        colPos: 0
                        frame_class: 'surface'
                        header: 'First band - colPos 0'
                        header_layout: 1
                        bodytext: |
                          <p>The page gives up its content container while it holds a
                          band stack, so a band reaches both edges of the viewport. The
                          header and the footer keep theirs.</p>
                  - self:
                        id: 17702
                        CType: 'text'
                        colPos: 3
                        frame_class: 'accent'
                        header: 'Second band - colPos 3'
                        header_layout: 2
                        bodytext: |
                          <p>The same column numbers as the three column layout, laid
                          out one below the other instead of side by side - so a page
                          moved between the two changes how it reads and not where its
                          content sits.</p>
                  - self:
                        id: 17703
                        CType: 'text'
                        colPos: 4
                        frame_class: 'inverse'
                        header: 'Third band - colPos 4'
                        header_layout: 2
                        bodytext: |
                          <p>An inverted band, which is the appearance that makes the
                          width obvious at a glance. Bands are flush against each
                          other; the space between two of them is the space after the
                          last element of the one above.</p>

        # The styleguide. Not a showcase of content elements: the "styleguide"
        # backend layout declares no usable content column at all, and
        # "Templates/Page/Styleguide.html" renders the component library
        # straight from Fluid, ignoring anything placed on the page.
        #
        # In the main navigation, like the form showcase below: the showcase
        # sections - elements, typography, and the styleguide with the forms -
        # are what a reviewer of the theme navigates. Both pages used to be
        # "nav_hide"; neither was ever "hidden", which answers 404 in the
        # frontend and defeats seeding a page that exists to be opened.
        - self:
              id: 9
              title: 'Styleguide'
              slug: '/styleguide'
              backend_layout: 'styleguide'

        # The form showcase. A request form built from the form contract of the
        # component library, rendered by "Templates/Page/Forms.html" from the
        # "forms" backend layout - which, like "styleguide", declares no usable
        # content column: the form is the page.
        - self:
              id: 10
              title: 'Forms'
              slug: '/forms'
              backend_layout: 'forms'
