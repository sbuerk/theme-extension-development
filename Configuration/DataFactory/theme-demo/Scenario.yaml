# The records of the seed set "theme-demo", in the scenario format of
# typo3/testing-framework as sbuerk/data-factory reads it.
#
# UIDS
#
# Every record declares its uid, and the numbering is a rule rather than a
# sequence:
#
#   pages               1-10, the first tree, and 30-39 and 50-56, the
#                       pages added below its sections - see below for why
#                       the new ones skip decades
#   tt_content          the uid of its page times 100, plus its position on
#                       that page: the third element of page 6 is 603, the
#                       second of page 35 is 3502
#   tx_theme_list_item  1-18 on page 8, numbered in the order they are
#                       declared - the link list's four deliberately not in
#                       the order they render in, see there
#
# The development instances mirror this tree below "/legacy/", generated by
# "Build/Scripts/generateLegacyScenario.php", which moves every uid by 1000
# in every table. For "tt_content" that is ten pages: the mirror of the
# content of page p takes the uids of the content of page p + 10. So a page
# with content never sits ten uids from another one:
#
#   11-19   taken by the mirror of the content of pages 1-9
#   20-29   the account pages of the instance set ("Accounts.yaml" of
#           "packages-dev/dev-site"), 2101 and 2201 their content
#   30-39   new pages, their mirrors' content in 40-49
#   50-59   new pages, their mirrors' content in 60-69
#
# and the next pages take 70-79, then 90-99. "GeneratedLegacyScenarioTest"
# fails on a uid the composed instance set declares twice.
#
# A declared "id" is a suggestion to DataHandler, honoured for an admin backend
# user only - which is why "data-factory:import" refuses to run as anybody
# else. A record without one would not get an auto increment uid either, but
# one from a counter starting at 10000, so nothing here relies on that.
#
# The uids are what the rest of the repository points at: the committed site
# configurations of the development instances name root page 1, the file
# references in "config.yml" name the records they hang on, the "shortcut"
# element names "tt_content_601", the link fields name "t3://page?uid=2", and
# the functional tests assert against pages 1 to 10.
#
# RELATIONS
#
# An inline relation needs no construct of its own. The parent writes the
# comma separated list of its children's declared ids into its relation field,
# the children are records of their own on the same page, and DataHandler
# resolves that list exactly like a backend form submit - writing "uid_foreign",
# "tablename" and "sorting_foreign" from the TCA of the parent field. Their
# order is the order of that list.
#
# "hidden: 0" sits on the wildcard entity and nowhere else. The "pages" TCA
# defaults the column to 1, so a page written without it exists and renders
# nothing. It must not be repeated on a declared entity: the wildcard is merged
# into each of them with array_merge_recursive(), so a key on both sides
# becomes the list [0, 0] and reaches the database as the string "Array".
entitySettings:
  '*':
    nodeColumnName: 'pid'
    columnNames: {id: 'uid'}
    defaultValues: {pid: 0, hidden: 0}
  page:
    isNode: true
    tableName: 'pages'
    parentColumnName: 'pid'
    defaultValues: {doktype: 1}
  content:
    tableName: 'tt_content'
  listItem:
    tableName: 'tx_theme_list_item'

entities:
  page:
    - self:
          id: 1
          title: 'Theme demo'
          slug: '/'
          is_siteroot: 1
          # "backend_layout" is an ordinary field of the record: everything a
          # scenario declares is written as it stands. The identifiers are the
          # ones registered in "Configuration/PageTsConfig/BackendLayouts".
          #
          # A site root is where the footer columns are conventionally edited,
          # which is what the "start" layout offers over "content".
          backend_layout: 'start'
      entities:
        content:
          - self:
                id: 101
                CType: 'header'
                header: 'A frontend to look at'
                header_layout: 1
          - self:
                id: 102
                CType: 'text'
                header: 'What this is'
                header_layout: 2
                bodytext: |
                  <p>This page tree is seeded from a definition in the repository, so a
                  development instance can be rebuilt from nothing without clicking a
                  page tree together by hand.</p>
                  <p>Two of its pages are not in the navigation: the
                  <a href="t3://page?uid=9">styleguide</a>, which renders every
                  component of the library, and the
                  <a href="t3://page?uid=10">form showcase</a>, a request form put
                  together from the form components alone.</p>
      children:
        - self:
              id: 2
              title: 'Typography'
              slug: '/typography'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 201
                    CType: 'header'
                    header: 'Typography'
                    header_layout: 1
              - self:
                    id: 202
                    CType: 'text'
                    header: 'Running text'
                    header_layout: 2
                    bodytext: |
                      <p>A paragraph of running text, so the theme has something to set
                      in its body font. It carries <strong>bold</strong> and
                      <em>emphasised</em> words and a <a href="t3://page?uid=1">link</a>, which are
                      the three inline cases a stylesheet has to answer for.</p>

              # The appearance fields of the core "Appearance" tab and of the
              # header palette, one band each, so the four bands the theme
              # adds to "frame_class" can be looked at next to plain running
              # text. "frame_class", "space_*_class", "header_position" and
              # "tx_theme_header_style" become modifiers in
              # "Layouts/ContentElement.html" and
              # "Partials/ContentElement/Header.html"; the page TSconfig in
              # "Configuration/PageTsConfig/ContentElementAppearance.tsconfig"
              # offers exactly these values in the form.
              - self:
                    id: 203
                    CType: 'text'
                    header: 'A surface band, header centred'
                    header_layout: 2
                    header_position: 'center'
                    frame_class: 'surface'
                    space_before_class: 'large'
                    bodytext: |
                      <p>The frame "Surface band" fills the element with the surface
                      colour and draws the hairline around it. The header is centred,
                      and the space before the element is "large".</p>
              - self:
                    id: 204
                    CType: 'text'
                    header: 'A raised band, header at the end, looking like heading 4'
                    header_layout: 2
                    header_position: 'right'
                    tx_theme_header_style: 'h4'
                    frame_class: 'raised'
                    bodytext: |
                      <p>The raised surface is white in the light appearance, so the
                      hairline is what draws this band there. The header is an h2 set
                      in the look of heading 4.</p>
              - self:
                    id: 205
                    CType: 'text'
                    header: 'An accent band, header in the display size'
                    header_layout: 2
                    tx_theme_header_style: 'display'
                    frame_class: 'accent'
                    bodytext: |
                      <p>A tint of the primary accent of the active palette, with a
                      <a href="t3://page?uid=3">link to the media page</a> in it:
                      switch the palette, and the tint follows it.</p>
              - self:
                    id: 206
                    CType: 'text'
                    header: 'An inverse band, header at the start'
                    header_layout: 2
                    header_position: 'left'
                    frame_class: 'inverse'
                    space_after_class: 'extra-large'
                    bodytext: |
                      <p>The page in the other appearance: dark on a light page, light
                      on a dark one, with every colour of the theme turned with it -
                      the <a href="t3://page?uid=1">link to the start page</a> as well.
                      The space after the element is "extra large".</p>

          # The typography section: the rich text path, from records. The
          # styleguide (uid 9) shows the same components as literal markup;
          # these pages prove that a record reaches them. Seeded "bodytext" is
          # written through DataHandler like a backend save, so the RTE
          # processing of the installation applies to it - with the theme's
          # preset, everything below survives - but it never went through
          # CKEditor, which is why it holds more than the toolbar offers.
          # The first element of each page says so.
          children:
            - self:
                  id: 52
                  title: 'Text'
                  slug: '/typography/text'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5201
                        CType: 'header'
                        header: 'Text'
                        header_layout: 1
                  - self:
                        id: 5202
                        CType: 'text'
                        bodytext: |
                          <p class="theme-lead">Running text, headings inside it, the inline
                          semantics and the alignments of the rich text editor - all from
                          one text element each.</p>
                          <p>The rich text of these pages is written into the database the
                          way a backend save writes it, so the processing of the rich text
                          preset has run over it. It has not been typed into the editor:
                          several things here need its source view.</p>
                  - self:
                        id: 5203
                        CType: 'text'
                        header: 'Headings inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The header of the element is its heading 2. The rich text
                          continues the outline below it.</p>
                          <h3>A heading 3 inside the text</h3>
                          <p>The editor offers heading 2 and heading 3, so a text can carry
                          sections of its own without an element per section.</p>
                          <h3>A second heading 3, long enough to wrap onto a second line at
                          the width of the content column</h3>
                          <p>A long heading is balanced over its lines, so the last line is
                          not a single orphaned word.</p>
                  - self:
                        id: 5204
                        CType: 'text'
                        header: 'Inline semantics'
                        header_layout: 2
                        bodytext: |
                          <p><strong>Strong importance</strong> and <em>stress emphasis</em>,
                          <mark>a marked passage</mark>, <small>small print</small>,
                          <s>no longer accurate</s>, <del>deleted</del> and <ins>inserted</ins>
                          text, H<sub>2</sub>O and E = mc<sup>2</sup>.</p>
                          <p>An <abbr title="Rich Text Editor">RTE</abbr> writes
                          <code>&lt;p&gt;</code> around a paragraph; <kbd>Ctrl</kbd> +
                          <kbd>B</kbd> makes a word strong. A <dfn>content element</dfn> is
                          a record rendered into a column of a page. <q>A quotation inside
                          a sentence</q>, as <cite>the style guide</cite> puts it, gets its
                          quotation marks from the language.</p>
                  - self:
                        id: 5205
                        CType: 'text'
                        header: 'Alignment'
                        header_layout: 2
                        bodytext: |
                          <p class="theme-text--start">Aligned to the start of the line, which
                          is where every paragraph starts anyway.</p>
                          <p class="theme-text--center">Centred, for a short line that stands
                          on its own.</p>
                          <p class="theme-text--end">Aligned to the end of the line.</p>
                          <p class="theme-text--justify">Justified: both edges of the column
                          are straight, and the spaces between the words stretch to make them
                          so. Without hyphenation the gaps grow on a narrow column, which is
                          why the theme leaves justification to the paragraph that asks for
                          it.</p>
                  - self:
                        id: 5206
                        CType: 'text'
                        header: 'Language and direction'
                        header_layout: 2
                        bodytext: |
                          <p lang="de">Ein deutscher Absatz mit langen Zusammensetzungen wie
                          Inhaltselementdarstellung und Seitenbaumkonfigurationsdatei: mit der
                          Sprache am Absatz trennt der Browser sie am Zeilenende.</p>
                          <p dir="rtl" lang="ar">هذه فقرة قصيرة باللغة العربية، تبدأ من
                          اليمين وتنتهي في اليسار.</p>

            - self:
                  id: 53
                  title: 'Lists'
                  slug: '/typography/lists'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5301
                        CType: 'header'
                        header: 'Lists'
                        header_layout: 1
                  - self:
                        id: 5302
                        CType: 'text'
                        header: 'Lists inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The lists of the rich text keep the markers of the element
                          baseline, nested lists included:</p>
                          <ul>
                            <li>A first item</li>
                            <li>A second item, holding a list of its own
                              <ul>
                                <li>A nested item</li>
                                <li>A second nested item
                                  <ul><li>A third level</li></ul>
                                </li>
                              </ul>
                            </li>
                            <li>A last item</li>
                          </ul>
                          <ol>
                            <li>A first step</li>
                            <li>A second step, with sub steps
                              <ol><li>A sub step</li><li>Another sub step</li></ol>
                            </li>
                            <li>A last step</li>
                          </ol>
                  - self:
                        id: 5303
                        CType: 'text'
                        header: 'The list component inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The class of a list is kept by the rich text processing, so a
                          list in the source view can be one of the list component:</p>
                          <ul class="theme-list theme-list--check">
                            <li>Check marks, from the source view</li>
                            <li>The same component the bullet list element renders</li>
                          </ul>
                          <ul class="theme-list theme-list--columns-2">
                            <li>Header</li><li>Text</li><li>Text &amp; Images</li>
                            <li>Text &amp; Media</li><li>Images</li><li>Bullet list</li>
                            <li>Table</li><li>File links</li>
                          </ul>
                  # The element, rather than rich text: see "/elements/core/bullets"
                  # (uid 35) for every layout.
                  - self:
                        id: 5304
                        CType: 'bullets'
                        header: 'A definition list from the bullet list element'
                        header_layout: 2
                        bullets_type: 2
                        bodytext: |
                          Unstyled|No marker and no indent
                          Check|A check mark in front of every item
                          Icon|An icon of the set in front of every item
                          Columns|The items flow into two or three columns
                          Divided|A hairline between the rows
                  - self:
                        id: 5305
                        CType: 'text'
                        bodytext: '<p>Every layout of the bullet list element is on its <a href="t3://page?uid=35">own page</a>.</p>'

            - self:
                  id: 54
                  title: 'Tables'
                  slug: '/typography/tables'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5401
                        CType: 'header'
                        header: 'Tables'
                        header_layout: 1
                  # A table of the rich text keeps its classes through the
                  # parsing function of the frontend, but is not wrapped in the
                  # scroll region the table element renders - which is why the
                  # table element is the way to a wide table.
                  - self:
                        id: 5402
                        CType: 'text'
                        header: 'A table inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>A table of the rich text, with the classes of the table
                          component written in the source view. It has no scroll region of
                          its own, so it has to fit the column.</p>
                          <table class="theme-table theme-table--striped">
                            <caption>The two appearances</caption>
                            <thead><tr><th scope="col">Appearance</th><th scope="col">Background</th><th scope="col">Text</th></tr></thead>
                            <tbody>
                              <tr><th scope="row">Light</th><td>A near white</td><td>A near black</td></tr>
                              <tr><th scope="row">Dark</th><td>A near black</td><td>A near white</td></tr>
                            </tbody>
                          </table>
                  - self:
                        id: 5403
                        CType: 'table'
                        header: 'The table element, with a header column and a footer'
                        header_layout: 2
                        table_class: 'compact'
                        table_caption: 'Contrast of the text colours on the page background'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 2
                        table_tfoot: 1
                        bodytext: |
                          Primary text|The body copy|at least 7:1
                          Secondary text|Leads and captions|at least 4.5:1
                          Muted text|Dates and hints|at least 4.5:1
                          Every text colour|Holds AA on its surface|4.5:1
                  - self:
                        id: 5404
                        CType: 'text'
                        bodytext: '<p>Every class of the table element is on its <a href="t3://page?uid=36">own page</a>.</p>'

            - self:
                  id: 55
                  title: 'Quotes and code'
                  slug: '/typography/quotes-and-code'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5501
                        CType: 'header'
                        header: 'Quotes and code'
                        header_layout: 1
                  - self:
                        id: 5502
                        CType: 'text'
                        header: 'A quotation inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>A block quotation of the rich text, set apart from the running
                          text around it:</p>
                          <blockquote><p>Programs must be written for people to read, and only
                          incidentally for machines to execute.</p></blockquote>
                          <p>- <cite>Abelson and Sussman, Structure and Interpretation of
                          Computer Programs</cite></p>
                  # The testimonial element renders the quote component with its
                  # attribution; the name is "header", the role "subheader".
                  - self:
                        id: 5503
                        CType: 'theme_testimonial'
                        header: 'Harold Abelson and Gerald Jay Sussman'
                        subheader: 'Preface to the first edition, 1985'
                        bodytext: '<p>Programs must be written for people to read, and only incidentally for machines to execute.</p>'
                  - self:
                        id: 5504
                        CType: 'text'
                        header: 'Preformatted text inside the text'
                        header_layout: 2
                        bodytext: |
                          <p>The editor offers "Pre-Formatted Text" as a heading option. It
                          keeps line breaks and spaces, and scrolls rather than wraps:</p>
                          <pre><code>tt_content.bullets =&lt; lib.contentElement
                          tt_content.bullets.templateName = ContentElements/Bullets</code></pre>
                  # The code block component needs a figure, a caption and a
                  # scroll region with a label - markup for the HTML element,
                  # which renders it untouched.
                  - self:
                        id: 5505
                        CType: 'html'
                        header: 'A code block with its file name (name only, not rendered)'
                        bodytext: |
                          <h2 class="theme-content-element__heading">A code block with its file name</h2>
                          <figure class="theme-code">
                              <figcaption class="theme-code__caption">
                                  <span class="theme-code__filename">config/sites/demo/config.yaml</span>
                                  <span class="theme-code__language">YAML</span>
                              </figcaption>
                              <pre class="theme-code__block" role="region" tabindex="0" aria-label="config/sites/demo/config.yaml"><code>base: 'https://theme.example.com/'
                          rootPageId: 1
                          dependencies:
                            - sbuerk/theme-extension-development</code></pre>
                          </figure>

            # A long form article: every component of the section in one
            # reading flow, the way a page of a site uses them.
            - self:
                  id: 56
                  title: 'Article'
                  slug: '/typography/article'
                  backend_layout: 'content'
              entities:
                content:
                  - self:
                        id: 5601
                        CType: 'header'
                        header: 'How a content element reaches the page'
                        header_layout: 1
                  - self:
                        id: 5602
                        CType: 'text'
                        bodytext: |
                          <p class="theme-lead">An editor fills in a form and saves it. A
                          moment later a visitor reads a paragraph. Between the two lie a
                          database row, a few lines of configuration and a template - and
                          each of them decides something about what the visitor sees.</p>
                          <p>This article follows one element along that way. It is also a
                          specimen: a long text, set with the components a site package gets
                          from the theme, so the rhythm of headings, paragraphs, lists,
                          figures, tables and code can be read as a whole rather than one
                          specimen at a time.</p>
                  - self:
                        id: 5603
                        CType: 'text'
                        header: 'The record'
                        header_layout: 2
                        bodytext: |
                          <p>Everything starts as a row of the table <code>tt_content</code>.
                          Its type, the CType, decides which fields the form shows and which
                          rendering the frontend picks. The fields an editor fills in are
                          plain columns of that row: a header, a text, a layout, a frame.</p>
                          <h3>What the form does not show</h3>
                          <p>Some columns are there without a field in the form. The page the
                          element belongs to, its position in the column and its language are
                          written by the backend itself. They matter just as much: a content
                          element is rendered because a page asks for the content of one of
                          its columns, not because the element exists.</p>
                  - self:
                        id: 5604
                        CType: 'textpic'
                        header: 'The frame'
                        header_layout: 2
                        imageorient: 17
                        imagecols: 1
                        bodytext: |
                          <p>Every element is rendered through one frame, the layout
                          <code>ContentElement</code> of the theme. It writes the wrapper
                          with the anchor of the element, the CType as a modifier and as an
                          attribute, and the modifiers of the appearance fields: the band of
                          the frame, the space before and after.</p>
                          <p>The frame is also what the outlines of the development mode
                          draw. Switch them on in the display settings, and every element
                          of this article shows its boundary and its CType - the image
                          beside this text belongs to the same element as the text itself.</p>
                          <p>The image is placed in the text, at the end of the line. The
                          gallery processor of the core computes its width from the column
                          and the number of columns, and the theme floats it, so this text
                          flows around it and continues under it once it is past.</p>
                  - self:
                        id: 5605
                        CType: 'bullets'
                        header: 'What the frame reads'
                        header_layout: 2
                        bullets_type: 0
                        layout: 1
                        bodytext: |
                          The frame: default, no frame, or one of four bands
                          The space before and after the element
                          The alignment and the look of the header
                          The CType, for the modifier and the outline
                  - self:
                        id: 5606
                        CType: 'text'
                        header: 'The template'
                        header_layout: 2
                        bodytext: |
                          <p>Inside the frame, the template of the CType renders what is
                          particular to it. A text element renders its header and its rich
                          text; a table element renders a table from the lines of its text
                          field, split by a data processor before the template sees them.</p>
                          <blockquote><p>A template receives data it can render and never
                          computes what a processor could hand it.</p></blockquote>
                          <p>That division is the reason a table element and a table of the
                          rich text look the same: both end in the same component, one from
                          a processor, the other from the source view of the editor.</p>
                  - self:
                        id: 5607
                        CType: 'table'
                        header: 'The fields of a table element'
                        header_layout: 3
                        table_class: 'striped'
                        table_caption: 'What the table element reads, and what it does with it'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 1
                        bodytext: |
                          Field|Holds|Read by
                          bodytext|The rows, one per line|TableProcessor
                          table_delimiter|The character code of the column separator|TableProcessor
                          table_header_position|Header row, header column, or none|TableProcessor
                          table_class|The look of the table|Table.html
                  - self:
                        id: 5608
                        CType: 'html'
                        header: 'The TypoScript of the bullet list (name only, not rendered)'
                        bodytext: |
                          <figure class="theme-code">
                              <figcaption class="theme-code__caption">
                                  <span class="theme-code__filename">Configuration/TypoScript/ContentElements.typoscript</span>
                                  <span class="theme-code__language">TypoScript</span>
                              </figcaption>
                              <pre class="theme-code__block" role="region" tabindex="0" aria-label="Configuration/TypoScript/ContentElements.typoscript"><code>tt_content.bullets =&lt; lib.contentElement
                          tt_content.bullets {
                              templateName = ContentElements/Bullets
                          }</code></pre>
                          </figure>
                  - self:
                        id: 5609
                        CType: 'text'
                        header: 'What is left to the editor'
                        header_layout: 2
                        bodytext: |
                          <p>The editor chooses the type, fills in the fields and picks from
                          the values the form offers. The theme offers only values it
                          renders: a frame it does not draw is not in the list, and a field
                          it does not read is not in the form.</p>
                          <p>Everything else - the markup, the spacing, the colours in both
                          appearances - follows from the theme, which is what lets a page of
                          many elements read as one page.</p>

        - self:
              id: 3
              title: 'Media'
              slug: '/media'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 301
                    CType: 'header'
                    header: 'Media'
                    header_layout: 1
              # The image is attached in "config.yml", which names this uid.
              - self:
                    id: 302
                    CType: 'image'
                    header: 'A single image'
                    header_layout: 2
              # The gallery layout is driven by the TCA fields of the element,
              # not by the template: "imagecols" is what an editor changes in
              # the backend to get a second column.
              - self:
                    id: 303
                    CType: 'image'
                    header: 'Two images, two columns'
                    header_layout: 2
                    imagecols: 2

        # The one page that declares no "backend_layout" at all, and it stays
        # that way on purpose: it is the only page in the tree that reaches the
        # hard coded "default" fallback of "PageLayoutResolver", so that path
        # has no other coverage. Asserted by "ShowcaseTreeTest".
        - self:
              id: 4
              title: 'Empty page'
              slug: '/empty'

        # The showcase branch. Its three children each demonstrate one family of
        # content elements, and between them they carry every CType this theme
        # renders - which "ShowcaseTreeTest" holds them to, by reading the
        # TypoScript rather than a list kept in the test.
        - self:
              id: 5
              title: 'Elements'
              slug: '/elements'
              backend_layout: 'content'
          entities:
            content:
              - self:
                    id: 501
                    CType: 'header'
                    header: 'Elements'
                    header_layout: 1
              - self:
                    id: 502
                    CType: 'text'
                    bodytext: |
                      <p>Three pages, one per family: the classic types
                      <em>EXT:frontend</em> registers without supplying a rendering,
                      the eleven menu types, and the thirteen this extension adds itself.
                      Every one of them appears exactly once, so a single branch of the
                      tree answers what the theme does with each.</p>
                      <p>Below the classic types, each has a page of its own with its
                      variants, and the <a href="t3://page?uid=51">Frames</a> page shows
                      every value of the Appearance tab, one element each.</p>
          children:
            - self:
                  id: 6
                  title: 'Core elements'
                  slug: '/elements/core'
                  # Read by the "menu_abstract" and "menu_related_pages" elements
                  # on page 7, which list the children of page 5 - this page
                  # among them. Without an abstract the item renders as a bare
                  # link, and without a shared keyword the related-pages menu
                  # comes back empty; both look like the element is broken rather
                  # than like the target page is missing a field.
                  abstract: 'Every classic content type EXT:frontend registers without supplying a rendering for it.'
                  keywords: 'content elements, core'
                  # "content_sidebar" rather than "content", and its sibling
                  # page 8 deliberately does not use it: two pages under one
                  # parent rendering with and without the sub navigation is what
                  # proves the backend layout is resolved per page instead of
                  # inherited down the branch.
                  #
                  # Nothing is seeded into the sidebar column (colPos 1) and
                  # nothing needs to be: "Partials/Page/Sidebar.html" renders the
                  # sub navigation above the "sidebar" slot unconditionally, so
                  # the aside is populated on a page that puts all its content in
                  # "main".
                  backend_layout: 'content_sidebar'
              entities:
                content:
                  # "header" - the whole element is a heading, nothing else.
                  #
                  # The "shortcut" element at the bottom of this page names this
                  # one by its uid: "records" is a plain string column holding
                  # "tt_content_<uid>", so the target's uid has to be known while
                  # the scenario is written.
                  #
                  # "header_layout" is the heading level: 1-5 are <h1>-<h5>, 0
                  # falls back to <h2>, and 100 means "do not display" - see
                  # "Partials/ContentElement/Header.html".
                  - self:
                        id: 601
                        CType: 'header'
                        header: 'Core elements'
                        header_layout: 1

                  # "text" - "bodytext" through "f:format.html", which means
                  # "lib.parseFunc_RTE": links are rewritten by typolink and
                  # lines that are not already wrapped are wrapped in <p>.
                  #
                  # The markup available here is not free-form. This CType's
                  # "bodytext" is declared "enableRichtext", so DataHandler runs
                  # the RTE "db" transformation over it ON SAVE, and which tags
                  # survive depends on the installation. With rte_ckeditor it is
                  # the processing of the rich text preset - the theme's own
                  # imports the core "Processing.yaml", which keeps "code" and
                  # "kbd". Without it, it is "RteHtmlParser::$defaultAllowedTagsList"
                  # (typo3/cms-core, Classes/Html/RteHtmlParser.php), which has
                  # neither. Dropping a tag is not an error and reports nothing;
                  # it is simply not in the row afterwards. So the copy of this
                  # element uses neither, and the "html" element further down is
                  # the one place on this page where they survive in every
                  # installation - its copy says so.
                  - self:
                        id: 602
                        CType: 'text'
                        header: 'What this page is'
                        header_layout: 2
                        bodytext: |
                          <p>Every classic content type this theme renders is on this
                          page exactly once, so a single URL answers what each one
                          looks like and which of its fields the theme actually reads.
                          The copy of each element describes that element.</p>
                          <p>The rendering is defined in
                          <em>Configuration/TypoScript/ContentElements.typoscript</em>.
                          This theme does not depend on <em>fluid_styled_content</em>,
                          so a content type without a branch in that file would render
                          the core's <strong>"no rendering definition"</strong> notice
                          instead of anything at all.</p>
                          <p>Each of them also has a page of its own below this one, in the
                          sub navigation, showing its variants: every orientation of
                          <a href="t3://page?uid=32">Text &amp; Images</a>, every layout of
                          the <a href="t3://page?uid=35">bullet list</a>, every class of the
                          <a href="t3://page?uid=36">table</a> and so on.</p>

                  # "bullets" - "bodytext" is one item per line, split by the
                  # core's own "SplitProcessor" (delimiter LF,
                  # "removeEmptyEntries = 1", so the trailing newline of this
                  # block scalar does not become an empty item).
                  #
                  # "bullets_type" selects the list element: 0 = <ul>, 1 = <ol>,
                  # 2 = <dl>. 0 is also the TCA default; it is written out anyway
                  # so the value the copy below talks about is visible next to it.
                  - self:
                        id: 603
                        CType: 'bullets'
                        header: 'Bullet list'
                        header_layout: 2
                        bullets_type: 0
                        bodytext: |
                          One item per line, with no markup and no delimiter
                          Each line is escaped on output, so a typed tag stays text
                          bullets_type 1 renders the same lines as an ordered list
                          bullets_type 2 renders them as a definition list of terms

                  # "table" - the element whose fields are least like their names.
                  #
                  # "bodytext" is delimiter separated text, one row per line, and
                  # "TableProcessor" (Classes/DataProcessing/TableProcessor.php)
                  # decodes it into header/body/footer rows before the template
                  # sees it. The fields that control the decode store CHARACTER
                  # CODES, not characters:
                  #
                  #   table_delimiter  the column separator, TCA default 124 = "|"
                  #                    (the other offered codes are 59, 44, 58, 9)
                  #   table_enclosure  the quoting character, 0 = none
                  #                    (the other offered codes are 39 and 34)
                  #
                  # 0 for "no enclosure" never reaches PHP as an empty string:
                  # "fgetcsv()" rejects that with a ValueError, so the processor
                  # passes chr(0) instead. That matters for the content here -
                  # with no real enclosure character, no cell below may contain
                  # the delimiter, which is why the first body row spells "124"
                  # out in words instead of showing the character.
                  #
                  # The remaining two are ordinary:
                  #
                  #   table_header_position  0 = none, 1 = first row is <thead>,
                  #                          2 = first cell of every row is a
                  #                              <th scope="row">
                  #   table_tfoot            1 = the last remaining row (after the
                  #                              header row was taken off) becomes
                  #                              <tfoot>
                  #
                  # "cols" and "table_class" exist in the core TCA of this element
                  # and are deliberately not set: the processor pads every row to
                  # the widest one by itself, and the template styles the table
                  # through ".theme-table" rather than a class from the record.
                  - self:
                        id: 604
                        CType: 'table'
                        header: 'Table'
                        header_layout: 2
                        table_caption: 'The five table_* fields, and what this element sets them to'
                        table_delimiter: 124
                        table_enclosure: 0
                        table_header_position: 1
                        table_tfoot: 1
                        bodytext: |
                          Field|What it stores|Value on this element
                          table_delimiter|The character CODE of the column separator|124, a vertical bar
                          table_enclosure|The character CODE of the quoting character, 0 for none|0
                          table_header_position|0 none, 1 first row, 2 first column|1
                          table_tfoot|Whether the last row becomes a table footer|1
                          Decoded by|Classes/DataProcessing/TableProcessor.php|not by Table.html

                  # "textpic" - text plus the core "image" field, laid out by
                  # "GalleryProcessor" from "imageorient" and "imagecols".
                  #
                  # "imageorient" 17 is "in text, right"; the values are 0/1/2
                  # above, 8/9/10 below and 17/18 in text - the processor turns
                  # them into the vertical/horizontal position the gallery partial
                  # renders as modifier classes. Together with "imagecols: 1" this
                  # is deliberately the opposite of the "textmedia" element below,
                  # so the two are distinguishable on the page rather than looking
                  # like one element seeded twice.
                  - self:
                        id: 605
                        CType: 'textpic'
                        header: 'Text & Images'
                        header_layout: 2
                        imageorient: 17
                        imagecols: 1
                        bodytext: |
                          <p>Reads the <em>image</em> field, which the core TCA
                          restricts to images. The gallery is placed by
                          <em>imageorient</em> - 17 here, "in text, right" - and split
                          into <em>imagecols</em> columns, both of them computed by
                          the core's <strong>GalleryProcessor</strong> rather than by
                          the template.</p>

                  # "textmedia" - identical to "textpic" except for the source
                  # field: "assets" allows every "common-media-type", not only
                  # images. Same two processors, and "GalleryProcessor" does not
                  # care which field the files came from.
                  #
                  # "imageorient" 8 is "below, centered", so this is also the one
                  # element on the page where the template puts the body text
                  # before the gallery in the DOM.
                  - self:
                        id: 606
                        CType: 'textmedia'
                        header: 'Text & Media'
                        header_layout: 2
                        imageorient: 8
                        imagecols: 2
                        bodytext: |
                          <p>Reads the <em>assets</em> field instead of
                          <em>image</em>, so a video or an audio file is allowed here
                          where "Text &amp; Images" above would refuse it. With
                          <em>imageorient</em> 8 the media sits below the text and
                          centered, and <em>imagecols</em> 2 puts the two files side
                          by side.</p>

                  # "image" - the same two processors as "textpic" without the
                  # body text. Kept minimal on purpose: the Media page (uid 3)
                  # already demonstrates the gallery settings, and this page only
                  # has to be complete, not to repeat them.
                  - self:
                        id: 607
                        CType: 'image'
                        header: 'Images'
                        header_layout: 2

                  # "uploads" - a file list, not a gallery.
                  #
                  # The fields "Uploads.html" actually reads are all set here:
                  #
                  #   uploads_type          0 = name only, 1 = name and file type
                  #                         icon, 2 = name and thumbnail. The
                  #                         component library has no file type
                  #                         icon, so 1 renders as 0; 2 renders a
                  #                         thumbnail only for files FAL
                  #                         classifies as an image, which an SVG
                  #                         is (image/* maps to FileType::IMAGE).
                  #   filelink_size         shows the byte count
                  #   uploads_description   shows the description of the file
                  #                         REFERENCE - which is why both
                  #                         references in "config.yml" carry one,
                  #                         and why they differ from the ones the
                  #                         same files carry elsewhere on this
                  #                         page.
                  #   filelink_sorting      "FilesProcessor" sorts natively, so
                  #                         this overrides the order the
                  #                         references are declared in - by name
                  #                         ascending, the portrait file first.
                  #                         That is the point of setting it.
                  #
                  # "target" is left at its default (empty): the template passes
                  # it straight into the link, and a demo page has no business
                  # forcing new windows.
                  #
                  # "file_collections" is the second source "FilesProcessor" is
                  # wired to and stays empty: a demonstration of one field does
                  # not justify seeding a "sys_file_collection".
                  - self:
                        id: 608
                        CType: 'uploads'
                        header: 'File links'
                        header_layout: 2
                        uploads_type: 2
                        filelink_size: 1
                        uploads_description: 1
                        filelink_sorting: 'name'
                        filelink_sorting_direction: 'asc'

                  # "html" - "bodytext" straight out, through "f:format.raw": no
                  # escaping, no parseFunc, no RTE. That is the entire element.
                  #
                  # Its "header" is not rendered - EXT:frontend relabels the
                  # field to "Name (not visible in frontend)" for this CType, the
                  # same way it does for "div" and "shortcut" - so the copy below
                  # has to carry its own heading, and it also introduces those
                  # two elements, which follow it and have no visible header
                  # either.
                  - self:
                        id: 609
                        CType: 'html'
                        header: 'Plain HTML (name only, not rendered)'
                        bodytext: |
                          <h2 class="theme-content-element__heading">Plain HTML</h2>
                          <p>This paragraph, its heading and the class on that heading
                          were written as raw markup in <code>bodytext</code> and
                          reached the page untouched. Nothing was escaped and no rich
                          text parsing ran, which is what separates this element from
                          "Text" further up - the <code>code</code> tags in this
                          sentence survive here in every installation. That
                          element's field is declared <em>enableRichtext</em>, so a
                          save keeps only what the processing of the rich text preset
                          allows: <code>code</code> with the theme's preset, nothing
                          of the kind without the rich text editor installed. Access to raw HTML is not restricted by
                          anything in the frontend: the <code>CType</code> field
                          carries <code>authMode = explicitAllow</code>, so a
                          non-admin editor can only pick it if a backend group grants
                          it explicitly.</p>
                          <p>Two elements follow that have no visible header either.
                          The horizontal rule below is the <strong>Divider</strong>
                          element, which renders nothing but that rule. After it,
                          <strong>Insert Records</strong> renders the page's own
                          "Core elements" heading a second time - it is a reference to
                          the first element on this page, not a copy of it.</p>

                  # "div" - a horizontal rule and nothing else. The core TCA gives
                  # this CType exactly one field besides "CType", and relabels it
                  # to "Name (not visible in frontend)", so the header below is
                  # there for the backend and the page module only.
                  - self:
                        id: 610
                        CType: 'div'
                        header: 'Divider before the record reference'

                  # "shortcut" - renders other content elements in place.
                  #
                  # "records" is a "type: group" field limited to "tt_content"
                  # (EXT:frontend/Configuration/TCA/tt_content.php). Its value is
                  # a comma separated list, each entry in the form
                  # "<tablename>_<uid>": "RecordsContentObject" hands it to
                  # "RelationHandler", which reverses the string and splits it on
                  # the first "_" from the right, so the id is the part after the
                  # last underscore and everything before it is the table name. A
                  # bare uid works too - a single allowed table makes it fall
                  # back to that table - but the qualified form is what the
                  # backend writes and what makes the scenario readable.
                  #
                  # This is also the element that demonstrates the cycle break of
                  # PR #19: inside a shortcut, "tt_content.shortcut" is redefined
                  # to render nothing, so a shortcut reached through a shortcut
                  # produces no output. TYPO3 v13.4 has a recursion guard of its
                  # own ("recordRegister"), v14 has none, and this element renders
                  # identically on both because the theme does not rely on it.
                  - self:
                        id: 611
                        CType: 'shortcut'
                        header: 'Insert records (name only, not rendered)'
                        records: 'tt_content_601'

              # One page per classic CType, below the page that shows each of
              # them once. The page is named after the CType and shows its
              # variants: every value of the field that changes how the
              # element looks, one element each. "ShowcaseTreeTest" derives
              # the CTypes from the TypoScript and the values from the TCA and
              # the page TSconfig, and holds these pages to both.
              #
              # "content_sidebar" like their parent, so the sub navigation
              # lists the sibling pages next to every one of them.
              children:
                # "header" - "header_layout" is the level, "header_position"
                # the alignment. The look independent of the level,
                # "tx_theme_header_style", is on the Frames page (uid 51).
                - self:
                      id: 30
                      title: 'Header'
                      slug: '/elements/core/header'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3001
                            CType: 'header'
                            header: 'Header'
                            header_layout: 1
                      - self:
                            id: 3002
                            CType: 'text'
                            header: 'Levels and positions'
                            header_layout: 2
                            bodytext: |
                              <p>The element is a heading and nothing else. Its level is the
                              value of "Layout" in the header palette, heading 1 to heading 5;
                              "Default" is a heading 2, and "Hidden" keeps the record and
                              renders nothing. The alignment moves the heading to the start,
                              the centre or the end of the line - the logical edges, so it
                              follows a right-to-left language.</p>
                      - self:
                            id: 3003
                            CType: 'header'
                            header: 'Heading level 2'
                            header_layout: 2
                      - self:
                            id: 3004
                            CType: 'header'
                            header: 'Heading level 3'
                            header_layout: 3
                      - self:
                            id: 3005
                            CType: 'header'
                            header: 'Heading level 4'
                            header_layout: 4
                      - self:
                            id: 3006
                            CType: 'header'
                            header: 'Heading level 5'
                            header_layout: 5
                      - self:
                            id: 3007
                            CType: 'header'
                            header: 'The default level, rendered as heading 2'
                            header_layout: 0
                      # Rendered as the wrapper alone: the record exists, the
                      # heading does not. Visible with the outlines switched on.
                      - self:
                            id: 3008
                            CType: 'header'
                            header: 'Hidden, and not displayed'
                            header_layout: 100
                      - self:
                            id: 3009
                            CType: 'header'
                            header: 'Centred'
                            header_layout: 2
                            header_position: 'center'
                      - self:
                            id: 3010
                            CType: 'header'
                            header: 'At the end of the line'
                            header_layout: 2
                            header_position: 'right'
                      - self:
                            id: 3011
                            CType: 'header'
                            header: 'At the start of the line'
                            header_layout: 2
                            header_position: 'left'
                      - self:
                            id: 3012
                            CType: 'header'
                            header: 'No alignment chosen'
                            header_layout: 2
                            header_position: ''

                # "text" - the rich text field and nothing else. The
                # typography section (below page 2) shows what rich text can
                # hold; this page shows the element.
                - self:
                      id: 31
                      title: 'Text'
                      slug: '/elements/core/text'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3101
                            CType: 'header'
                            header: 'Text'
                            header_layout: 1
                      - self:
                            id: 3102
                            CType: 'text'
                            header: 'Running text'
                            header_layout: 2
                            bodytext: |
                              <p>The text element renders its header and its rich text, and
                              the rich text goes through the parsing function of the frontend,
                              so a <a href="t3://page?uid=35">link to another page</a> is
                              resolved and <strong>strong</strong> and <em>emphasised</em>
                              words keep their meaning.</p>
                              <p>A second paragraph, so the space between two of them is on
                              the page as well.</p>
                      - self:
                            id: 3103
                            CType: 'text'
                            header: 'A list and a quotation inside the text'
                            header_layout: 2
                            bodytext: |
                              <p>Rich text holds lists and quotations of its own:</p>
                              <ul><li>an unordered list,</li><li>with a second item,</li></ul>
                              <ol><li>an ordered one,</li><li>numbered by the browser.</li></ol>
                              <blockquote><p>A quotation set apart from the text around it.</p></blockquote>
                      - self:
                            id: 3104
                            CType: 'text'
                            bodytext: |
                              <p>This element has no header, so it starts with its text.</p>

                # "textpic" - one element per value of "imageorient", in the
                # order the select offers them. The in-text values carry a
                # portrait image and the others a landscape one, and all share
                # one paragraph long enough to show where the text runs past
                # the image.
                - self:
                      id: 32
                      title: 'Text & Images'
                      slug: '/elements/core/textpic'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3201
                            CType: 'header'
                            header: 'Text & Images'
                            header_layout: 1
                      - self:
                            id: 3202
                            CType: 'textpic'
                            header: 'Above, centred (0)'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 1
                            bodytext: &textpicBody |
                              <p>The position of the image is the value of
                              <em>imageorient</em> in the heading. The core's
                              <strong>GalleryProcessor</strong> turns it into a vertical and a
                              horizontal position, and the gallery partial turns those into
                              modifier classes. This paragraph is long enough to run past an
                              image placed in the text, which is what tells the variants that
                              let the text flow back under the image from the ones that keep it
                              beside the image.</p>
                      - self:
                            id: 3203
                            CType: 'textpic'
                            header: 'Above, right (1)'
                            header_layout: 2
                            imageorient: 1
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3204
                            CType: 'textpic'
                            header: 'Above, left (2)'
                            header_layout: 2
                            imageorient: 2
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3205
                            CType: 'textpic'
                            header: 'Below, centred (8)'
                            header_layout: 2
                            imageorient: 8
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3206
                            CType: 'textpic'
                            header: 'Below, right (9)'
                            header_layout: 2
                            imageorient: 9
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3207
                            CType: 'textpic'
                            header: 'Below, left (10)'
                            header_layout: 2
                            imageorient: 10
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3208
                            CType: 'textpic'
                            header: 'In text, right (17)'
                            header_layout: 2
                            imageorient: 17
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3209
                            CType: 'textpic'
                            header: 'In text, left (18)'
                            header_layout: 2
                            imageorient: 18
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3210
                            CType: 'textpic'
                            header: 'In text, right, no wrap (25)'
                            header_layout: 2
                            imageorient: 25
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3211
                            CType: 'textpic'
                            header: 'In text, left, no wrap (26)'
                            header_layout: 2
                            imageorient: 26
                            imagecols: 1
                            bodytext: *textpicBody

                # "textmedia" - the same processors as "textpic" on the
                # "assets" field, so four orientations and three column counts
                # are enough to show it is the same layout.
                - self:
                      id: 33
                      title: 'Text & Media'
                      slug: '/elements/core/textmedia'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3301
                            CType: 'header'
                            header: 'Text & Media'
                            header_layout: 1
                      - self:
                            id: 3302
                            CType: 'textmedia'
                            header: 'Two files above the text, two columns'
                            header_layout: 2
                            imageorient: 0
                            imagecols: 2
                            bodytext: &textmediaBody |
                              <p>The same gallery as "Text &amp; Images", read from the
                              <em>assets</em> field, which also takes a video or an audio
                              file. Orientation and columns are the values in the heading.</p>
                      - self:
                            id: 3303
                            CType: 'textmedia'
                            header: 'One file in the text, right'
                            header_layout: 2
                            imageorient: 17
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3304
                            CType: 'textmedia'
                            header: 'One file in the text, left, no wrap'
                            header_layout: 2
                            imageorient: 26
                            imagecols: 1
                            bodytext: *textpicBody
                      - self:
                            id: 3305
                            CType: 'textmedia'
                            header: 'Three files below the text, three columns'
                            header_layout: 2
                            imageorient: 8
                            imagecols: 3
                            bodytext: *textmediaBody

                # "image" - one element per column count up to four, each with
                # as many files as columns. The select offers up to eight; at
                # the gallery width of the theme a fifth column is a row of
                # thumbnails, which the page has no reason to show.
                - self:
                      id: 34
                      title: 'Images'
                      slug: '/elements/core/image'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3401
                            CType: 'header'
                            header: 'Images'
                            header_layout: 1
                      - self:
                            id: 3402
                            CType: 'image'
                            header: 'One column'
                            header_layout: 2
                            imagecols: 1
                      - self:
                            id: 3403
                            CType: 'image'
                            header: 'Two columns'
                            header_layout: 2
                            imagecols: 2
                      - self:
                            id: 3404
                            CType: 'image'
                            header: 'Three columns'
                            header_layout: 2
                            imagecols: 3
                      - self:
                            id: 3405
                            CType: 'image'
                            header: 'Four columns'
                            header_layout: 2
                            imagecols: 4

                # "bullets" - every "layout" on an unordered list, two on an
                # ordered one, and the definition list, which reads
                # "term|description" per line and ignores the layout. See
                # "Templates/ContentElements/Bullets.html".
                - self:
                      id: 35
                      title: 'Bullet list'
                      slug: '/elements/core/bullets'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3501
                            CType: 'header'
                            header: 'Bullet list'
                            header_layout: 1
                      - self:
                            id: 3502
                            CType: 'bullets'
                            header: 'Bullets or numbers'
                            header_layout: 2
                            bullets_type: 0
                            layout: 0
                            bodytext: &bulletsBody |
                              One item per line, typed into a plain text field
                              Each line is escaped, so a typed tag stays text
                              The layout picks the marker of the whole list
                      - self:
                            id: 3503
                            CType: 'bullets'
                            header: 'Check marks'
                            header_layout: 2
                            bullets_type: 0
                            layout: 1
                            bodytext: *bulletsBody
                      # The icon of the list is picked in "tx_theme_icon", the
                      # icon picker of the theme, from the curated list of
                      # "IconPicker.tsconfig". Without one the layout renders
                      # the markers of layout 0 - see "Bullets.html".
                      - self:
                            id: 3504
                            CType: 'bullets'
                            header: 'Icons'
                            header_layout: 2
                            bullets_type: 0
                            layout: 2
                            tx_theme_icon: 'star'
                            bodytext: *bulletsBody
                      - self:
                            id: 3505
                            CType: 'bullets'
                            header: 'Inline'
                            header_layout: 2
                            bullets_type: 0
                            layout: 3
                            bodytext: |
                              Header
                              Text
                              Bullet list
                              Table
                              File links
                      - self:
                            id: 3506
                            CType: 'bullets'
                            header: 'Numbered'
                            header_layout: 2
                            bullets_type: 1
                            layout: 0
                            bodytext: |
                              Pick the list type "Numbered list"
                              Type one step per line
                              Save, and the browser numbers the steps
                      - self:
                            id: 3507
                            CType: 'bullets'
                            header: 'Numbered, with check marks'
                            header_layout: 2
                            bullets_type: 1
                            layout: 1
                            bodytext: |
                              The list stays ordered for a screen reader
                              The check mark replaces the number on screen
                      - self:
                            id: 3508
                            CType: 'bullets'
                            header: 'Definition list'
                            header_layout: 2
                            bullets_type: 2
                            bodytext: |
                              Bullets or numbers|The markers of the element baseline
                              Check marks|A check mark in front of every item
                              Icons|One icon of the set in front of every item
                              Inline|The items on one line, a hairline between them
                              A term without a description

                # "table" - one element per "table_class": the three of the
                # core TCA and the five "TCEFORM/TableClass.tsconfig" adds, all
                # on the same rows so only the class differs.
                - self:
                      id: 36
                      title: 'Table'
                      slug: '/elements/core/table'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3601
                            CType: 'header'
                            header: 'Table'
                            header_layout: 1
                      - self:
                            id: 3602
                            CType: 'table'
                            header: 'Default, with a header row and a footer'
                            header_layout: 2
                            table_class: ''
                            table_caption: 'The spacing scale'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            table_tfoot: 1
                            bodytext: &tableBody |
                              Value|Token|Used for
                              extra-small|--theme-space-2|Tight groups
                              small|--theme-space-4|Related elements
                              medium|--theme-space-6|The default gap
                              large|--theme-space-8|A new topic
                              extra-large|--theme-space-10|A new section
                      - self:
                            id: 3603
                            CType: 'table'
                            header: 'Striped'
                            header_layout: 2
                            table_class: 'striped'
                            table_caption: 'The spacing scale, striped'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3604
                            CType: 'table'
                            header: 'Bordered'
                            header_layout: 2
                            table_class: 'bordered'
                            table_caption: 'The spacing scale, bordered'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3605
                            CType: 'table'
                            header: 'Striped columns'
                            header_layout: 2
                            table_class: 'striped-columns'
                            table_caption: 'The spacing scale, striped columns'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3606
                            CType: 'table'
                            header: 'Hover'
                            header_layout: 2
                            table_class: 'hover'
                            table_caption: 'The spacing scale, a tint on the row under the pointer'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3607
                            CType: 'table'
                            header: 'Borderless'
                            header_layout: 2
                            table_class: 'borderless'
                            table_caption: 'The spacing scale, borderless'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody
                      - self:
                            id: 3608
                            CType: 'table'
                            header: 'Compact, with a header column'
                            header_layout: 2
                            table_class: 'compact'
                            table_caption: 'The spacing scale, compact'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 2
                            bodytext: *tableBody
                      - self:
                            id: 3609
                            CType: 'table'
                            header: 'Sticky header'
                            header_layout: 2
                            table_class: 'sticky-header'
                            table_caption: 'The spacing scale, the header row stays in view'
                            table_delimiter: 124
                            table_enclosure: 0
                            table_header_position: 1
                            bodytext: *tableBody

                # "uploads" - one element per "uploads_type". The two files
                # and their references are declared in "config.yml".
                - self:
                      id: 37
                      title: 'File links'
                      slug: '/elements/core/uploads'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3701
                            CType: 'header'
                            header: 'File links'
                            header_layout: 1
                      - self:
                            id: 3702
                            CType: 'uploads'
                            header: 'Only the file name (0)'
                            header_layout: 2
                            uploads_type: 0
                      # The theme has no file type icon, so 1 renders as 0 -
                      # see "Uploads.html". Seeded anyway, so the value is on
                      # the page next to the one it renders like.
                      - self:
                            id: 3703
                            CType: 'uploads'
                            header: 'File name and file type icon (1)'
                            header_layout: 2
                            uploads_type: 1
                      - self:
                            id: 3704
                            CType: 'uploads'
                            header: 'File name and thumbnail, with size and description (2)'
                            header_layout: 2
                            uploads_type: 2
                            filelink_size: 1
                            uploads_description: 1

                # "div" - a rule between two elements, and nothing else.
                - self:
                      id: 38
                      title: 'Divider'
                      slug: '/elements/core/div'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3801
                            CType: 'header'
                            header: 'Divider'
                            header_layout: 1
                      - self:
                            id: 3802
                            CType: 'text'
                            header: 'Before the divider'
                            header_layout: 2
                            bodytext: '<p>The divider below is an element of its own. Its only field is a name for the backend.</p>'
                      - self:
                            id: 3803
                            CType: 'div'
                            header: 'The divider between the two texts'
                      - self:
                            id: 3804
                            CType: 'text'
                            header: 'After the divider'
                            header_layout: 2
                            bodytext: '<p>A text element again, below the rule.</p>'

                # "html" - raw markup, unescaped and unparsed. Its header is
                # a name for the backend, so the markup brings its own
                # heading.
                - self:
                      id: 39
                      title: 'Plain HTML'
                      slug: '/elements/core/html'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 3901
                            CType: 'header'
                            header: 'Plain HTML'
                            header_layout: 1
                      - self:
                            id: 3902
                            CType: 'html'
                            header: 'Keyboard input and a disclosure (name only, not rendered)'
                            bodytext: |
                              <h2 class="theme-content-element__heading">Keyboard input and a disclosure</h2>
                              <p>Written as markup and rendered as written: press
                              <kbd><kbd>Ctrl</kbd> + <kbd>K</kbd></kbd> to insert a link in the
                              rich text editor. Nothing here was escaped, and no rich text
                              processing ran over it.</p>
                              <details>
                                <summary>Who may use this element?</summary>
                                <p>An administrator, and an editor whose backend group allows the
                                CType explicitly: the field carries "authMode = explicitAllow".</p>
                              </details>

                # "shortcut" - other elements rendered in place: one, and two
                # in the order the field lists them.
                - self:
                      id: 50
                      title: 'Insert records'
                      slug: '/elements/core/shortcut'
                      backend_layout: 'content_sidebar'
                  entities:
                    content:
                      - self:
                            id: 5001
                            CType: 'header'
                            header: 'Insert records'
                            header_layout: 1
                      - self:
                            id: 5002
                            CType: 'text'
                            header: 'What follows is not on this page'
                            header_layout: 2
                            bodytext: |
                              <p>The two elements below this text reference elements of the
                              pages "Bullet list" and "Table". They are rendered here from the
                              records on those pages, not copied: change one there, and it
                              changes here.</p>
                      - self:
                            id: 5003
                            CType: 'shortcut'
                            header: 'The check list of the bullet list page (name only, not rendered)'
                            records: 'tt_content_3503'
                      - self:
                            id: 5004
                            CType: 'shortcut'
                            header: 'The inline list, then the striped table (name only, not rendered)'
                            records: 'tt_content_3505,tt_content_3603'

            # Every "menu_*" content element the theme renders, each pointed at a
            # different part of the seeded tree so the eleven are told apart by
            # what they list rather than by their header alone.
            #
            # "backend_layout: content_sidebar" is deliberate and is asserted:
            # page 8 is a sibling under the same parent and does *not* use it, so
            # the pair prove the layout is resolved per page rather than
            # inherited down the branch.
            - self:
                  id: 7
                  title: 'Menu elements'
                  slug: '/elements/menu'
                  backend_layout: 'content_sidebar'
                  # Read by the "menu_abstract" element below, which lists the
                  # children of page 5 - this page among them. Without an
                  # abstract the item still renders, just as a bare link
                  # ("showAbstract" is guarded by "{item.data.abstract}" in
                  # "Partials/ContentElement/Menu.html"), and nothing says why.
                  abstract: 'The eleven menu content elements, each pointed at a different part of this page tree.'
                  # The keyword source of the "menu_related_pages" element below.
                  # "special = keywords" reads the keywords of the page selected
                  # in "pages" and matches every *other* page under the site root
                  # that shares one, so this page is the source and is itself
                  # excluded from the result.
                  keywords: 'content elements'
              entities:
                content:
                  - self:
                        id: 701
                        CType: 'header'
                        header: 'Menu elements'
                        header_layout: 1

                  # "special = list" - "pages" names the pages to render, in the
                  # order they are listed; nothing is looked up by rootline
                  # position. Three top level pages, so the list is visibly a
                  # hand-picked selection rather than a branch of the tree.
                  - self:
                        id: 702
                        CType: 'menu_pages'
                        header: 'menu_pages: a hand-picked list of pages'
                        header_layout: 2
                        pages: '2,3,4'

                  # "special = directory" - the *children* of the selected pages,
                  # not the pages themselves. Page 5 is the parent of this page,
                  # so this renders its three siblings.
                  - self:
                        id: 703
                        CType: 'menu_subpages'
                        header: 'menu_subpages: the children of one page'
                        header_layout: 2
                        pages: '5'

                  # "special = list" plus "levels = 2" - the selected page itself
                  # *and* one level of its own children. Same starting point as
                  # "menu_subpages" above, so the two side by side show what the
                  # second level adds.
                  - self:
                        id: 704
                        CType: 'menu_section'
                        header: 'menu_section: one page and its children'
                        header_layout: 2
                        pages: '5'

                  # "special = directory" plus "levels = 2" - the children of the
                  # selected page, each with their own children beneath them.
                  # Rooted at the home page, so the "Elements" branch appears
                  # with its three sub pages nested under it.
                  - self:
                        id: 705
                        CType: 'menu_section_pages'
                        header: 'menu_section_pages: two levels below one page'
                        header_layout: 2
                        pages: '1'

                  # No source field at all: the TCA of this type has no "pages"
                  # (EXT:frontend,
                  # "Overrides/280-tt_content-content_type-menu_sitemap.php"),
                  # and the TypoScript sets no "special" either - it always
                  # starts at the site root and walks seven levels down.
                  - self:
                        id: 706
                        CType: 'menu_sitemap'
                        header: 'menu_sitemap: the whole site from its root'
                        header_layout: 2

                  # The same seven-level walk as "menu_sitemap", rooted at the
                  # selected page instead of the site root. This tree is only
                  # three levels deep, so what it renders here is the same single
                  # level "menu_subpages" above renders - the depth has nothing
                  # further to reach, and that is a property of the demo tree,
                  # not of the element.
                  - self:
                        id: 707
                        CType: 'menu_sitemap_pages'
                        header: 'menu_sitemap_pages: a whole branch from one page'
                        header_layout: 2
                        pages: '5'

                  # "special = directory" again, with each item's own "abstract"
                  # shown beneath its link. Rooted at page 5, so the items are
                  # pages 6, 7 and 8 - each of which needs an "abstract" of its
                  # own for this element to show anything but bare links.
                  - self:
                        id: 708
                        CType: 'menu_abstract'
                        header: 'menu_abstract: children with their abstracts'
                        header_layout: 2
                        pages: '5'

                  # "special = updated" - the selected page and everything
                  # beneath it, sorted by "SYS_LASTCHANGED" descending. That
                  # column is 0 on a freshly seeded tree and is only written when
                  # a page is first rendered in the frontend, so the links are
                  # all there from the start and the dates appear page by page as
                  # the tree is browsed. The theme sets no "maxAge", so nothing
                  # is filtered out by age.
                  - self:
                        id: 709
                        CType: 'menu_recently_updated'
                        header: 'menu_recently_updated: the tree by last change'
                        header_layout: 2
                        pages: '1'

                  # "special = keywords" - "pages" names the page whose keywords
                  # are the search term, not the pages to list. This page is that
                  # source (see "keywords" on the page above); the result is
                  # every other page under the site root sharing one of them.
                  - self:
                        id: 710
                        CType: 'menu_related_pages'
                        header: 'menu_related_pages: pages sharing a keyword'
                        header_layout: 2
                        pages: '7'

                  # No category is selected, deliberately: this tree seeds no
                  # "sys_category" records, so there is nothing to select. An
                  # empty selection renders an empty menu, which is the correct
                  # rendering of "nothing selected".
                  #
                  # "selected_categories: 0" rather than leaving the field out:
                  # it is the value DataHandler itself normalises an
                  # unresolvable selection to, so it is what the column holds
                  # once anything real has touched it, and it keeps this element
                  # identical to its sibling below so neither reads as the
                  # special case.
                  - self:
                        id: 711
                        CType: 'menu_categorized_pages'
                        header: 'menu_categorized_pages: pages by category'
                        header_layout: 2
                        selected_categories: 0
                        category_field: 'categories'

                  # The same deliberate gap as above.
                  #
                  # The explicit 0 used to be load-bearing: the TypoScript
                  # interpolated the field straight into "uid_local IN (...)",
                  # and an empty value made that "IN ()" - accepted by SQLite,
                  # rejected as a syntax error by MariaDB, MySQL and PostgreSQL,
                  # taking the whole page down rather than just the element. That
                  # is now guarded where it belongs, in
                  # "ContentElements.typoscript", because an editor who saves the
                  # element before picking a category hits the same path and no
                  # seed value can help them.
                  - self:
                        id: 712
                        CType: 'menu_categorized_content'
                        header: 'menu_categorized_content: content by category'
                        header_layout: 2
                        selected_categories: 0
                        category_field: 'categories'

            # Every content element this extension registers itself, so the
            # thirteen "theme_*" CTypes can be looked at side by side rather than
            # one fixture at a time.
            #
            # No "content_sidebar" here, unlike its two siblings 6 and 7: the
            # layout is resolved per page, not inherited down the branch, and
            # this page is what proves it.
            - self:
                  id: 8
                  title: 'Theme elements'
                  slug: '/elements/theme'
                  backend_layout: 'content'
                  # As on page 6: the abstract and the keyword are read by the
                  # two menu elements on page 7, not by anything on this page.
                  abstract: 'The thirteen content elements this extension registers itself, from the hero down to the accordion.'
                  keywords: 'content elements, theme'
              entities:
                content:
                  # theme_hero - the full hero: heading, lead, media and one call
                  # to action. Its own template defaults an unset
                  # "header_layout" to h1 (Partials/ContentElement/Hero.html),
                  # which is right for a page whose hero is its top heading and
                  # wrong for a page carrying three of them, so the level is set
                  # explicitly here.
                  - self:
                        id: 801
                        CType: 'theme_hero'
                        header: 'The full hero'
                        header_layout: 2
                        bodytext: |
                          <p>The hero carries a heading, a lead paragraph, one media
                          slot and a single call to action. It is the widest element
                          the theme ships and is meant to open a page.</p>
                        tx_theme_link: 't3://page?uid=2'
                        tx_theme_link_label: 'Read the typography page'
                        # The empty string is a real item of the select, not a
                        # missing value: it is the default variant and renders the
                        # plain ".theme-button" with no modifier.
                        tx_theme_link_variant: ''
                        # An icon of the shipped set, before the label. The four
                        # link styles are spread over 801 to 805: primary here,
                        # secondary 802, ghost 803 and 805, link 804.
                        tx_theme_link_icon: 'arrow-right'
                  # theme_hero_small - the same field set as theme_hero, rendered
                  # through the same partial with "compact: 1", so the difference
                  # between the two is only visible with both on one page.
                  - self:
                        id: 802
                        CType: 'theme_hero_small'
                        header: 'The reduced hero'
                        header_layout: 2
                        bodytext: |
                          <p>The same fields as the full hero, rendered compactly. The
                          element above and this one differ in nothing an editor
                          fills in - only in the variant that was chosen.</p>
                        tx_theme_link: 't3://page?uid=3'
                        tx_theme_link_label: 'Look at the media page'
                        tx_theme_link_variant: 'secondary'
                  # theme_hero_text_only - no "image" field on the CType at all,
                  # so no file reference either. The hero renders without its
                  # "theme-hero--media" modifier.
                  - self:
                        id: 803
                        CType: 'theme_hero_text_only'
                        header: 'The hero without media'
                        header_layout: 2
                        bodytext: |
                          <p>This variant has no image field in the backend form, so
                          there is nothing to leave empty. What it demonstrates is the
                          hero body on its own, full width.</p>
                        tx_theme_link: 't3://page?uid=1'
                        tx_theme_link_label: 'Back to the start page'
                        tx_theme_link_variant: 'ghost'
                  # theme_teaser - a short block of text with a call to action
                  # and no media. Like theme_hero_text_only it has no "image"
                  # field, which is why the teaser partial renders without its
                  # "theme-teaser__media" part.
                  - self:
                        id: 804
                        CType: 'theme_teaser'
                        header: 'A teaser without media'
                        bodytext: |
                          <p>A teaser is a secondary block: a heading, a couple of
                          sentences and one link. Its heading defaults to h2 rather
                          than the hero's h1.</p>
                        tx_theme_link: 't3://page?uid=4'
                        tx_theme_link_label: 'Open the empty page'
                        tx_theme_link_variant: 'link'
                  # theme_media_teaser - the same teaser with an image beside the
                  # text. The link is an external URL rather than a page, because
                  # "tx_theme_link" is a TCA type=link field and holds whatever
                  # the link wizard encoded - a page, a file, a mail address or,
                  # as here, a plain URL.
                  - self:
                        id: 805
                        CType: 'theme_media_teaser'
                        header: 'A teaser beside an image'
                        bodytext: |
                          <p>Text and a single image side by side. The image is one
                          file reference; the theme deliberately wires no gallery
                          processing here, because there is no grid to arrange.</p>
                        tx_theme_link: 'https://docs.typo3.org/'
                        tx_theme_link_label: 'Read the TYPO3 documentation'
                        tx_theme_link_variant: 'ghost'
                  # theme_testimonial - a quotation with its attribution.
                  # "header" and "subheader" are relabelled on this CType to hold
                  # the attributed person's name and role, not a content heading.
                  #
                  # No media, and no file reference: the element renders through
                  # ".theme-quote", which has no media slot, and the image field
                  # was removed from the CType rather than left inert. Do not add
                  # one back without giving the component a slot first.
                  - self:
                        id: 806
                        CType: 'theme_testimonial'
                        header: 'Ada Lovelace'
                        subheader: 'Mathematician, 1843'
                        bodytext: |
                          <p>The Analytical Engine weaves algebraic patterns just as
                          the Jacquard loom weaves flowers and leaves.</p>
                  # theme_author - a person: portrait, name, role, a short bio and
                  # the person's own links. The name is the content element's
                  # heading and goes through the shared header partial; the role
                  # is "subheader" and sits inside the component.
                  #
                  # "tx_theme_list_items" is the inline relation: the declared
                  # ids of its children, in the order they are rendered.
                  - self:
                        id: 807
                        CType: 'theme_author'
                        header: 'Grace Hopper'
                        subheader: 'Compiler pioneer'
                        bodytext: |
                          <p>Wrote the first compiler and spent the rest of her career
                          arguing that a programming language should read like
                          English.</p>
                        tx_theme_list_items: '1,2'
                  # theme_linklist - a flat list of links. "header" is the list
                  # title here, not a content heading, which is why the CType
                  # offers the bare field rather than the headers palette.
                  #
                  # The list names its children out of uid order on purpose:
                  # 5, 3, 6, 4 renders "Typography", "Media", "Empty page" and
                  # the unlabelled link, while the uid order - which is also
                  # the order they are created in - would put "Media" first.
                  # Only the relation can produce the rendered order, which is
                  # what lets "ShowcaseTreeTest" tell the relation from the uid
                  # or the insertion order.
                  - self:
                        id: 808
                        CType: 'theme_linklist'
                        header: 'Where to read more'
                        tx_theme_list_items: '5,3,6,4'
                  # theme_sociallinks - the same list, rendered as text labels:
                  # the logos of the platforms are Font Awesome's brands set,
                  # which the theme does not ship, so "link_label" carries the
                  # platform name. The child "link" is
                  # restricted to url, email and telephone on this relation, so
                  # each of the three appears once.
                  - self:
                        id: 809
                        CType: 'theme_sociallinks'
                        header: 'Elsewhere'
                        tx_theme_list_items: '7,8,9'
                  # theme_media_teaser_grid - several cards in a grid. "header"
                  # and "bodytext" are the lead-in above the grid; the cards
                  # themselves are the same child table, here with the full
                  # header/text/image/link set the other three relations narrow
                  # away.
                  - self:
                        id: 810
                        CType: 'theme_media_teaser_grid'
                        header: 'Three cards in a grid'
                        bodytext: |
                          <p>Every card is one row of the shared child table. The
                          lead-in above is the element's own heading and body.</p>
                        tx_theme_list_items: '10,11,12'
                  # theme_notice - the six kinds of ".theme-alert", one element
                  # each. The kind decides the modifier and the "role" together
                  # ("Templates/ContentElements/ThemeNotice.html"): "status" for
                  # info and success, "alert" for warning and danger, "note" for
                  # note and tip. "header" is the title inside the component,
                  # not a content heading, and "bodytext" is rich text on this
                  # CType, so it is written as a paragraph.
                  - self:
                        id: 811
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'note'
                        header: 'A note'
                        bodytext: '<p>An aside to the text around it. It takes the note role and is never a live region, because nothing about it ever changes. It is also the kind a notice gets when nobody chose one.</p>'
                  - self:
                        id: 812
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'info'
                        header: 'Information'
                        bodytext: '<p>The neutral kind, and the look of the bare class. It takes the status role, a polite live region that waits for a pause before it is read.</p>'
                  - self:
                        id: 813
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'tip'
                        header: 'A tip'
                        bodytext: '<p>A recommendation, and the one kind that follows the palette: switch the palette in the display settings, and this is the only notice that changes colour.</p>'
                  - self:
                        id: 814
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'success'
                        header: 'Something finished'
                        bodytext: '<p>Confirms that something went through. It takes the status role, like the information.</p>'
                  - self:
                        id: 815
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'warning'
                        header: 'A warning'
                        bodytext: '<p>Something needs attention before it becomes a problem. It takes the alert role, the assertive one.</p>'
                  - self:
                        id: 816
                        CType: 'theme_notice'
                        tx_theme_notice_kind: 'danger'
                        header: 'Something failed'
                        bodytext: '<p>Assertive like the warning, with a glyph of its own: once colours are forced, the glyph is what tells the six kinds apart.</p>'
                  # theme_tabs - one tab per child. The ids of the tabs and
                  # panels are derived from the anchor of the element, "c817",
                  # so a second tab group on the page cannot collide with this
                  # one. The children's "text" is rich text on this relation
                  # only; the second one links to the start page to show it.
                  - self:
                        id: 817
                        CType: 'theme_tabs'
                        header: 'Three tabs'
                        header_layout: 2
                        tx_theme_list_items: '13,14,15'
                  # theme_accordion - native "details" sharing one "name",
                  # "c818-accordion", so opening one item closes the others.
                  - self:
                        id: 818
                        CType: 'theme_accordion'
                        header: 'An accordion'
                        header_layout: 2
                        tx_theme_list_items: '16,17,18'

                # The children of the four relations above. A relation is not a
                # containment: a child is an ordinary record on this page, and
                # only the relation columns DataHandler writes tie it to its
                # parent element.
                listItem:
                  # theme_author, uid 807. This relation is narrowed to the link
                  # palette on that CType, so a child carries "link" and
                  # "link_label" and nothing else - "header", "text" and "image"
                  # are not part of the form there and would be dead data.
                  - self:
                        id: 1
                        link: 't3://page?uid=1'
                        link_label: 'Profile'
                  - self:
                        id: 2
                        # TYPO3 link syntax, not an "href": an email address is
                        # stored with its "mailto:" scheme.
                        link: 'mailto:grace@example.org'
                        link_label: 'Write an email'

                  # theme_linklist, uid 808, which lists these as 5, 3, 6, 4.
                  - self:
                        id: 3
                        link: 't3://page?uid=3'
                        link_label: 'Media'
                  # No "link_label" on purpose: the label is optional, and a row
                  # without one falls back to the resolved URL rather than
                  # rendering an empty link. This entry is what shows that
                  # fallback in the seeded tree.
                  - self:
                        id: 4
                        link: 'https://typo3.org/'
                  - self:
                        id: 5
                        link: 't3://page?uid=2'
                        link_label: 'Typography'
                        # "link_icon" is part of the link palette of every
                        # relation; one row of the list shows it.
                        link_icon: 'book-open'
                  - self:
                        id: 6
                        link: 't3://page?uid=4'
                        link_label: 'Empty page'

                  # theme_sociallinks, uid 809.
                  - self:
                        id: 7
                        link: 'https://typo3.org/'
                        link_label: 'TYPO3 on the web'
                  - self:
                        id: 8
                        link: 'mailto:hello@example.org'
                        link_label: 'Send an email'
                  - self:
                        id: 9
                        # "tel:" plus digits, "+", "," and ";" - anything else is
                        # stripped when the link wizard encodes a number.
                        link: 'tel:+4930123456'
                        link_label: 'Call the office'

                  # theme_media_teaser_grid, uid 810. The images of the three
                  # cards are attached in "config.yml".
                  #
                  # The child's "text" is a plain text column, never RTE-enabled,
                  # and is rendered through nl2br - so it holds sentences and
                  # line breaks, never markup.
                  - self:
                        id: 10
                        header: 'Typography'
                        text: 'Headings, running text and the inline cases a stylesheet has to answer for.'
                        link: 't3://page?uid=2'
                        link_label: 'Open the typography page'
                        link_icon: 'arrow-right'
                  - self:
                        id: 11
                        header: 'Media'
                        text: 'A single image and a two column gallery, both driven by the fields of the element.'
                        link: 't3://page?uid=3'
                        link_label: 'Open the media page'
                  # No "link" on this one: unlike the other three relations, the
                  # grid does not require it, and a card without a link renders
                  # its title and text with no ".theme-card__link". That shape is
                  # part of what this page demonstrates.
                  - self:
                        id: 12
                        header: 'A card without a link'
                        text: |
                          The link is optional on this relation only.
                          A card without one is a plain block of text.

                  # theme_tabs, uid 817. Title and rich text, nothing else: the
                  # relation is narrowed to those two on that CType. The text is
                  # stored as written - "overrideChildTca" enables the editor in
                  # the form only, and DataHandler runs no rich text
                  # transformation for it (see "tt_content_theme_tabs.php").
                  - self:
                        id: 13
                        header: 'Site set'
                        text: '<p>A site depends on the site set of the theme, and constants, page TSconfig and setup arrive together. This is the first tab, the one the markup selects.</p>'
                  - self:
                        id: 14
                        header: 'Static include'
                        text: '<p>An installation without site sets includes the static TypoScript in a template record instead: the same files, read the same way. The <a href="t3://page?uid=1">start page</a> of the second tree of a development instance is delivered like that.</p>'
                  - self:
                        id: 15
                        header: 'Without the script'
                        text: '<p>Switch JavaScript off and reload: the tab list is gone, and every panel is shown in a frame of its own, under a heading repeating its tab.</p>'

                  # theme_accordion, uid 818.
                  - self:
                        id: 16
                        header: 'What makes the items exclusive?'
                        text: '<p>One <em>name</em> shared by every item of this element. Open another item and this one closes, with no script involved.</p>'
                  - self:
                        id: 17
                        header: 'Why is the name derived from the element?'
                        text: '<p>The browser groups items by name across the whole page, so two accordions sharing one would close each other. The name is the anchor of the element with a suffix.</p>'
                  - self:
                        id: 18
                        header: 'Is anything open at first?'
                        text: '<p>No. Which item a reader meets first is what the order of the items already says.</p>'

            # The appearance fields of a content element, every value once:
            # the frames, the space before and after, the alignment and the
            # look of the header. Text elements throughout, so nothing but
            # the field under demonstration differs between two of them.
            # "ShowcaseTreeTest" reads the values the form offers - TCA and
            # page TSconfig - and holds this page to every one of them.
            #
            # The spacing elements sit on the surface band, so the edge the
            # space is measured from is on the page.
            - self:
                  id: 51
                  title: 'Frames'
                  slug: '/elements/frames'
                  backend_layout: 'content'
                  abstract: 'Every frame, every spacing and every header alignment and look of the Appearance tab, one element each.'
                  keywords: 'content elements, appearance'
              entities:
                content:
                  - self:
                        id: 5101
                        CType: 'header'
                        header: 'Frames'
                        header_layout: 1
                  - self:
                        id: 5102
                        CType: 'text'
                        header: 'The Appearance tab'
                        header_layout: 2
                        bodytext: |
                          <p>Every content element carries the same appearance fields,
                          whatever it renders. The frame fills the element with a band, the
                          spacing moves it away from its neighbours, and the header palette
                          aligns the heading and sets its look apart from its level. The
                          elements below are text elements, one per value.</p>
                  - self:
                        id: 5103
                        CType: 'text'
                        header: 'Frame: Default'
                        header_layout: 2
                        frame_class: 'default'
                        bodytext: &frameBody |
                          <p>Running text in the frame of the heading, with
                          <a href="t3://page?uid=51">a link</a> and <strong>strong</strong>
                          words, so the colours of the band are tested on more than one
                          role.</p>
                  - self:
                        id: 5104
                        CType: 'text'
                        header: 'Frame: No frame'
                        header_layout: 2
                        frame_class: 'none'
                        bodytext: *frameBody
                  - self:
                        id: 5105
                        CType: 'text'
                        header: 'Frame: Surface band'
                        header_layout: 2
                        frame_class: 'surface'
                        bodytext: *frameBody
                  - self:
                        id: 5106
                        CType: 'text'
                        header: 'Frame: Raised band'
                        header_layout: 2
                        frame_class: 'raised'
                        bodytext: *frameBody
                  - self:
                        id: 5107
                        CType: 'text'
                        header: 'Frame: Accent band'
                        header_layout: 2
                        frame_class: 'accent'
                        bodytext: *frameBody
                  - self:
                        id: 5108
                        CType: 'text'
                        header: 'Frame: Inverse band'
                        header_layout: 2
                        frame_class: 'inverse'
                        bodytext: *frameBody
                  - self:
                        id: 5109
                        CType: 'text'
                        header: 'Space before and after: extra small'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'extra-small'
                        space_after_class: 'extra-small'
                        bodytext: &spaceBody '<p>The space before and after this element is the value in its heading.</p>'
                  - self:
                        id: 5110
                        CType: 'text'
                        header: 'Space before and after: small'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'small'
                        space_after_class: 'small'
                        bodytext: *spaceBody
                  - self:
                        id: 5111
                        CType: 'text'
                        header: 'Space before and after: medium'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'medium'
                        space_after_class: 'medium'
                        bodytext: *spaceBody
                  - self:
                        id: 5112
                        CType: 'text'
                        header: 'Space before and after: large'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'large'
                        space_after_class: 'large'
                        bodytext: *spaceBody
                  - self:
                        id: 5113
                        CType: 'text'
                        header: 'Space before and after: extra large'
                        header_layout: 2
                        frame_class: 'surface'
                        space_before_class: 'extra-large'
                        space_after_class: 'extra-large'
                        bodytext: *spaceBody
                  - self:
                        id: 5114
                        CType: 'text'
                        header: 'Header centred'
                        header_layout: 2
                        header_position: 'center'
                        bodytext: &headerBody '<p>The heading of this element is set as its heading says.</p>'
                  - self:
                        id: 5115
                        CType: 'text'
                        header: 'Header at the end of the line'
                        header_layout: 2
                        header_position: 'right'
                        bodytext: *headerBody
                  - self:
                        id: 5116
                        CType: 'text'
                        header: 'Header at the start of the line'
                        header_layout: 2
                        header_position: 'left'
                        bodytext: *headerBody
                  # The looks, on a heading 3 each: the level stays the
                  # outline, the look is how loud the heading is.
                  - self:
                        id: 5117
                        CType: 'text'
                        header: 'A heading 3 in the display size'
                        header_layout: 3
                        tx_theme_header_style: 'display'
                        bodytext: *headerBody
                  - self:
                        id: 5118
                        CType: 'text'
                        header: 'A heading 3 like heading 1'
                        header_layout: 3
                        tx_theme_header_style: 'h1'
                        bodytext: *headerBody
                  - self:
                        id: 5119
                        CType: 'text'
                        header: 'A heading 3 like heading 2'
                        header_layout: 3
                        tx_theme_header_style: 'h2'
                        bodytext: *headerBody
                  - self:
                        id: 5120
                        CType: 'text'
                        header: 'A heading 3 like heading 3'
                        header_layout: 3
                        tx_theme_header_style: 'h3'
                        bodytext: *headerBody
                  - self:
                        id: 5121
                        CType: 'text'
                        header: 'A heading 3 like heading 4'
                        header_layout: 3
                        tx_theme_header_style: 'h4'
                        bodytext: *headerBody
                  - self:
                        id: 5122
                        CType: 'text'
                        header: 'A heading 3 like heading 5'
                        header_layout: 3
                        tx_theme_header_style: 'h5'
                        bodytext: *headerBody

        # The styleguide. Not a showcase of content elements: the "styleguide"
        # backend layout declares no usable content column at all, and
        # "Templates/Page/Styleguide.html" renders the component library
        # straight from Fluid, ignoring anything placed on the page.
        #
        # "nav_hide", never "hidden". A hidden page returns 404 in the frontend
        # and is only reachable through a backend preview link carrying a valid
        # hash, which defeats the point of seeding it - the page exists to be
        # opened. "nav_hide" keeps it reachable by URL and out of every menu,
        # which is what "not published" meant here.
        - self:
              id: 9
              title: 'Styleguide'
              slug: '/styleguide'
              backend_layout: 'styleguide'
              nav_hide: 1

        # The form showcase. A request form built from the form contract of the
        # component library, rendered by "Templates/Page/Forms.html" from the
        # "forms" backend layout - which, like "styleguide", declares no usable
        # content column: the form is the page. "nav_hide" for the reason given
        # above; the start page links to both.
        - self:
              id: 10
              title: 'Forms'
              slug: '/forms'
              backend_layout: 'forms'
              nav_hide: 1
